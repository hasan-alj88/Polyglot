# PAUL Handoff

**Date:** 2026-03-27
**Status:** context-limit

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** Polyglot — async programming language and compilation service
**Branch:** `design/issue-75-add-map-k-v-parameterized-string-subtype`
**Issue:** #75 (dict:K:V) + #88 (schema properties prerequisite)

---

## Current State

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY ──▶ MERGE
  ✓        ✓        ✓        ○     [75-02 loop closed, 75-03 needed]
```

Plans 75-01 (gap resolution) and 75-02 (core spec rewrite) are COMPLETE.

**Plan 75-03 needs to be CREATED and executed.** It covers:
1. Technical spec updates (EBNF, metadata-tree, compile rules)
2. Stdlib type definitions (add #String, #Int, #Array, #Dict, etc. to stdlib/types/types.md)

**Plan 75-04 needs to be CREATED and executed after 75-03.** It covers:
- Spec-wide `;` → `#` migration across ~120 remaining files (~1,467 occurrences)

---

## What Was Done This Session

1. Resumed from previous handoff, restored context
2. Created Plan 75-02 (core spec rewrite — types.md + collections.md)
3. Executed Plan 75-02:
   - **Rewrote `docs/user/syntax/types.md`** — complete ground-up type hierarchy:
     - Layer 0: RawString (compiler intrinsic)
     - Layer 1: #String (foundation — .string + .re)
     - Layer 2: #Int, #UnsignedInt, #Float, #Sci, #Eng, #Dimension (all with `<~ #String`)
     - Layer 2b: #Boolean (independent enum struct)
     - Schema properties section (%Key.Type, %Key.Gap, %Ordered, %Depth.Max, %Alias)
     - Generic type parameters (`<` prefix, #* wildcard, [<] constraints)
     - All `;type` → `#type` annotations migrated
     - Array access changed from `.` to `:` ($arr:0 not $arr.0)
   - **Updated `docs/user/concepts/collections.md`**:
     - Collection table expanded to 5 entries (#array, #dict, #dataframe, #serial, struct)
     - Added #Dict section (typed K-V pairs)
     - Added #Dataframe section (array of dicts, tabular data)
     - All `;type` → `#type` annotations migrated
     - %Open removed
4. Unified Plan 75-02 — SUMMARY created

---

## What's Next

**Immediate:** Create Plan 75-03 — `/paul:plan`

Plan 75-03 scope (technical spec + stdlib types):
1. **`docs/technical/EBNF.md`** — Grammar for `#type` annotations, `[#] %` properties, `[<]` constraints, generic type params, `#*` wildcard
2. **`docs/technical/spec/metadata-tree.md`** — Schema properties in `%definition` tree, update for `#` annotations
3. **`docs/technical/COMPILE-RULES.md`** — New rules: PGE-312 (type param constraint violation), PGE-418 (duplicate keys), PGE-419 (key gap violation)
4. **`docs/user/stdlib/types/types.md`** — Add core type definitions (#String, #Int, #UnsignedInt, #Float, #Sci, #Eng, #Dimension, #Array, #Dict, #Dataframe, #Serial) + update existing entries (`;` → `#`)

**After 75-03:** Create Plan 75-04 — spec-wide `;` → `#` migration (~120 files)

**After all plans:** `/paul:unify` then `/paul:merge` to close issue #75

---

## Key Files

| File | Purpose |
|------|---------|
| `docs/draft.md` | Approved ground-up type hierarchy (source of truth for definitions) |
| `docs/technical/plan/decisions/schema-properties.md` | All gap resolutions + additional decisions |
| `docs/user/syntax/types.md` | **REWRITTEN** — complete type system spec with # annotations |
| `docs/user/concepts/collections.md` | **UPDATED** — #Dict, #Dataframe, array : keys |
| `.paul/phases/issue-75-dict-kv/75-02-SUMMARY.md` | Plan 75-02 completion summary |
| `.paul/phases/issue-75-dict-kv/75-01-PLAN.md` | Plan 75-01 (gap resolution) |
| `.paul/STATE.md` | Live project state |

---

## Resume Instructions

1. Read this handoff
2. Read `.paul/STATE.md`
3. Run `/paul:resume` or directly `/paul:plan` for Plan 75-03
4. Scope for 75-03: EBNF + metadata-tree + compile rules + stdlib type definitions
5. After 75-03: Plan 75-04 for spec-wide ; → # migration

---

*Handoff created: 2026-03-27*
