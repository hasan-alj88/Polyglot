# PAUL Handoff

**Date:** 2026-04-08
**Status:** context-limit

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** Polyglot — async programming language and compilation service
**Core value:** Building a new async programming language that can asynchronously compile other programming languages

---

## Current State

**Issue:** #272 — Parameterized ## schemas replace {M} macros
**Branch:** design/issue-272-parameterized-schemas-replace-m-macros
**Plan file:** /home/hhj/.claude/plans/floofy-brewing-quail.md

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY ──▶ MERGE
  ✓        ○        ○        ○     [Issue #272 active]
```

---

## What Was Done

- Fetched issue #272 and created feature branch from main
- Deep design discussion with user covering:
  - Ground truth: all branches are enums, only leaves are Enum or Value
  - Redesigned `%##` properties — dropped `Children.` prefix, `##` implies children at this level
  - New `%##Active` property (#ActiveKind: .All, .One, .Partial) — compiler validation rule for branch presence
  - New `%##Flexible` property (#FlexKind: .Fixed, .Flexible, .Range) — three kinds of children access
  - New `%##Sorted`, `%##Propagate`, `%##Level.N`, `%##Range` properties
  - New `%###Type`, `%###Kind`, `%###Unique` leaf-level properties
  - `##Inf` as composable schema, `#Bound` as concrete type (##Int + ##Inf)
  - Generic `#` types with `[#]` input parameters, `:` positional binding
  - `##Rectangular` parameterized with `<Dim`, generates per-level structure
  - `##Nullable`, `##Result`, `##Fields`, `##Set` new parameterized schemas
  - Retired: `{M}`, `[M]`, `##Homogeneous`, `##Heterogeneous`, `##EnumLeafs`, `%##Children.*`, `-1` magic numbers
- Created 5-plan execution strategy (272-01 through 272-05)
- Plan approved by user
- Saved full design to memory: `pg_design_272_branch_descriptors.md`

---

## What's In Progress

- **Plan 272-01: Core Properties & New Enums** — NOT STARTED (no files written yet)
  - Todo list created but no tasks executed

---

## What's Next

**Immediate:** Execute Plan 272-01 — create 3 new files (FlexKind.md, ActiveKind.md, Bound.md), rewrite schema-properties.md, update enums.md, types.md, INDEX.md

**After that:** Plans 272-02 through 272-05 sequentially (schema files, collection types, syntax/concept docs, technical docs)

---

## Key Design Decisions

Read `pg_design_272_branch_descriptors.md` in memory — it has the COMPLETE agreed design including:
- All `%##` properties (branch-level)
- All `%###` properties (leaf-level)
- All named `##` schemas (static and parameterized)
- All generic `#` type definitions
- New enum types (#FlexKind, #ActiveKind, #Bound)
- Resolution rules for `:` separator binding
- What was retired and why

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | Phase overview |
| `/home/hhj/.claude/plans/floofy-brewing-quail.md` | 5-plan execution strategy |
| `memory/pg_design_272_branch_descriptors.md` | Complete agreed design |
| `memory/pg_design_parameterized_schemas.md` | Earlier design session (superseded by 272 design) |
| `docs/user/syntax/types/schema-properties.md` | Primary rewrite target for 272-01 |
| `docs/user/aj3lib/types/schemas/INDEX.md` | Schema registry (272-02) |
| `docs/user/aj3lib/types/enums.md` | Enum registry (272-01) |

---

## Resume Instructions

1. Read `.paul/STATE.md` for latest position
2. Read `pg_design_272_branch_descriptors.md` in memory for full design
3. Read plan file at `/home/hhj/.claude/plans/floofy-brewing-quail.md`
4. Run `/paul:resume` or start directly with Plan 272-01 execution
5. Git stash has uncommitted work from before branch creation — `git stash list` to check

---

*Handoff created: 2026-04-08*
