---
phase: issue-26-confirm-remove-speculative-t-triggers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/user/jm3lib/pipelines/T.md, docs/user/jm3lib/INDEX.md]
autonomous: true
---

<objective>
## Goal
Verify that all three speculative =T.* trigger pipelines from issue #26 have been resolved (confirmed with IO spec or removed), and close the issue.

## Purpose
Issue #26 was filed to formally confirm or remove =T.Schedule.Cron, =T.HTTP.Webhook, and =T.File.Created. Phase 12 (v0.2 milestone) already performed this work — this plan verifies completeness and closes the issue.

## Output
- Verified T.md has complete IO specs for all confirmed triggers
- Verified no speculative (?) markers remain in jm3lib INDEX.md
- Issue #26 closed on GitHub
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work
@.paul/phases/12-package-system-jm3lib/12-01-SUMMARY.md
Phase 12 already removed speculative =T.Schedule, =T.HTTP, =T.File and confirmed =T.Webhook as top-level.

## Source Files
@docs/user/jm3lib/pipelines/T.md
@docs/user/jm3lib/INDEX.md
</context>

<skills>
No specialized flows configured.
</skills>

<acceptance_criteria>

## AC-1: All speculative triggers resolved
```gherkin
Given issue #26 lists =T.Schedule.Cron, =T.HTTP.Webhook, =T.File.Created as speculative
When T.md is reviewed
Then each has been either confirmed (with IO spec under a new name) or removed
And no speculative (?) markers remain
```

## AC-2: T.md has complete IO specs
```gherkin
Given T.md lists all confirmed trigger pipelines
When each trigger entry is reviewed
Then every trigger has input/output declarations and behavioral notes
```

## AC-3: INDEX.md clean
```gherkin
Given INDEX.md tracks all jm3lib pipeline files
When the =T section is reviewed
Then no (?) or speculative markers remain for trigger pipelines
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Verify speculative trigger resolution</name>
  <files>docs/user/jm3lib/pipelines/T.md, docs/user/jm3lib/INDEX.md</files>
  <action>
    Read T.md and verify:
    1. =T.Schedule.Cron → resolved as =T.Daily (confirmed with IO spec) OR removed
    2. =T.HTTP.Webhook → resolved as =T.Webhook (confirmed with IO spec) OR removed
    3. =T.File.Created → resolved as =T.Folder.NewFiles (confirmed with IO spec) OR removed
    4. No (?) markers remain in T.md

    Read INDEX.md and verify:
    5. No (?) markers remain for =T.* entries
    6. =T status shows "Stable" or "complete" (not "Partial (?)")

    If any gaps found: fix them in-place.
    If all clean: report verification passed.
  </action>
  <verify>
    grep -c "(?" docs/user/jm3lib/pipelines/T.md returns 0
    grep -c "(?" docs/user/jm3lib/INDEX.md returns 0
    All five triggers in T.md (Call, Manual, Daily, Webhook, Folder.NewFiles) have IO declarations
  </verify>
  <done>AC-1, AC-2, AC-3 satisfied: all speculative triggers resolved, IO specs present, no (?) markers</done>
</task>

<task type="auto">
  <name>Task 2: Close issue #26 on GitHub</name>
  <files>none (GitHub API only)</files>
  <action>
    Close issue #26 with a comment summarizing the resolution:
    - =T.Schedule.Cron → removed (zero usage; =T.Daily covers scheduled triggers)
    - =T.HTTP.Webhook → confirmed as =T.Webhook (top-level, matches EDGE-CASES usage)
    - =T.File.Created → confirmed as =T.Folder.NewFiles (with IO spec)
    - Work completed in Phase 12 (v0.2 milestone)
  </action>
  <verify>gh issue view 26 --json state shows "CLOSED"</verify>
  <done>Issue #26 closed with resolution summary</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- docs/user/concepts/* (pipeline concepts stable)
- docs/technical/* (EBNF, EDGE-CASES, compile rules stable)
- .paul/phases/12-*/* (completed phase artifacts)

## SCOPE LIMITS
- Verification only — do not redesign trigger naming or add new triggers
- If T.md or INDEX.md need fixes, only fix speculative marker issues
- Do not modify any other jm3lib files

</boundaries>

<verification>
Before declaring plan complete:
- [ ] T.md has 5 triggers (Call, Manual, Daily, Webhook, Folder.NewFiles) each with IO
- [ ] No (?) markers in T.md or INDEX.md
- [ ] Issue #26 closed on GitHub
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All three speculative triggers from issue #26 verified as resolved
- T.md confirmed complete with IO specs
- INDEX.md clean of speculative markers
- Issue #26 closed on GitHub with resolution comment
</success_criteria>

<output>
After completion, create `.paul/phases/issue-26-confirm-remove-speculative-t-triggers/26-01-SUMMARY.md`
</output>
