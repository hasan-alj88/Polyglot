---
phase: 146-semicolon-type-annotation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/user/jm3lib/types/structs.md
  - docs/user/jm3lib/errors/errors.md
  - docs/user/jm3lib/pipelines/Q.md
  - docs/user/concepts/pipelines/queue.md
  - docs/user/concepts/pipelines/io-triggers.md
  - docs/user/concepts/errors.md
  - docs/technical/ebnf/09-definition-blocks.md
  - docs/technical/edge-cases/15-metadata-blocks.md
  - docs/technical/compile-rules/PGE/PGE01012-queue-definition-prefix.md
  - docs/technical/compile-rules/PGE/PGE01013-queue-control-contradiction.md
  - docs/technical/compile-rules/PGE/PGE01014-unresolved-queue-reference.md
  - docs/technical/compile-rules/PGE/PGE03006-race-collector-type-homogeneity.md
  - docs/technical/compile-rules/PGE/PGE04001-type-mismatch.md
  - docs/technical/compile-rules/PGE/PGE12002-duplicate-alias.md
  - docs/technical/plan/decisions/schema-properties.md
  - docs/technical/plan/decisions/string-re-subfields.md
  - docs/technical/plan/decisions/metadata-data-tree.md
  - docs/audit/reference/glossary.md
autonomous: true
---

<objective>
## Goal
Remove all retired `;` type annotation separators from documentation. Replace `;#Type` with `#Type` and `;RawString` with `#RawString` across all files.

## Purpose
The `;` separator was explicitly retired in issue #88 decisions. 119 occurrences remain across 14+ files, creating a contradiction between the EBNF grammar (which uses `#` directly) and examples that still use `;`.

## Output
All `;` type annotation remnants removed. Zero occurrences of `;#` or `;RawString` in docs/.
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Design Decision
Issue #88 decisions doc states: "`;` Retired — `#` for Type Annotations". User confirms `;` no longer means datatype. All occurrences are batch-replaced.

## Source Files
All 14 files with `;#` occurrences (119 total) plus 3 decision/glossary files with `;RawString`.
</context>

<acceptance_criteria>

## AC-1: Zero `;#` occurrences in docs
```gherkin
Given all documentation files in docs/
When searching for the pattern `;#`
Then zero matches are found
```

## AC-2: Zero `;RawString` occurrences in docs
```gherkin
Given all documentation files in docs/
When searching for the pattern `;RawString`
Then zero matches are found
```

## AC-3: Glossary updated
```gherkin
Given docs/audit/reference/glossary.md
When reading the #String entry
Then it uses `#RawString` notation, not `;RawString`
```

## AC-4: Decision docs updated
```gherkin
Given schema-properties.md and string-re-subfields.md
When reading #String struct examples
Then they use `.string#RawString` not `.string;RawString`
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Batch replace `;#` with `#` across all 14 files</name>
  <files>
    docs/user/jm3lib/types/structs.md,
    docs/user/jm3lib/errors/errors.md,
    docs/user/jm3lib/pipelines/Q.md,
    docs/user/concepts/pipelines/queue.md,
    docs/user/concepts/pipelines/io-triggers.md,
    docs/user/concepts/errors.md,
    docs/technical/ebnf/09-definition-blocks.md,
    docs/technical/edge-cases/15-metadata-blocks.md,
    docs/technical/compile-rules/PGE/PGE01012-queue-definition-prefix.md,
    docs/technical/compile-rules/PGE/PGE01013-queue-control-contradiction.md,
    docs/technical/compile-rules/PGE/PGE01014-unresolved-queue-reference.md,
    docs/technical/compile-rules/PGE/PGE03006-race-collector-type-homogeneity.md,
    docs/technical/compile-rules/PGE/PGE04001-type-mismatch.md,
    docs/technical/compile-rules/PGE/PGE12002-duplicate-alias.md
  </files>
  <action>
    Use sed or Edit to replace all `;#` with `#` in each file.
    This covers patterns like:
    - `.strategy;#QueueStrategy` → `.strategy#QueueStrategy`
    - `<mb;#Float` → `<mb#Float`
    - `.Denied;#Error` → `.Denied#Error`
    - `;#Error` → `#Error` (in error definitions)
    - `>profile;#UserProfile` → `>profile#UserProfile`
  </action>
  <verify>grep -r ';#' docs/ returns zero matches</verify>
  <done>AC-1 satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Replace `;RawString` and `;string` in decision docs and glossary</name>
  <files>
    docs/technical/plan/decisions/schema-properties.md,
    docs/technical/plan/decisions/string-re-subfields.md,
    docs/technical/plan/decisions/metadata-data-tree.md,
    docs/audit/reference/glossary.md
  </files>
  <action>
    Replace remaining `;` type annotation patterns:
    - `.string;RawString` → `.string#RawString`
    - `.regex;RawString` → `.regex#RawString`
    - `typed ';string'` or `';string'` → remove `;` prefix in prose
    - `.schema;RawString` → `.schema#RawString`
    - Glossary #String entry: update `;string` and `;RawString` references

    Also update the "`;` Retired" section in schema-properties.md to note
    that the migration is now complete (the remnants have been removed).
  </action>
  <verify>grep -r ';RawString\|;string' docs/ returns zero matches</verify>
  <done>AC-2, AC-3, AC-4 satisfied</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- EBNF grammar rules (type_annotation production is already correct with `#`)
- Any `;` that is EBNF statement terminators (`;` at end of EBNF rules)
- Semicolons in non-type-annotation contexts

## SCOPE LIMITS
- Only replace `;` when used as type annotation separator
- Do not modify archive/ files
- Do not add new compile rules or EBNF productions

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `grep -r ';#' docs/` returns zero matches (excluding EBNF rule terminators)
- [ ] `grep -r ';RawString' docs/` returns zero matches
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All 119+ `;#` occurrences replaced
- All `;RawString` / `;string` occurrences replaced
- Glossary and decision docs updated
- Zero regression in EBNF grammar terminators
</success_criteria>

<output>
After completion, create `.paul/phases/146-semicolon-type-annotation/146-01-SUMMARY.md`
</output>
