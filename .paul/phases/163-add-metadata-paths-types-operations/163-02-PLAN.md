---
phase: 163-add-metadata-paths-types-operations
plan: 02
type: execute
wave: 1
depends_on: ["163-01"]
files_modified:
  # Permission enum files (new)
  - docs/user/jm3lib/types/PermissionIntent.md
  - docs/user/jm3lib/types/PermissionCategory.md
  - docs/user/jm3lib/types/IODirection.md
  - docs/user/jm3lib/types/AccessLevel.md
  - docs/user/jm3lib/types/GrantAuthority.md
  - docs/user/jm3lib/types/OSTarget.md
  - docs/user/jm3lib/types/Protocol.md
  - docs/user/jm3lib/types/HandleKind.md
  - docs/user/jm3lib/types/AuditLevel.md
  - docs/user/jm3lib/types/AlertLevel.md
  # ## schema folder (new)
  - docs/user/jm3lib/types/schemas/Leaf.md
  - docs/user/jm3lib/types/schemas/Scalar.md
  - docs/user/jm3lib/types/schemas/Flat.md
  - docs/user/jm3lib/types/schemas/Deep.md
  - docs/user/jm3lib/types/schemas/Homogeneous.md
  - docs/user/jm3lib/types/schemas/Heterogeneous.md
  - docs/user/jm3lib/types/schemas/Contiguous.md
  - docs/user/jm3lib/types/schemas/Sparse.md
  - docs/user/jm3lib/types/schemas/Rectangular.md
  - docs/user/jm3lib/types/schemas/Enum.md
  - docs/user/jm3lib/types/schemas/INDEX.md
  # ### field type folder (new)
  - docs/user/jm3lib/types/field-types/Value.md
  - docs/user/jm3lib/types/field-types/Enum.md
  - docs/user/jm3lib/types/field-types/ScalarValue.md
  - docs/user/jm3lib/types/field-types/ScalarEnum.md
  - docs/user/jm3lib/types/field-types/None.md
  - docs/user/jm3lib/types/field-types/INDEX.md
  # Existing files updated
  - docs/user/jm3lib/types/scalars.md
  - docs/user/jm3lib/types/enums.md
autonomous: true
---

<objective>
## Goal
Create permission enum type files, ## schema tier folder, and ### field type tier folder — all with metadata tree paths in YAML frontmatter and ## Metadata sections.

## Purpose
Completes the type-level metadata path coverage for issue #163. Permission enums are referenced in FULL-TREE.md but have no type definition files. ## schemas and ### field types live at %definition.##:{name} and %definition.###:{name} on the metadata tree but have no dedicated documentation files.

## Output
- 10 new permission enum files in types/
- 10 new ## schema files in types/schemas/ + INDEX.md
- 5 new ### field type files in types/field-types/ + INDEX.md
- scalars.md updated with metadata paths
- enums.md index updated to link permission enums
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work
@.paul/phases/163-add-metadata-paths-types-operations/163-01-SUMMARY.md

## Source Files
@docs/technical/spec/metadata-tree/FULL-TREE.md — authoritative paths for permission enums, ## schemas, ### field types
@docs/user/concepts/permissions.md — permission enum usage context (PermissionIntent, PermissionCategory, etc.)
@docs/user/jm3lib/types/scalars.md — existing ## scalar subtypes (needs metadata added)
@docs/user/jm3lib/types/enums.md — index page (needs permission enum links added)
</context>

<acceptance_criteria>

## AC-1: Permission enum files created with metadata
```gherkin
Given the 10 permission enums listed in FULL-TREE.md under "Permission enums"
When each is created as an individual file in types/
Then each file has: YAML frontmatter with metadata_definition/metadata_instance, {#} definition block with enum variants from FULL-TREE.md, ## Metadata section, ## Related section linking to permissions.md
```

## AC-2: Schema tier folder created with ## files
```gherkin
Given the 10 ## schemas in FULL-TREE.md under %definition.##
When types/schemas/ folder is created with individual files
Then each file has: YAML frontmatter with metadata_definition ("%definition.##:{Name}"), description of what the schema constrains, which %## property it sets, ## Metadata section, ## Related section
And an INDEX.md exists listing all schemas
```

## AC-3: Field type tier folder created with ### files
```gherkin
Given the 5 ### field types in FULL-TREE.md under %definition.###
When types/field-types/ folder is created with individual files
Then each file has: YAML frontmatter with metadata_definition ("%definition.###:{Name}"), description of the leaf content type, ## Metadata section, ## Related section
And an INDEX.md exists listing all field types
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Create 10 permission enum type files</name>
  <files>
    docs/user/jm3lib/types/PermissionIntent.md, docs/user/jm3lib/types/PermissionCategory.md,
    docs/user/jm3lib/types/IODirection.md, docs/user/jm3lib/types/AccessLevel.md,
    docs/user/jm3lib/types/GrantAuthority.md, docs/user/jm3lib/types/OSTarget.md,
    docs/user/jm3lib/types/Protocol.md, docs/user/jm3lib/types/HandleKind.md,
    docs/user/jm3lib/types/AuditLevel.md, docs/user/jm3lib/types/AlertLevel.md,
    docs/user/jm3lib/types/enums.md
  </files>
  <action>
    Create each permission enum file following the pattern from 163-01 enum files.

    Enum variants from FULL-TREE.md:
    - #PermissionIntent: .Ceiling, .Grant
    - #PermissionCategory: .File, .Web, .Database, .System, .Crypto, .IPC, .Device, .Memory
    - #IODirection: .Inbound, .Outbound, .Both
    - #AccessLevel: .Allow, .Deny
    - #GrantAuthority: .Package, .Pipeline
    - #OSTarget: .Any, .Linux, .Windows, .MacOS
    - #Protocol: .File, .TCP, .UDP, .HTTPS
    - #HandleKind: .Path, .ConnectionString
    - #AuditLevel: .None, .OnUse, .OnDeny, .All
    - #AlertLevel: .None, .OnDeny, .OnEscalation

    Each file:
    ```yaml
    ---
    audience: pg-coder
    type: specification
    updated: 2026-04-07
    status: complete
    metadata_definition: "%definition.#:{TypeName}"
    metadata_instance: "%#:{TypeName}:N"
    ---
    ```
    - H1 heading, <!-- @types --> comment
    - Intro: "Permission enum used in `{_}` permission objects. See [[concepts/permissions|Permissions]]."
    - ## Definition with {#} block (all use ##Enum, ##Scalar, ###ScalarEnum, lowercase %##Alias)
    - Brief description of what each variant means in permission context
    - ## Metadata table (definition + instance paths, enum uses :0)
    - ## Related linking to [[concepts/permissions|Permissions]], [[enums]]

    After creating files, update enums.md index to add a "Permission Enums" section with table linking all 10.
  </action>
  <verify>
    Count new permission files: `ls docs/user/jm3lib/types/{PermissionIntent,PermissionCategory,IODirection,AccessLevel,GrantAuthority,OSTarget,Protocol,HandleKind,AuditLevel,AlertLevel}.md | wc -l` = 10.
    All have metadata: `grep -l "metadata_definition" docs/user/jm3lib/types/PermissionIntent.md docs/user/jm3lib/types/AlertLevel.md` returns both.
    enums.md has permission section: `grep -c "Permission Enums" docs/user/jm3lib/types/enums.md` >= 1.
  </verify>
  <done>AC-1 satisfied: all 10 permission enums created with definitions, metadata, and linked from enums.md index</done>
</task>

<task type="auto">
  <name>Task 2: Create types/schemas/ folder with ## schema files</name>
  <files>
    docs/user/jm3lib/types/schemas/INDEX.md,
    docs/user/jm3lib/types/schemas/Leaf.md, docs/user/jm3lib/types/schemas/Scalar.md,
    docs/user/jm3lib/types/schemas/Flat.md, docs/user/jm3lib/types/schemas/Deep.md,
    docs/user/jm3lib/types/schemas/Homogeneous.md, docs/user/jm3lib/types/schemas/Heterogeneous.md,
    docs/user/jm3lib/types/schemas/Contiguous.md, docs/user/jm3lib/types/schemas/Sparse.md,
    docs/user/jm3lib/types/schemas/Rectangular.md, docs/user/jm3lib/types/schemas/Enum.md,
    docs/user/jm3lib/types/scalars.md
  </files>
  <action>
    Create types/schemas/ directory with 10 individual ## schema files + INDEX.md.

    Schema definitions from FULL-TREE.md (%definition.##):
    - ##Leaf: %##Depth.Max -> 0
    - ##Scalar: %##Depth.Max -> 1
    - ##Flat: %##Depth.Max -> 1
    - ##Deep: %##Depth.Max -> -1
    - ##Homogeneous: %##Children.Uniform -> #True
    - ##Heterogeneous: %##Children.Uniform -> #False
    - ##Contiguous: %##Children.Gap -> #False, .Ordered -> #True
    - ##Sparse: %##Children.Gap -> #True
    - ##Rectangular: %##Children.Regular -> #True, .Uniform -> #True
    - ##Enum: enum classification (all fields are enum fields)

    Each schema file:
    ```yaml
    ---
    audience: pg-coder
    type: specification
    updated: 2026-04-07
    status: complete
    metadata_definition: "%definition.##:{Name}"
    ---
    ```
    - H1: `## {Name} Schema` (e.g., "##Leaf Schema")
    - Brief description of what the schema constrains
    - Which %## property(ies) it sets (table showing property → value)
    - Which types compose this schema (examples from existing type files)
    - ## Metadata section with definition path only (schemas have no instances)
    - ## Related linking to [[scalars]], [[collections]], relevant types

    INDEX.md: summary table of all 10 schemas with property/value and links.

    Also update scalars.md: add metadata_definition/metadata_instance frontmatter fields and ## Metadata section. scalars.md documents ## tier types (##Int, ##Float, etc.) — its metadata_definition is "%definition.##:Int" (representative), metadata_instance is N/A (schemas don't have instances, but scalar subtypes appear as %#:String:int).
  </action>
  <verify>
    Directory exists with files: `ls docs/user/jm3lib/types/schemas/*.md | wc -l` = 11 (10 + INDEX).
    All have metadata: `grep -rl "metadata_definition" docs/user/jm3lib/types/schemas/` returns 10 files (INDEX excluded).
    scalars.md updated: `grep "metadata_definition" docs/user/jm3lib/types/scalars.md` returns match.
  </verify>
  <done>AC-2 satisfied: schemas/ folder created with 10 ## schema files + INDEX, scalars.md updated</done>
</task>

<task type="auto">
  <name>Task 3: Create types/field-types/ folder with ### files</name>
  <files>
    docs/user/jm3lib/types/field-types/INDEX.md,
    docs/user/jm3lib/types/field-types/Value.md,
    docs/user/jm3lib/types/field-types/Enum.md,
    docs/user/jm3lib/types/field-types/ScalarValue.md,
    docs/user/jm3lib/types/field-types/ScalarEnum.md,
    docs/user/jm3lib/types/field-types/None.md
  </files>
  <action>
    Create types/field-types/ directory with 5 individual ### field type files + INDEX.md.

    Field type definitions from FULL-TREE.md (%definition.###):
    - ###Value: leaf holds typed data (e.g., .name#string)
    - ###Enum: leaf is variant selector (e.g., .True, .False — no type annotation)
    - ###ScalarValue: regex-validated string (e.g., ##Int leaf holds "42")
    - ###ScalarEnum: variant selector in scalar (e.g., #Boolean.True)
    - ###None: empty string "" is only valid value (#None type)

    Each field type file:
    ```yaml
    ---
    audience: pg-coder
    type: specification
    updated: 2026-04-07
    status: complete
    metadata_definition: "%definition.###:{Name}"
    ---
    ```
    - H1: `###{Name} Field Type` (e.g., "###Value Field Type")
    - Brief description of what leaf content this describes
    - How it's declared in {#} blocks (e.g., `[#] << ###Value`)
    - Example types that use it
    - ## Metadata section with definition path only (no instances)
    - ## Related linking to schemas, type system

    INDEX.md: summary table of all 5 field types with descriptions and links.
  </action>
  <verify>
    Directory exists: `ls docs/user/jm3lib/types/field-types/*.md | wc -l` = 6 (5 + INDEX).
    All have metadata: `grep -rl "metadata_definition" docs/user/jm3lib/types/field-types/` returns 5 files.
  </verify>
  <done>AC-3 satisfied: field-types/ folder created with 5 ### field type files + INDEX</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- docs/technical/spec/metadata-tree/* (reference only)
- docs/user/jm3lib/types/boolean.md, string.md, NativeKind.md (already done in 163-01)
- docs/user/jm3lib/types/{OS,PipelineStatus,QueueStrategy,...}.md (already done in 163-01)
- docs/user/jm3lib/pipelines/* (handled in Plan 03)

## SCOPE LIMITS
- Permission enums: create definitions from FULL-TREE.md variants — do not invent new variants
- Schema files: document what each schema constrains — do not define new schemas
- Do not add metadata to datetime/ files (could be a future follow-up)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] 10 permission enum files exist with correct variants and metadata
- [ ] enums.md index updated with permission enum section
- [ ] types/schemas/ folder exists with 10 ## files + INDEX.md
- [ ] types/field-types/ folder exists with 5 ### files + INDEX.md
- [ ] scalars.md has metadata frontmatter and ## Metadata section
- [ ] All new files have metadata_definition in YAML frontmatter
- [ ] All new files have ## Metadata section
</verification>

<success_criteria>
- 10 permission enum type files created
- 10 ## schema files + INDEX.md in schemas/ folder
- 5 ### field type files + INDEX.md in field-types/ folder
- scalars.md and enums.md updated
- All files have metadata paths
</success_criteria>

<output>
After completion, create `.paul/phases/163-add-metadata-paths-types-operations/163-02-SUMMARY.md`
</output>
