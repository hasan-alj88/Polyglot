---
phase: 94-schema-validation
plan: 03
type: execute
wave: 1
depends_on: ["94-01", "94-02"]
files_modified:
  - docs/user/jm3lib/expanders/ForEach/Dataframe.md
  - docs/technical/EBNF.md
autonomous: true
---

<objective>
## Goal
Audit expand/collect operators for compatibility with the macro-generated type system (94-01) and row-oriented Dataframe. Fix stale references to deprecated ~ForEach.Dataframe.Column.

## Purpose
Closes issue #91 — verify expand/collect operators work correctly with ## schema properties, macro-generated collections, and row-oriented Dataframe. Ensures no stale references to column-oriented patterns remain.

## Output
- Stale ~ForEach.Dataframe.Column references removed from active spec files
- All expand/collect IO signatures verified compatible with macro-generated types
</objective>

<context>
@.paul/phases/94-schema-validation/94-01-SUMMARY.md — macro-for-generics redesign
@docs/user/jm3lib/expanders/ForEach/Dataframe.md — still references Column variant
@docs/technical/EBNF.md — still lists ~ForEach.Dataframe.Column in expander table
@docs/user/jm3lib/expanders/ForEach/Dataframe/Column.md — already deprecated in 94-01
</context>

<acceptance_criteria>

## AC-1: No Active References to Deprecated Column Expander
```gherkin
Given ~ForEach.Dataframe.Column was deprecated in 94-01
When all spec files are audited
Then no non-deprecated file references ~ForEach.Dataframe.Column as active functionality
```

## AC-2: Expand/Collect IO Compatible with Macro Types
```gherkin
Given collections are now macro-generated ({M} #Array, {M} #Map, {M} #Dataframe)
When expander/collector IO signatures are reviewed
Then all IO types use : notation (not old < generic syntax) and match macro-generated definitions
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Fix stale ~ForEach.Dataframe.Column references</name>
  <files>docs/user/jm3lib/expanders/ForEach/Dataframe.md, docs/technical/EBNF.md</files>
  <action>
    **Dataframe.md** — Update Variants section: remove or mark Column link as deprecated, note =#.Column replaces it
    **EBNF.md** — Remove ~ForEach.Dataframe.Column from expander signature table (it's deprecated, shouldn't appear in grammar)
  </action>
  <verify>Grep for ~ForEach.Dataframe.Column in non-deprecated files — zero matches</verify>
  <done>AC-1 satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Verify expand/collect IO compatibility (audit-only)</name>
  <files>docs/user/jm3lib/expanders/ForEach/*.md, docs/user/jm3lib/collectors/Into/*.md</files>
  <action>
    Read all expander and collector files. Verify:
    - No old generic syntax (#Array<Type, #Map<K<V) remains
    - IO signatures are compatible with macro-generated types
    - Row-oriented Dataframe (>row as Map) is consistent
    Report findings — no edits expected if clean
  </action>
  <verify>Grep for old generic patterns — zero matches (already confirmed)</verify>
  <done>AC-2 satisfied</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- docs/user/jm3lib/expanders/ForEach/Dataframe/Column.md (already deprecated in 94-01)
- docs/user/concepts/collections.md (already updated in 94-01)

## SCOPE LIMITS
- Audit only — no new operators or pipelines
- Do not modify collector files unless inconsistency found
</boundaries>

<verification>
- [ ] Zero grep matches for ~ForEach.Dataframe.Column in non-deprecated files
- [ ] Zero grep matches for old generic syntax in expander/collector files
- [ ] Dataframe expander >row output consistent with row-oriented design
</verification>

<success_criteria>
- All stale references fixed
- Audit confirms compatibility — no structural issues
</success_criteria>

<output>
After completion, create `.paul/phases/94-schema-validation/94-03-SUMMARY.md`
</output>
