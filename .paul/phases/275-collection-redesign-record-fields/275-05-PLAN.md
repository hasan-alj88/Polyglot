---
phase: 275-collection-redesign-record-fields
plan: 05
type: execute
wave: 3
depends_on: ["275-02", "275-03", "275-04"]
files_modified:
  - docs/user/pglib/types/RetriggerStrategy.md
  - docs/user/pglib/types/Protocol.md
  - docs/user/pglib/types/OS.md
  - docs/user/pglib/types/FileAccess.md
  - docs/user/pglib/types/PipelineStatus.md
  - docs/user/pglib/types/PermissionIntent.md
  - docs/user/pglib/types/NativeKind.md
  - docs/user/pglib/types/FieldKind.md
  - docs/user/pglib/types/ResourceTag.md
  - docs/user/pglib/types/datetime/calendar-date-types.md
  - docs/user/pglib/types/datetime/main-type.md
  - docs/user/pglib/types/datetime/calendar-infrastructure.md
  - docs/user/pglib/types/datetime/cultural-types.md
  - docs/user/pglib/types/Bound.md
  - docs/archive/wip/draft.md
  - docs/technical/compile-rules/PGE/PGE02012-duplicate-operation-label.md
autonomous: true
---

<objective>
## Goal
Propagate the collection redesign changes to ALL remaining files — pglib enum types, datetime types, draft.md, and any other files with stale `[#] <<` schema composition syntax or retired type references. Final verification sweep.

## Purpose
Ensures zero orphaned references to retired types/properties/schemas across the entire docs/ directory. This is the cleanup and verification plan.

## Output
Every file in docs/ uses the new collection model consistently. Full-codebase grep verification passes.
</objective>

<context>
## Project Context
@.paul/PROJECT.md

## Design Source
GitHub Issue #275 body
memory/pg_design_collection_redesign.md

## Prior Work
Plans 275-01 through 275-04 (core definitions, collection docs, schema files, technical docs)
</context>

<acceptance_criteria>

## AC-1: All pglib enum type files updated
```gherkin
Given pglib type files like RetriggerStrategy.md, Protocol.md, OS.md, etc.
When they contain [#] << ##Schema composition lines
Then those lines are updated to [#] ##Schema (no <<)
And any retired type references are replaced
```

## AC-2: DateTime type files updated
```gherkin
Given datetime/*.md files (calendar-date-types.md has ~50 occurrences)
When they contain [#] << ##Schema composition lines
Then ALL are updated to [#] ##Schema (no <<)
And no retired schema references remain
```

## AC-3: Draft.md updated
```gherkin
Given docs/archive/wip/draft.md
When it contains collection type examples
Then examples use ##Record, %##Fields, #Inf
And schema composition uses new syntax
```

## AC-4: Full-codebase verification passes
```gherkin
Given the entire docs/ directory (excluding archive/ for content, but including for syntax)
When grepping for [#] << ## (old schema composition)
Then 0 matches found
When grepping for retired types as active (non-historical) references
Then 0 matches found outside migration/retirement notes
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Update pglib enum type files</name>
  <files>docs/user/pglib/types/RetriggerStrategy.md, docs/user/pglib/types/Protocol.md, docs/user/pglib/types/OS.md, docs/user/pglib/types/FileAccess.md, docs/user/pglib/types/PipelineStatus.md, docs/user/pglib/types/PermissionIntent.md, docs/user/pglib/types/NativeKind.md, docs/user/pglib/types/FieldKind.md, docs/user/pglib/types/ResourceTag.md, docs/user/pglib/types/Bound.md</files>
  <action>
    1. In ALL files: Replace [#] << ##Schema with [#] ##Schema (drop << for schema composition)
       KEEP: [#] %##Prop << value (properties still use <<)
    2. Bound.md: Update to show #Bound.Inf → #Inf alias. Ensure ##Inf schema documented.
    3. FieldKind.md: Verify it already defines #FieldKind correctly (used by %###Kind).
    4. All files: Replace any references to retired types (##Contiguous, ##Rectangular, etc.) with direct properties.
  </action>
  <verify>
    - Grep for "[#] << ##" in modified files → 0
    - All files parse correctly (no broken markdown)
  </verify>
  <done>AC-1 satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Update datetime type files</name>
  <files>docs/user/pglib/types/datetime/calendar-date-types.md, docs/user/pglib/types/datetime/main-type.md, docs/user/pglib/types/datetime/calendar-infrastructure.md, docs/user/pglib/types/datetime/cultural-types.md</files>
  <action>
    1. calendar-date-types.md: This file has ~50 occurrences of [#] << ##Schema.
       Replace ALL with [#] ##Schema (drop <<).
       KEEP: [#] %##Prop << value lines unchanged.
       This is a bulk find-and-replace: "[#] << ##" → "[#] ##"
       BUT be careful: "[#] << #" (single #, inheritance) should NOT change — only "[#] << ##" (double #, schema composition).
    2. main-type.md: Same pattern — update [#] << ## → [#] ##.
    3. calendar-infrastructure.md: Same pattern.
    4. cultural-types.md: Same pattern (~10 occurrences).
  </action>
  <verify>
    - Grep for "[#] << ##" in datetime/ files → 0
    - Grep for "[#] %##" → still present (properties unchanged)
    - File structure intact (no broken definitions)
  </verify>
  <done>AC-2 satisfied</done>
</task>

<task type="auto">
  <name>Task 3: Update draft.md, remaining files, and full verification</name>
  <files>docs/archive/wip/draft.md, docs/technical/compile-rules/PGE/PGE02012-duplicate-operation-label.md</files>
  <action>
    1. draft.md: Update collection type examples to use ##Record, %##Fields, #Inf. Update schema composition syntax. Update macro examples (#Map → ##Record where the macro generates a record).
    2. PGE02012 (if it exists as new file): Update any references.
    3. FINAL VERIFICATION — run full-codebase greps:
       a. Grep docs/ for "[#] << ##" → must be 0
       b. Grep docs/ (non-archive) for "##Contiguous" → must be 0 (except retirement notes)
       c. Grep docs/ (non-archive) for "##Rectangular" → must be 0 (except retirement notes)
       d. Grep docs/ (non-archive) for "##Sparse" → must be 0 (except retirement/history)
       e. Grep docs/ (non-archive) for "##Deep" → must be 0 (except retirement/history)
       f. Grep docs/ (non-archive) for "%##Key" → must be 0
       g. Grep docs/ (non-archive) for "%##Range" → must be 0
       h. Grep docs/ (non-archive) for "%##Flexible" → must be 0
       i. Grep docs/ (non-archive) for "%##Regular" → must be 0
       j. Grep docs/ (non-archive) for "#FlexKind" → must be 0
    4. Report any remaining occurrences and fix them.
  </action>
  <verify>
    - All 10 verification greps return 0 matches (outside archive/ historical context)
    - draft.md examples use new types
    - No file has broken markdown from the changes
  </verify>
  <done>AC-3, AC-4 satisfied</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- docs/audit/ (rules — except glossary already done in Plan 275-04)
- .paul/ (except STATE.md)
- Files already handled by Plans 275-01 through 275-04

## SCOPE LIMITS
- archive/ files get syntax updates but NOT content rewrites
- datetime files are bulk syntax updates only — do not redesign datetime types
- draft.md gets example updates but is not authoritative (it's WIP)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] Grep docs/ for "[#] << ##" → 0 matches across ENTIRE docs/
- [ ] Grep docs/ (non-archive) for retired schemas → 0 (except retirement/migration notes)
- [ ] Grep docs/ (non-archive) for retired properties → 0
- [ ] Grep docs/ (non-archive) for #FlexKind → 0
- [ ] All datetime files updated (calendar-date-types.md is the big one)
- [ ] draft.md examples consistent with new model
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All 3 tasks completed
- Full-codebase verification passes: zero orphaned references
- Every file in docs/ uses consistent schema composition syntax
- Issue #275 changes fully propagated
</success_criteria>

<output>
After completion, create `.paul/phases/275-collection-redesign-record-fields/275-05-SUMMARY.md`
</output>
