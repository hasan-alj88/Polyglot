---
phase: 275-collection-redesign-record-fields
plan: 03
type: execute
wave: 2
depends_on: ["275-01"]
files_modified:
  - docs/user/pglib/types/schemas/Nullable.md
  - docs/user/pglib/types/schemas/Inf.md
  - docs/user/pglib/types/schemas/Rectangular.md
  - docs/user/pglib/types/schemas/Result.md
  - docs/user/pglib/types/schemas/String.md
  - docs/user/pglib/types/schemas/Array.md
  - docs/user/pglib/types/string.md
  - docs/user/pglib/types/Serial.md
  - docs/user/concepts/collections/serial.md
  - docs/user/pglib/types/schemas/Rectangular.md
  - docs/user/syntax/blocks.md
  - docs/user/syntax/comments.md
  - docs/user/syntax/io.md
  - docs/user/syntax/types/macro-types.md
  - docs/user/syntax/types/prefix-system.md
  - docs/user/pglib/errors/errors.md
autonomous: true
---

<objective>
## Goal
Update remaining schema files, retire obsolete schema docs, update composition syntax across all user docs that use `[#] <<` for schema lines, and fix remaining user-facing docs.

## Purpose
Ensures schema composition syntax is consistent (`[#] ##Schema` not `[#] << ##Schema`) and all remaining user docs reference the new type model.

## Output
All user-facing documentation uses the new schema composition syntax. Obsolete schema files updated or marked as retired.
</objective>

<context>
## Project Context
@.paul/PROJECT.md

## Design Source
GitHub Issue #275 body
memory/pg_design_collection_redesign.md

## Prior Work
@.paul/phases/275-collection-redesign-record-fields/275-01-PLAN.md (core definitions)

## Source Files
All files listed in files_modified
</context>

<acceptance_criteria>

## AC-1: Schema composition syntax updated
```gherkin
Given any user-facing documentation file
When it contains [#] followed by a ## schema name
Then it uses [#] ##Schema (no <<)
And property lines still use [#] %##Prop << value (with <<)
```

## AC-2: Retired schema files handled
```gherkin
Given schemas/Rectangular.md
When a reader opens it
Then it explains ##Rectangular is retired and properties are stated directly
```

## AC-3: Remaining type references updated
```gherkin
Given Serial.md, string.md, and other pglib type docs
When they reference collection schemas or properties
Then they use the new names (%##Fields, #Inf, ##Record)
And do not reference retired types/properties
```

## AC-4: Syntax docs updated
```gherkin
Given blocks.md, io.md, comments.md, macro-types.md, prefix-system.md
When they contain schema composition examples
Then they use [#] ##Schema (no <<) syntax
And reference new types where applicable
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Update schema files (Nullable, Inf, Rectangular, Result, String, Array)</name>
  <files>docs/user/pglib/types/schemas/Nullable.md, docs/user/pglib/types/schemas/Inf.md, docs/user/pglib/types/schemas/Rectangular.md, docs/user/pglib/types/schemas/Result.md, docs/user/pglib/types/schemas/String.md, docs/user/pglib/types/schemas/Array.md</files>
  <action>
    1. In ALL files: Update schema composition syntax [#] << ##Schema → [#] ##Schema
       KEEP: [#] %##Prop << value (properties still use <<)
    2. Rectangular.md: Rewrite as retirement notice. ##Rectangular is retired — its properties (%##Gap << #False, %##Ordered << #True, %##Propagate << #True) are now stated directly on types.
    3. Inf.md: Update to reflect #Bound.Inf → #Inf alias pattern. Ensure ##Inf schema is documented.
    4. Array.md (schemas): Update to use %##Fields << #Range. Remove ##Contiguous/##Rectangular composition.
    5. Other files: Update any retired type/property references.
  </action>
  <verify>
    - Grep for "[#] << ##" in modified files → 0 (schema composition without <<)
    - Grep for "[#] %##" in modified files → still present (properties keep <<)
    - Grep for ##Contiguous, ##Rectangular (as composed schemas) → 0 except retirement notice
  </verify>
  <done>AC-1, AC-2 satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Update remaining pglib type docs and concept docs</name>
  <files>docs/user/pglib/types/string.md, docs/user/pglib/types/Serial.md, docs/user/concepts/collections/serial.md, docs/user/pglib/errors/errors.md</files>
  <action>
    1. Serial.md: Update #Serial definition to use %##Depth.Max << #Inf, %##Count << #Inf. Remove ##Deep/##Sparse composition. Update composition syntax.
    2. string.md: Update any schema references. Fix composition syntax.
    3. serial.md (concept): Update to match new #Serial definition. Remove retired schema references.
    4. errors.md: Update any references to retired types in error descriptions.
  </action>
  <verify>
    - Serial.md uses #Inf instead of ##Deep/##Sparse
    - Grep for retired schemas in modified files → 0
    - Composition syntax correct
  </verify>
  <done>AC-3 satisfied</done>
</task>

<task type="auto">
  <name>Task 3: Update syntax docs (blocks, io, comments, macro-types, prefix-system)</name>
  <files>docs/user/syntax/blocks.md, docs/user/syntax/io.md, docs/user/syntax/comments.md, docs/user/syntax/types/macro-types.md, docs/user/syntax/types/prefix-system.md</files>
  <action>
    1. In ALL files: Update schema composition syntax [#] << ##Schema → [#] ##Schema
    2. Update any examples that reference #Map, #Set, ##Contiguous, etc. to use new types
    3. macro-types.md: Update macro examples to use ##Record instead of #Map
    4. prefix-system.md: Update tier examples if they reference retired types
    5. blocks.md/io.md/comments.md: Fix any schema composition examples
  </action>
  <verify>
    - Grep for "[#] << ##" in modified files → 0
    - Grep for retired types in modified files → 0
    - Examples use new type names
  </verify>
  <done>AC-4 satisfied</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- docs/archive/ (historical)
- docs/audit/ (rules)
- Files already handled by Plan 275-01 or 275-02
- docs/technical/ (Plan 275-04)
- datetime types (Plan 275-05)
- pglib pipeline docs (Plan 275-05)

## SCOPE LIMITS
- Focus on schema composition syntax and remaining user docs
- Do not redefine types — reference Plan 275-01 definitions
- RetriggerStrategy.md, Protocol.md, OS.md, etc. are Plan 275-05

</boundaries>

<verification>
Before declaring plan complete:
- [ ] Grep for "[#] << ##" in ALL modified files → 0
- [ ] Grep for "[#] %##" in modified files → still present (correct — properties keep <<)
- [ ] Grep for ##Contiguous, ##Rectangular, ##Sparse, ##Deep (as composed schemas) in modified files → 0
- [ ] Grep for retired properties in modified files → 0
- [ ] All schema composition examples use new syntax
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All 3 tasks completed
- Schema composition syntax consistent across all modified files
- Retired schemas documented as retired where they had dedicated files
- No orphaned references to retired types/properties
</success_criteria>

<output>
After completion, create `.paul/phases/275-collection-redesign-record-fields/275-03-SUMMARY.md`
</output>
