---
phase: issue-120-chain-io-scoping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/user/syntax/io.md
  - docs/user/concepts/pipelines/chains.md
  - docs/technical/ebnf/10-execution.md
autonomous: true
---

<objective>
## Goal
Replace the misleading "caller perspective" label with "pipeline perspective" across all IO documentation, and add a one-line clarifying rule that `<`/`>` always describes the port from the pipeline's own viewpoint.

## Purpose
NotebookLM audit (issue #120, P1-critical) found that readers perceive `<`/`>` as reversed between pipeline definitions and chain step addressing. The semantics are correct — the label is wrong. Fixing the terminology eliminates the apparent contradiction.

## Output
Three files updated: io.md, chains.md, 10-execution.md — "caller perspective" → "pipeline perspective" with a clarifying note.
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Source Files
@docs/user/syntax/io.md
@docs/user/concepts/pipelines/chains.md
@docs/technical/ebnf/10-execution.md
</context>

<acceptance_criteria>

## AC-1: No "caller perspective" remains
```gherkin
Given the three modified files
When searching for "caller perspective" or "caller-perspective"
Then zero matches are found
```

## AC-2: "pipeline perspective" replaces all instances
```gherkin
Given the three modified files
When searching for "pipeline perspective" or "pipeline-perspective"
Then matches appear wherever "caller perspective" previously existed
```

## AC-3: Clarifying rule present in io.md
```gherkin
Given docs/user/syntax/io.md
When reading the Chain IO Addressing section
Then a one-line rule states that < and > always describe the port from the pipeline's own viewpoint regardless of context (definition, call, or chain)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Update io.md — replace "caller perspective" and add clarifying rule</name>
  <files>docs/user/syntax/io.md</files>
  <action>
    1. Line 91: Change "**caller-perspective**" to "**pipeline-perspective**"
    2. Line 106: Change "caller-perspective `<`/`>` convention" to "pipeline-perspective `<`/`>` convention"
    3. After the Chain IO Addressing heading (line 87) and before the direction table, add a one-line rule:
       "`<` and `>` always describe the port from the pipeline's own viewpoint — `<` marks the pipeline's input, `>` marks its output — whether in a definition, a call site, or a chain step reference."
    4. Update the Mermaid flowchart labels (lines 30-32) if they reference "caller" — replace with pipeline-perspective wording
  </action>
  <verify>Grep io.md for "caller.perspective" — expect 0 matches. Grep for "pipeline-perspective" — expect 2+ matches.</verify>
  <done>AC-1 and AC-3 satisfied for io.md</done>
</task>

<task type="auto">
  <name>Task 2: Update chains.md — replace "caller perspective"</name>
  <files>docs/user/concepts/pipelines/chains.md</files>
  <action>
    1. Lines 28-29: Change "(caller perspective)" to "(pipeline perspective)" in both table rows
    2. Line 33: Change "**caller-perspective**" to "**pipeline-perspective**" and update the explanation to say the `<`/`>` describes the port from the step's own viewpoint
  </action>
  <verify>Grep chains.md for "caller.perspective" — expect 0 matches. Grep for "pipeline perspective" — expect 3+ matches.</verify>
  <done>AC-1 and AC-2 satisfied for chains.md</done>
</task>

<task type="auto">
  <name>Task 3: Update EBNF 10-execution.md — replace "caller perspective"</name>
  <files>docs/technical/ebnf/10-execution.md</files>
  <action>
    1. Lines 79-80: Change "(caller perspective)" to "(pipeline perspective)" in both rule lines
  </action>
  <verify>Grep 10-execution.md for "caller.perspective" — expect 0 matches. Grep for "pipeline perspective" — expect 2 matches.</verify>
  <done>AC-1 and AC-2 satisfied for 10-execution.md</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- All files outside the three listed above
- The actual `<`/`>` syntax or semantics (they are correct)
- Code examples in any file (they are correct)
- Mermaid diagrams (content is correct, only update text labels if they say "caller")

## SCOPE LIMITS
- Do not create new files or new sections beyond the one-line clarifying rule
- Do not rewrite surrounding paragraphs — minimal targeted edits only
- Do not touch compile rules (PGE08010 etc.) — they don't use "caller perspective"

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `grep -ri "caller.perspective" docs/user/syntax/io.md` returns nothing
- [ ] `grep -ri "caller.perspective" docs/user/concepts/pipelines/chains.md` returns nothing
- [ ] `grep -ri "caller.perspective" docs/technical/ebnf/10-execution.md` returns nothing
- [ ] `grep -ri "pipeline.perspective" docs/` returns matches in all three files
- [ ] The one-line clarifying rule is present in io.md Chain IO Addressing section
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All "caller perspective" / "caller-perspective" replaced with "pipeline perspective" / "pipeline-perspective"
- Clarifying one-liner added to io.md
- No other content changed
- Three files, minimal diff
</success_criteria>

<output>
After completion, create `.paul/phases/issue-120-chain-io-scoping/120-01-SUMMARY.md`
</output>
