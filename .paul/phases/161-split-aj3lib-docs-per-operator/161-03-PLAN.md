---
phase: 161-split-jm3lib-docs-per-operator
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/user/jm3lib/pipelines/File/ (create 12 files)
  - docs/user/jm3lib/pipelines/Math/ (create 9 files)
  - docs/user/jm3lib/pipelines/T/ (create 6 files)
  - docs/user/jm3lib/pipelines/File.md (delete)
  - docs/user/jm3lib/pipelines/Math.md (delete)
  - docs/user/jm3lib/pipelines/T.md (delete)
autonomous: true
---

<objective>
## Goal
Split File.md (11 operators), Math.md (8 operators), and T.md (5 operators) into individual per-operator files with {N} definitions following the DT/ template.

## Purpose
Applies 1-file-per-operator pattern to three straightforward pipeline namespaces.

## Output
- File/ folder: 11 operator files + INDEX (12 total)
- Math/ folder: 8 operator files + INDEX (9 total)
- T/ folder: 5 operator files + INDEX (6 total)
- 3 original files deleted
</objective>

<acceptance_criteria>

## AC-1: Individual pipeline files exist
```gherkin
Given File.md had 11 operators, Math.md had 8, T.md had 5
When plan executes
Then each operator has its own .md file under its namespace folder
And the 3 original files are deleted
```

## AC-2: All files follow pipeline template
```gherkin
Given pipeline docs use ## Definition with {N} blocks
When any pipeline .md file is inspected
Then it has: frontmatter, description, ## Definition, ## Inputs, ## Outputs, ## Errors, ## Permissions, ## Related
```

## AC-3: INDEX files provide navigation
```gherkin
Given 3 namespace folders created
When INDEX.md files are inspected
Then each lists all operators with wikilinks, grouped by category
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Split File.md, Math.md, T.md in parallel</name>
  <files>pipelines/File/ (12), pipelines/Math/ (9), pipelines/T/ (6)</files>
  <action>Run 3 parallel agents to create all files simultaneously</action>
  <verify>
    - ls File/ shows 12 files, Math/ shows 9, T/ shows 6
    - File.md, Math.md, T.md deleted
    - All files have audience: pg-coder frontmatter
    - All operator files have ## Definition, ## Inputs, ## Outputs, ## Errors, ## Permissions, ## Related
  </verify>
  <done>AC-1, AC-2, AC-3 satisfied</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- docs/user/jm3lib/pipelines/DT/ (already complete)
- docs/user/jm3lib/collectors/ (Plan 01 complete)
- docs/user/jm3lib/expanders/ (Plan 02 complete)
- pipelines/W.md, #.md, Q.md, RT.md (Plan 04)

## SCOPE LIMITS
- File, Math, T only
- No wikilink updates outside these folders (Plan 5)
</boundaries>

<verification>
- [ ] `find docs/user/jm3lib/pipelines/File/ -name "*.md" | wc -l` = 12
- [ ] `find docs/user/jm3lib/pipelines/Math/ -name "*.md" | wc -l` = 9
- [ ] `find docs/user/jm3lib/pipelines/T/ -name "*.md" | wc -l` = 6
- [ ] File.md, Math.md, T.md no longer exist
- [ ] All files have audience: pg-coder
</verification>

<output>
After completion, create `.paul/phases/161-split-jm3lib-docs-per-operator/161-03-SUMMARY.md`
</output>
