---
phase: 161-split-jm3lib-docs-per-operator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/user/jm3lib/collectors/Agg.md (delete)
  - docs/user/jm3lib/collectors/Sync.md (delete)
  - docs/user/jm3lib/collectors/INDEX.md (create)
  - docs/user/jm3lib/collectors/Agg/INDEX.md (create)
  - docs/user/jm3lib/collectors/Agg/Sum.md (create)
  - docs/user/jm3lib/collectors/Agg/Count.md (create)
  - docs/user/jm3lib/collectors/Agg/Average.md (create)
  - docs/user/jm3lib/collectors/Agg/Max.md (create)
  - docs/user/jm3lib/collectors/Agg/Min.md (create)
  - docs/user/jm3lib/collectors/Agg/Concatenate.md (create)
  - docs/user/jm3lib/collectors/Sync/INDEX.md (create)
  - docs/user/jm3lib/collectors/Sync/All.md (create)
  - docs/user/jm3lib/collectors/Sync/First.md (create)
  - docs/user/jm3lib/collectors/Sync/Nth.md (create)
  - docs/user/jm3lib/collectors/Sync/Ignore.md (create)
  - docs/user/jm3lib/collectors/Into/INDEX.md (create)
  - docs/user/jm3lib/collectors/Into/Array.md (reformat)
  - docs/user/jm3lib/collectors/Into/Map.md (reformat)
  - docs/user/jm3lib/collectors/Into/Serial.md (reformat)
  - docs/user/jm3lib/collectors/Into/Level.md (reformat)
  - docs/user/jm3lib/collectors/Into/Dataframe.md (reformat)
autonomous: true
---

<objective>
## Goal
Split collectors/Agg.md and collectors/Sync.md into individual per-operator files. Reformat existing Into/*.md files to match the collector doc template. Create INDEX files for all three collector sub-namespaces plus a top-level collectors/INDEX.md.

## Purpose
Applies the 1-file-per-operator pattern (established by #160 DT/ split) to the collector namespace. Makes each operator independently linkable and gives every collector file the standard sections: Syntax, Inputs, Outputs, Errors, Permissions, Related.

## Output
- 6 new Agg/*.md files + INDEX
- 4 new Sync/*.md files + INDEX (All, First, Nth, Ignore — *Second is documented as alias inside Nth.md)
- 5 reformatted Into/*.md files + INDEX
- 1 top-level collectors/INDEX.md
- Agg.md and Sync.md deleted
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work
@.paul/phases/160-split-dt-pipeline-template/160-01-SUMMARY.md — DT/ split established template

## Source Files
@docs/user/jm3lib/collectors/Agg.md — 6 operators to split
@docs/user/jm3lib/collectors/Sync.md — *All, *First, *Second, *Nth, $*, *Ignore
@docs/user/jm3lib/collectors/Into/Array.md — reformat reference (existing format)
@docs/user/jm3lib/collectors/Into/Map.md
@docs/user/jm3lib/collectors/Into/Serial.md
@docs/user/jm3lib/collectors/Into/Level.md
@docs/user/jm3lib/collectors/Into/Dataframe.md

## Template References
@docs/user/jm3lib/pipelines/DT/INDEX.md — INDEX template
@docs/user/jm3lib/pipelines/DT/Now.md — pipeline file template (adapt ## Definition → ## Syntax for collectors)
</context>

<acceptance_criteria>

## AC-1: Individual collector files exist
```gherkin
Given Agg.md contained 6 operators and Sync.md contained 5 operators
When plan executes
Then each operator has its own .md file under Agg/ or Sync/ respectively
And Agg.md and Sync.md are deleted
```

## AC-2: All collector files follow template
```gherkin
Given the collector doc template uses ## Syntax (not ## Definition)
When any collector .md file is inspected
Then it has: frontmatter (audience: pg-coder, type: specification), description, ## Syntax, ## Inputs, ## Outputs, ## Errors, ## Permissions, ## Related
```

## AC-3: INDEX files provide navigation
```gherkin
Given collectors have three sub-namespaces (Agg, Into, Sync)
When INDEX.md files are inspected
Then collectors/INDEX.md lists all three sub-namespaces with wikilinks
And each sub-namespace INDEX lists its individual operators with wikilinks
```

## AC-4: Into/*.md files reformatted
```gherkin
Given Into/ files currently have ## IO Signature and ## Usage
When reformatted
Then they have ## Syntax, ## Inputs, ## Outputs, ## Errors, ## Permissions, ## Related
And existing content (description, code examples) is preserved
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Split Agg.md into individual files + INDEX</name>
  <files>
    docs/user/jm3lib/collectors/Agg/INDEX.md,
    docs/user/jm3lib/collectors/Agg/Sum.md,
    docs/user/jm3lib/collectors/Agg/Count.md,
    docs/user/jm3lib/collectors/Agg/Average.md,
    docs/user/jm3lib/collectors/Agg/Max.md,
    docs/user/jm3lib/collectors/Agg/Min.md,
    docs/user/jm3lib/collectors/Agg/Concatenate.md,
    docs/user/jm3lib/collectors/Agg.md (delete)
  </files>
  <action>
    Create Agg/ directory with 6 operator files + INDEX.md.

    Each operator file follows this structure:
    ```
    ---
    audience: pg-coder
    type: specification
    updated: 2026-04-07
    status: stable
    ---

    # *Agg.{Name}

    {description}

    ## Syntax

    ```polyglot
    [r] *Agg.{Name}
       [*] <{input} << ${source}
       [*] >{output} >> ${target}
    ```

    ## Inputs

    | Name | Type | Description |
    |------|------|-------------|
    | `<{input}` | `#{type}` | {desc} |

    ## Outputs

    | Name | Type | Description |
    |------|------|-------------|
    | `>{output}` | `#{type}` | {desc} |

    ## Errors

    None.

    ## Permissions

    None.

    ## Related

    - [[jm3lib/collectors/Agg/INDEX|*Agg Aggregation Collectors]]
    - [[concepts/collections/collect|Collect Operators]]
    ```

    Operators and their IO (from Agg.md):
    - Sum: <number >sum
    - Count: <item >count
    - Average: <number >average
    - Max: <number >max
    - Min: <number >min
    - Concatenate: <string >result

    INDEX.md: frontmatter + description of *Agg namespace + wikilink listing of all 6 operators.

    After creating files, delete Agg.md.
  </action>
  <verify>
    - ls docs/user/jm3lib/collectors/Agg/ shows 7 files (INDEX + 6 operators)
    - Each file has ## Syntax, ## Inputs, ## Outputs, ## Errors, ## Permissions, ## Related
    - Agg.md no longer exists
  </verify>
  <done>AC-1 (Agg split) and AC-2 (template) and AC-3 (INDEX) satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Split Sync.md into individual files + INDEX</name>
  <files>
    docs/user/jm3lib/collectors/Sync/INDEX.md,
    docs/user/jm3lib/collectors/Sync/All.md,
    docs/user/jm3lib/collectors/Sync/First.md,
    docs/user/jm3lib/collectors/Sync/Nth.md,
    docs/user/jm3lib/collectors/Sync/Ignore.md,
    docs/user/jm3lib/collectors/Sync.md (delete)
  </files>
  <action>
    Create Sync/ directory with 4 operator files + INDEX.md.

    Operators (from Sync.md):
    - *All: wait for ALL listed variables to be Final. Uses [*] << only, no [*] >>. No type constraint.
    - *First: sugar for *Nth n=1. Takes first arriving value, cancels others. Same-type inputs. Has [*] >> output.
    - *Nth: generic race form. <n#int input + << candidates + >> winner. Document *Second as alias (n=2) inside this file, not as separate file.
    - *Ignore: explicit collector discard. << input only.

    Note: $* (inline discard) is a language construct, not a collector operator. Document it as a note in Sync/INDEX.md rather than its own file.

    Each file follows collector template (## Syntax, ## Inputs, ## Outputs, ## Errors, ## Permissions, ## Related).

    After creating files, delete Sync.md.
  </action>
  <verify>
    - ls docs/user/jm3lib/collectors/Sync/ shows 5 files (INDEX + 4 operators)
    - Each file has all required sections
    - *Second documented as alias inside Nth.md
    - $* documented in INDEX.md
    - Sync.md no longer exists
  </verify>
  <done>AC-1 (Sync split) and AC-2 (template) and AC-3 (INDEX) satisfied</done>
</task>

<task type="auto">
  <name>Task 3: Reformat Into/*.md files + create INDEX files</name>
  <files>
    docs/user/jm3lib/collectors/Into/INDEX.md,
    docs/user/jm3lib/collectors/Into/Array.md,
    docs/user/jm3lib/collectors/Into/Map.md,
    docs/user/jm3lib/collectors/Into/Serial.md,
    docs/user/jm3lib/collectors/Into/Level.md,
    docs/user/jm3lib/collectors/Into/Dataframe.md,
    docs/user/jm3lib/collectors/INDEX.md
  </files>
  <action>
    1. Reformat each Into/*.md file:
       - Keep existing frontmatter (update status: draft → stable if applicable, updated: 2026-04-07)
       - Keep existing description paragraph
       - Rename ## IO Signature → ## Inputs table + ## Outputs table (separate tables)
       - Rename ## Usage → ## Syntax
       - Add missing sections: ## Errors (None), ## Permissions (None), ## Related (wikilinks to Into/INDEX and concepts/collections)
       - Preserve all existing code examples and notes (e.g., Map duplicate key note)

    2. Create Into/INDEX.md:
       - Frontmatter + description of *Into namespace
       - Wikilink listing of all 5 Into collectors

    3. Create top-level collectors/INDEX.md:
       - Frontmatter + description of collector namespace (* prefix)
       - Three sub-sections: Data Collectors (*Into), Aggregation (*Agg), Collect-All & Race (*All/*First/*Nth)
       - Wikilinks to each sub-namespace INDEX
       - Note: collectors use [r]/[p] invocation markers and [*] IO lines
  </action>
  <verify>
    - Each Into/*.md has ## Syntax, ## Inputs, ## Outputs, ## Errors, ## Permissions, ## Related
    - Into/INDEX.md lists all 5 operators
    - collectors/INDEX.md lists all 3 sub-namespaces
    - No ## IO Signature or ## Usage headings remain
  </verify>
  <done>AC-3 (INDEX files) and AC-4 (Into reformat) satisfied</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- docs/user/jm3lib/pipelines/ (handled by later plans)
- docs/user/jm3lib/expanders/ (handled by Plan 2)
- docs/user/jm3lib/types/ (not in scope)
- docs/user/jm3lib/INDEX.md (handled by Plan 5)
- docs/technical/ (wikilink updates in Plan 5)

## SCOPE LIMITS
- Collectors only — no pipeline or expander work in this plan
- No wikilink updates outside the collectors/ directory
- Do not change jm3lib/INDEX.md (Plan 5)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `find docs/user/jm3lib/collectors/ -name "*.md" | wc -l` = 18 (INDEX + 5 Into + INDEX + 6 Agg + INDEX + 4 Sync + INDEX)
- [ ] `grep -r "## IO Signature" docs/user/jm3lib/collectors/` returns nothing
- [ ] `grep -r "## Usage" docs/user/jm3lib/collectors/` returns nothing
- [ ] Every .md file has `audience: pg-coder` in frontmatter
- [ ] Every operator file has ## Syntax, ## Inputs, ## Outputs, ## Errors, ## Permissions, ## Related
- [ ] Agg.md and Sync.md no longer exist
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All 3 tasks completed
- All verification checks pass
- 18 .md files in collectors/ tree
- Zero broken internal wikilinks within collectors/
</success_criteria>

<output>
After completion, create `.paul/phases/161-split-jm3lib-docs-per-operator/161-01-SUMMARY.md`
</output>
