---
phase: issue-318-otel-foundation
plan: 02
type: execute
wave: 1
depends_on: ["318-01"]
files_modified:
  - docs/technical/spec/otel-permission-events.md
  - docs/technical/spec/job-sandbox.md
  - docs/technical/compiler/compliance-report.md
  - docs/technical/spec/behavior-contract.md
autonomous: true
---

<objective>
## Goal
Add cross-references from existing OTel-related docs to the two new foundation specs (otel-foundation.md, otel-config.md), and resolve the 3 open questions in otel-permission-events.md that were answered by plan 318-01.

## Purpose
Completes the OTel documentation web — all related specs now reference each other bidirectionally. Closes the open questions that #315 deferred to #318.

## Output
4 existing files updated with cross-references and resolved open questions.
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work
@.paul/phases/issue-318-otel-foundation/318-01-SUMMARY.md — created otel-foundation.md and otel-config.md

## Source Files
@docs/technical/spec/otel-permission-events.md
@docs/technical/spec/job-sandbox.md
@docs/technical/compiler/compliance-report.md
@docs/technical/spec/behavior-contract.md
</context>

<acceptance_criteria>

## AC-1: Open Questions Resolved
```gherkin
Given otel-permission-events.md has 3 open questions (fallback logging, sampling, NATS trace context)
When plan 318-02 executes
Then the "Open Questions" section is replaced with a "Resolved by #318" section containing answers and cross-references to otel-foundation.md and otel-config.md
```

## AC-2: Cross-References Added to otel-permission-events.md
```gherkin
Given otel-permission-events.md references #318 but not the actual docs
When plan 318-02 executes
Then @c: imports and [[wikilinks]] for otel-foundation and otel-config are added to the header, and the "Scope boundary" paragraph links to [[otel-config]] instead of "per #318"
```

## AC-3: Cross-References Added to job-sandbox.md
```gherkin
Given job-sandbox.md references otel-permission-events but not otel-foundation or otel-config
When plan 318-02 executes
Then @c: imports and [[wikilinks]] for otel-foundation and otel-config are added to the header and Related line
```

## AC-4: Cross-References Added to compliance-report.md
```gherkin
Given compliance-report.md references otel-permission-events but not otel-foundation or otel-config
When plan 318-02 executes
Then @c: imports and [[wikilinks]] for otel-foundation and otel-config are added
```

## AC-5: Cross-References Added to behavior-contract.md
```gherkin
Given behavior-contract.md references otel-permission-events but not otel-foundation or otel-config
When plan 318-02 executes
Then @c: imports and [[wikilinks]] for otel-foundation and otel-config are added to the header and Related line
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Resolve open questions and add cross-refs in otel-permission-events.md</name>
  <files>docs/technical/spec/otel-permission-events.md</files>
  <action>
    1. Add @c: imports for otel-foundation and otel-config to the header cross-reference block (after existing @c: lines)
    2. Add [[otel-foundation]] and [[otel-config]] to the Related: line
    3. Update the "Scope boundary" paragraph: replace "per #318 (OTel foundation)" with "in [[otel-config]]" and add reference to [[otel-foundation]] for the tracing infrastructure
    4. Replace the "## Open Questions" section (lines 497-501) with a "## Resolved by #318" section that:
       - **Fallback logging:** Resolved — stderr JSON fallback when exporter unavailable. See [[otel-config#Fallback Logging]].
       - **Sampling:** Resolved — tail-based sampling with keep_errors=true guarantees error/violation retention. See [[otel-config#Sampling Strategy]].
       - **NATS trace context:** Resolved — W3C traceparent injected into all 3 NATS hops. See [[otel-foundation#NATS Trace Context Propagation]].
  </action>
  <verify>Grep for "Open Questions" — should not appear. Grep for "otel-foundation" and "otel-config" — both should appear in header and resolved section.</verify>
  <done>AC-1 and AC-2 satisfied: open questions resolved with cross-refs; header updated</done>
</task>

<task type="auto">
  <name>Task 2: Add cross-refs to job-sandbox.md, compliance-report.md, behavior-contract.md</name>
  <files>docs/technical/spec/job-sandbox.md, docs/technical/compiler/compliance-report.md, docs/technical/spec/behavior-contract.md</files>
  <action>
    For each file, add @c: imports and [[wikilinks]] for otel-foundation and otel-config:

    **job-sandbox.md:**
    - Add `<!-- @c:technical/spec/otel-foundation -->` and `<!-- @c:technical/spec/otel-config -->` after existing @c: lines
    - Add [[otel-foundation]] and [[otel-config]] to the Related: line

    **compliance-report.md:**
    - Add `<!-- @c:technical/spec/otel-foundation -->` and `<!-- @c:technical/spec/otel-config -->` after existing @c: line
    - Add [[otel-foundation]] and [[otel-config]] to the See also / Related section at bottom

    **behavior-contract.md:**
    - Add `<!-- @c:technical/spec/otel-foundation -->` and `<!-- @c:technical/spec/otel-config -->` after existing @c: line
    - Add [[otel-foundation]] and [[otel-config]] to the Related: line
  </action>
  <verify>Grep for "otel-foundation" in all 3 files — each should have at least 1 match. Grep for "otel-config" likewise.</verify>
  <done>AC-3, AC-4, AC-5 satisfied: all 3 files have bidirectional cross-references</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- docs/technical/spec/otel-foundation.md (created by 318-01, already complete)
- docs/technical/spec/otel-config.md (created by 318-01, already complete)
- Any file content beyond cross-reference headers and the open questions section

## SCOPE LIMITS
- Only add cross-references and resolve open questions
- Do not restructure or rewrite existing content
- Do not add new event specifications or attributes

</boundaries>

<verification>
Before declaring plan complete:
- [ ] No "Open Questions" section remains in otel-permission-events.md
- [ ] All 4 files have @c: imports for both otel-foundation and otel-config
- [ ] All 4 files have [[wikilinks]] for both otel-foundation and otel-config
- [ ] otel-permission-events.md scope boundary references [[otel-config]] not "per #318"
- [ ] No broken cross-references introduced
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- 4 files updated, 0 files created
- Open questions from #315 visibly resolved with traceable cross-references
</success_criteria>

<output>
After completion, create `.paul/phases/issue-318-otel-foundation/318-02-SUMMARY.md`
</output>
