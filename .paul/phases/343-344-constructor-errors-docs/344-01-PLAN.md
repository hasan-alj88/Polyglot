---
phase: 343-344-constructor-errors-docs
plan: 344-01
type: execute
wave: 2
depends_on: ["343-01"]
files_modified:
  - docs/technical/compile-rules/PGE/PGE12003-invalid-inline-pipeline-argument.md
  - docs/technical/compile-rules/PGE/PGE12005-inline-format-mismatch.md
  - docs/technical/compile-rules/PGE/PGE12009-template-type-coercion-failure.md
  - docs/user/concepts/pipelines/inline-calls.md
  - docs/user/aj3lib/pipelines/Path.md
  - docs/user/aj3lib/pipelines/DT/INDEX.md
  - docs/user/syntax/types/strings.md
  - docs/technical/edge-cases/04-type-system.md
  - docs/user/syntax/constructors.md
  - docs/user/SPEC-INDEX.md
autonomous: true
---

<objective>
## Goal
Update all documentation referencing inline pipeline syntax to reflect the {$} constructor design. Scope PGE12003/12005/12009 to infrastructure-only, rewrite inline-calls.md, update aj3lib pipeline docs and examples.

## Purpose
Completes the inline-to-constructor transition started by #341/#342. Without these updates, docs contain contradictory examples — some showing `-Path"..."` in execution body (old style) alongside `$Path"..."` constructors (new style). Users would see conflicting guidance.

## Output
- 3 PGE12xxx files updated (scoped to infrastructure lines, cross-ref to PGE14xxx)
- inline-calls.md fully rewritten as infrastructure-only reference
- aj3lib Path.md and DT/INDEX.md updated to reference constructors
- strings.md and edge-cases updated with constructor examples
- constructors.md gains compile rules reference table
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work (genuine dependency)
@.paul/phases/343-344-constructor-errors-docs/343-01-PLAN.md (PGE14xxx codes must exist first)

## Source Files
@docs/user/concepts/pipelines/inline-calls.md (major rewrite target)
@docs/user/aj3lib/pipelines/Path.md (update target)
@docs/user/syntax/types/strings.md (update examples)
@docs/technical/edge-cases/04-type-system.md (update examples)
@docs/user/syntax/constructors.md (add compile rules table)
@docs/technical/compile-rules/PGE/PGE12003-invalid-inline-pipeline-argument.md (scope to infrastructure)
@docs/technical/compile-rules/PGE/PGE12005-inline-format-mismatch.md (scope to infrastructure)
@docs/technical/compile-rules/PGE/PGE12009-template-type-coercion-failure.md (scope to infrastructure)
</context>

<skills>
No specialized flows configured.
</skills>

<acceptance_criteria>

## AC-1: PGE12003/12005/12009 Scoped to Infrastructure
```gherkin
Given PGE12003/12005/12009 currently describe inline pipeline calls generally
When the files are updated
Then each file's Statement clarifies it applies to infrastructure lines ([T]/[Q]/[W]) only
And each file cross-references PGE14xxx for constructor errors in execution body
And examples show infrastructure-line inline calls (not execution body)
```

## AC-2: inline-calls.md Rewritten as Infrastructure-Only
```gherkin
Given inline-calls.md currently has a "Scope change" notice at the top
When the file is rewritten
Then the title reflects infrastructure-only scope
And all execution body examples are removed or replaced with constructor references
And the %InlineString section remains (applies to infrastructure pipelines)
And the Three-Context Rule is cross-referenced (from constructors.md)
And the Mermaid flowchart is updated if it shows execution body usage
```

## AC-3: aj3lib Path.md Updated
```gherkin
Given Path.md shows -Path with %InlineString as its primary interface
When the file is updated
Then it clarifies -Path is an infrastructure pipeline (for [T]/[Q]/[W] config lines)
And it references $Path constructor (from aj3lib/constructors/Path.md) for execution body use
And the behavior section distinguishes infrastructure vs constructor usage
```

## AC-4: strings.md and Edge Cases Updated
```gherkin
Given strings.md shows `-Path"..."` as the way to create #path values
When the file is updated
Then execution body examples use `$Path"..."` constructor syntax
And infrastructure examples retain `-Path"..."` syntax
And edge-cases/04-type-system.md examples are updated accordingly
And any -Greeting"..." or -ParsePair"..." examples are updated or noted as infrastructure-only
```

## AC-5: Constructor Compile Rules Reference
```gherkin
Given constructors.md documents constructor syntax and behavior
When the file is updated
Then a Compile Rules section lists PGE14xxx codes with brief descriptions
And the section follows the same format as inline-calls.md's Compile Rules table
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Scope PGE12003/12005/12009 to infrastructure lines</name>
  <files>
    docs/technical/compile-rules/PGE/PGE12003-invalid-inline-pipeline-argument.md,
    docs/technical/compile-rules/PGE/PGE12005-inline-format-mismatch.md,
    docs/technical/compile-rules/PGE/PGE12009-template-type-coercion-failure.md
  </files>
  <action>
    For each file:
    1. Update Statement to clarify scope: "When an inline pipeline call `-Foo"arg"` on an infrastructure line ([T], [Q], [W]) targets a pipeline that..."
    2. Add a note: "This rule applies to infrastructure inline calls only. For constructor errors in execution body, see PGE14xxx (Constructors category)."
    3. Update examples to show infrastructure-line context (e.g., `[T] -T.SomeTrigger"config"`)
    4. Add cross-reference See Also links to relevant PGE14xxx codes
    
    PGE12003 (Undefined Inline Template): Still valid for infrastructure lines — if -T.MyTrigger"config" has no %InlineString. Update the -Greeting example to use a trigger/wrapper context.
    PGE12005 (Inline Format Mismatch): Still valid — template extraction fails on infrastructure line. Update examples.
    PGE12009 (Template Type Coercion Failure): Still valid — coercion fails on infrastructure inline. Update examples.
    
    Avoid: Deleting these files or marking them retired — they are still active for infrastructure lines.
    Avoid: Changing PGE12006/12007/12008/12010, PGW12001/12002 — those apply to %InlineString generally and remain correct.
  </action>
  <verify>Each file's Statement mentions "infrastructure line" and has PGE14xxx cross-reference.</verify>
  <done>AC-1 satisfied: PGE12003/12005/12009 scoped to infrastructure with PGE14xxx cross-refs.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite inline-calls.md and update Path.md, DT/INDEX.md</name>
  <files>
    docs/user/concepts/pipelines/inline-calls.md,
    docs/user/aj3lib/pipelines/Path.md,
    docs/user/aj3lib/pipelines/DT/INDEX.md
  </files>
  <action>
    **inline-calls.md:**
    1. Replace the temporary "Scope change" notice with proper content
    2. Rename title to "Inline Pipeline Calls (Infrastructure Configuration)"
    3. Remove or relocate the "Superseded" notice — fold into the body as the authoritative statement
    4. Keep all %InlineString documentation (still applies to infrastructure pipelines)
    5. Keep Cross-Language Inline Calls section (infrastructure wrappers are a valid use case)
    6. Remove all execution body inline examples or mark them clearly as "superseded by constructors"
    7. Update the Mermaid flowchart: remove -Path as example (it's now a constructor); use -T.Daily or -W.Env as the example
    8. Add a prominent "For Execution Body" redirect to [[syntax/constructors]]
    9. Update the "Compile Rules" table at the bottom: keep PGE12xxx for infrastructure, add note about PGE14xxx for constructors
    10. Update the "See Also" section to include constructors.md
    
    **Path.md:**
    1. Add a note at the top: "-Path is available as an infrastructure pipeline for [T]/[Q]/[W] configuration. For creating #path values in pipeline execution body, use the $Path constructor — see [[aj3lib/constructors/Path]]."
    2. Keep the %InlineString declaration (still valid for infrastructure)
    3. Update the Behavior section: distinguish infrastructure use (`[W] -W.Env; -Path"./scripts/..."`) from execution body use (redirect to $Path)
    
    **DT/INDEX.md:**
    1. Read the file first to assess current state
    2. If it references %InlineString or inline calls in execution body, add the same infrastructure/constructor distinction as Path.md
    3. Reference $DT constructor from aj3lib/constructors/DT.md
    
    Avoid: Deleting the %InlineString section from inline-calls.md — it's still authoritative for infrastructure.
    Avoid: Breaking the Cross-Language Inline Calls section — it's a valid infrastructure pattern.
  </action>
  <verify>inline-calls.md no longer shows execution body inline examples. Path.md references $Path constructor. DT/INDEX.md references $DT constructor.</verify>
  <done>AC-2 and AC-3 satisfied: inline-calls.md rewritten, aj3lib docs updated.</done>
</task>

<task type="auto">
  <name>Task 3: Update strings.md, edge-cases, and add compile rules to constructors.md</name>
  <files>
    docs/user/syntax/types/strings.md,
    docs/technical/edge-cases/04-type-system.md,
    docs/user/syntax/constructors.md,
    docs/user/SPEC-INDEX.md
  </files>
  <action>
    **strings.md:**
    1. Update the `-Path"..."` Inline Notation section (around line 54):
       - Rename to `$Path"..."` Constructor Notation (or "Path Creation")
       - Change all execution body examples from `-Path"..."` to `$Path"..."`
       - Add note that `-Path"..."` is still valid on infrastructure lines
       - Update the type mismatch note: use $Path constructor instead of -Path inline
    2. Update any cross-references that mention inline calls to include constructors
    
    **edge-cases/04-type-system.md:**
    1. EC-4.5: Update title and description — `-Path"..."` → `$Path"..."` for execution body examples
       - Keep infrastructure examples with `-Path"..."` if any
       - Update VALID examples: `[-] $dir#path << $Path"/tmp/MyApp"` etc.
       - The `-ParsePair"..."` example around line 134 is a user-defined pipeline — this is an infrastructure inline call, so it stays as-is. Add a comment clarifying this is infrastructure/pipeline context.
    2. EC-4.11 (-Greeting"..." dual mode): Update to note constructor distinction
    3. EC-4.12 (PGE12003): Add note that this applies to infrastructure lines
    
    **constructors.md:**
    1. Add a "## Compile Rules" section before "## What Constructors Are NOT"
    2. Include a table of PGE14xxx codes with brief descriptions:
       | Code | Name | Context |
       |------|------|---------|
       | PGE14001 | Ambiguous Constructor Overload | Definition |
       | ... (all 11 codes) |
    3. Link each code to its full specification file
    
    **SPEC-INDEX.md:**
    1. If constructors are not yet listed in the spec index, add an entry
    2. Verify the learning path includes constructor documentation
    
    Avoid: Changing examples that legitimately show infrastructure inline calls (triggers, wrappers).
    Avoid: Removing the -ParsePair or -Greeting pipeline examples — they are valid user-defined infrastructure pipelines.
  </action>
  <verify>strings.md shows $Path"..." for execution body. edge-cases examples updated. constructors.md has Compile Rules table with 11 PGE14xxx entries.</verify>
  <done>AC-4 and AC-5 satisfied: strings/edge-cases updated, compile rules table in constructors.md.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- docs/user/aj3lib/constructors/ (aj3lib constructor docs from #342 — stable)
- docs/technical/ebnf/definition-blocks/09-13-constructor.md (EBNF grammar — stable)
- docs/technical/compile-rules/PGE/PGE14xxx (created by plan 343-01 — do not modify)
- docs/technical/compile-rules/PGE/PGE12001 (metadata field access — unrelated)
- docs/technical/compile-rules/PGE/PGE12002 (duplicate alias — unrelated)
- docs/technical/compile-rules/PGE/PGE12004 (empty metadata alias — unrelated)
- docs/technical/compile-rules/PGE/PGE12006-12010 (template validation — still valid for %InlineString)
- docs/technical/compile-rules/PGW/PGW12001-12002 (template warnings — still valid)

## SCOPE LIMITS
- Do not create new documentation files (constructor concept page, block reference, etc. already exist from #341)
- Do not modify EBNF grammar
- Do not modify aj3lib constructor definition files
- Do not update brainstorming/datetime-tree.md (low priority, technical brainstorming doc)
- Do not modify the %InlineString mechanism itself — only update where it is referenced

</boundaries>

<verification>
Before declaring plan complete:
- [ ] PGE12003/12005/12009 scoped to infrastructure with PGE14xxx cross-refs
- [ ] inline-calls.md shows no execution body inline examples
- [ ] Path.md references $Path constructor for execution body
- [ ] DT/INDEX.md references $DT constructor for execution body
- [ ] strings.md uses $Path"..." in execution body examples
- [ ] edge-cases/04-type-system.md examples updated
- [ ] constructors.md has Compile Rules section with 11 PGE14xxx codes
- [ ] No infrastructure inline call examples were removed (triggers, wrappers, queues stay)
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All inline-to-constructor documentation transition complete
- PGE12xxx rules scoped to infrastructure with cross-references
- No contradictory examples remain (old -Path"..." vs new $Path"..." in execution body)
- Constructor compile rules discoverable from constructors.md
</success_criteria>

<output>
After completion, create `.paul/phases/343-344-constructor-errors-docs/344-01-SUMMARY.md`
</output>
