---
phase: issue-63-mermaid-expand-collect
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/user/concepts/collections.md]
autonomous: true
---

<objective>
## Goal
Add a Mermaid `flowchart LR` diagram to collections.md that visualizes the expand/collect cycle pattern.

## Purpose
Issue #63 — A flowchart makes the expand/collect pattern visually clear: how a source collection is expanded by ~ForEach, processed per item in mini-pipelines, and collected back by *Into/*Agg with results accessible one level up.

## Output
Modified `docs/user/concepts/collections.md` with a Mermaid diagram inserted after the "## Collect Operators" prose (line 63) and before "### `*Into`" heading (line 65).
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Source Files
@docs/user/concepts/collections.md (lines 56-74 — Collect Operators section)
</context>

<acceptance_criteria>

## AC-1: Mermaid diagram present
```gherkin
Given the file docs/user/concepts/collections.md
When a reader opens the "## Collect Operators" section
Then a Mermaid `flowchart LR` diagram appears after the prose and before "### *Into"
```

## AC-2: Diagram shows expand/collect cycle
```gherkin
Given the Mermaid diagram
When rendered
Then it shows left-to-right flow:
  Source Collection → ~ForEach (expand) → mini-pipeline per item → *Into / *Agg (collect) → Result
And the scoping boundary is visible (items isolated, result one level up)
```

## AC-3: No other content changes
```gherkin
Given the file docs/user/concepts/collections.md
When the diagram is added
Then no existing prose, code blocks, or headings are modified
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Insert Mermaid flowchart into Collect Operators section</name>
  <files>docs/user/concepts/collections.md</files>
  <action>
    Insert a Mermaid `flowchart LR` diagram after line 63 ("Use `[r]` when collectors have dependencies between them, `[p]` when they are independent.") and before line 65 ("### `*Into` — Collect into Collection").

    The diagram should show:
    1. Source node: "Source Collection"
    2. Expand node: "~ForEach (expand)"
    3. A subgraph labeled "Per-item scope" containing: "Mini-pipeline per item"
    4. Collect node: "*Into / *Agg (collect)"
    5. Result node: "Result (one level up)"
    6. Sequential arrows: Source → ~ForEach → Mini-pipeline → *Into/*Agg → Result

    Use a subgraph to show the scoping boundary around the mini-pipeline.
    Avoid special characters that break Mermaid (no raw <, >, #, ;).
    Use quoted labels for nodes with special characters like ~ and *.
  </action>
  <verify>
    - grep for "```mermaid" in collections.md confirms diagram present
    - grep for "flowchart LR" confirms correct diagram type
    - "### `*Into`" heading still present after diagram
    - No other sections modified
  </verify>
  <done>AC-1, AC-2, AC-3 satisfied: Mermaid flowchart shows expand/collect cycle with scoping boundary, no other changes</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- All content before the "## Collect Operators" prose paragraph
- The "## Collect Operators" heading and its prose
- "### `*Into`" section and everything after
- Frontmatter

## SCOPE LIMITS
- Only add Mermaid diagram between prose and *Into heading
- Do not modify any other files

</boundaries>

<verification>
Before declaring plan complete:
- [ ] grep for "mermaid" in collections.md finds the diagram
- [ ] grep for "flowchart LR" matches
- [ ] "### `*Into`" heading unchanged
- [ ] No other files modified
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- Mermaid `flowchart LR` diagram inserted in Collect Operators section
- Diagram shows expand/collect cycle with scoping boundary
- No existing content modified
</success_criteria>

<output>
After completion, create `.paul/phases/issue-63-mermaid-expand-collect/63-01-SUMMARY.md`
</output>
