---
phase: issue-272-parameterized-schemas
plan: 05
type: execute
wave: 1
depends_on: ["272-04"]
files_modified:
  - docs/technical/ebnf/01-file-structure.md
  - docs/technical/ebnf/04-type-system.md
  - docs/technical/ebnf/09-definition-blocks.md
  - docs/technical/ebnf/INDEX.md
  - docs/technical/spec/metadata-tree/FULL-TREE.md
  - docs/technical/spec/metadata-tree/object-types.md
  - docs/technical/spec/metadata-tree/definition-templates.md
  - docs/technical/COMPILE-RULES.md
  - docs/technical/compile-rules/PGE/PGE01002-io-before-trigger.md
  - docs/technical/compile-rules/PGE/PGE01008-wrapper-must-reference-macro.md
  - docs/technical/compile-rules/PGE/PGE01010-pipeline-io-name-mismatch.md
  - docs/technical/compile-rules/PGE/PGE01021-empty-data-definition.md
  - docs/technical/compile-rules/PGE/PGE01023-parameterless-macro.md
  - docs/technical/compile-rules/PGE/PGE01029-invalid-marker-for-definition-type.md
  - docs/technical/compile-rules/PGE/PGE01031-forbidden-element-in-definition.md
  - docs/technical/compile-rules/PGE/PGE02006-metadata-pull-only.md
  - docs/technical/compile-rules/PGE/PGE04017-array-dimension-access-mismatch.md
  - docs/technical/compile-rules/PGE/PGE09007-duplicate-pipeline-definition.md
  - docs/technical/compile-rules/PGE/PGE10006-duplicate-permission.md
  - docs/technical/compile-rules/PGW/PGW01003-no-definitions-in-file.md
  - docs/technical/compile-rules/PGW/PGW10001-unused-permission.md
  - docs/technical/edge-cases/18-macro-structure.md
  - docs/technical/edge-cases/24-datatype-defs.md
  - docs/technical/edge-cases/INDEX.md
  - docs/technical/plan/decisions/schema-properties.md
  - docs/technical/plan/decisions/metadata-data-tree.md
  - docs/technical/brainstorming/marker-declarations.md
autonomous: true
---

<objective>
## Goal
Remove all {M} macro references, retired schemas (##Homogeneous, ##Heterogeneous, ##EnumLeafs), %##Children.* properties, and %M metadata branch from technical docs. Replace with parameterized ## schema and generic # type syntax where applicable.

## Purpose
Completes Issue #272 — after plans 01-04 updated user-facing docs, this plan brings technical docs (EBNF, compile rules, metadata tree, edge cases) into alignment with the new design.

## Output
~27 technical files updated; zero {M}/{[M]}/%M/##Homogeneous/##Heterogeneous/##EnumLeafs/%##Children.* remaining anywhere in docs/ (except user redirect stubs).
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work
@.paul/phases/issue-272-parameterized-schemas/272-04-SUMMARY.md

## Design Reference
@memory/pg_design_272_branch_descriptors.md
</context>

<acceptance_criteria>

## AC-1: No {M} in docs/technical/
```gherkin
Given plan 272-05 is applied
When grepping for {M} in docs/technical/
Then zero matches are returned
```

## AC-2: No [M] in docs/technical/
```gherkin
Given plan 272-05 is applied
When grepping for [M] in docs/technical/
Then zero matches are returned
```

## AC-3: No retired schemas in docs/
```gherkin
Given plan 272-05 is applied
When grepping for ##Homogeneous, ##Heterogeneous, or ##EnumLeafs in docs/
Then zero matches are returned (except redirect stubs in docs/user/pglib/types/schemas/)
```

## AC-4: No %##Children.* in docs/
```gherkin
Given plan 272-05 is applied
When grepping for %##Children. in docs/
Then zero matches are returned
```

## AC-5: No %M branch in metadata trees
```gherkin
Given plan 272-05 is applied
When grepping for %M as metadata branch in docs/technical/
Then zero matches are returned
```

## AC-6: EBNF macro grammar replaced
```gherkin
Given plan 272-05 is applied
When reading EBNF section 09 (definition blocks)
Then {M} section is removed and {#} section includes [#] <param generic syntax
And macro_def/macro_body/macro_input/macro_output productions are removed from all EBNF files
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: EBNF grammar — remove macros, add generics</name>
  <files>
    docs/technical/ebnf/01-file-structure.md,
    docs/technical/ebnf/04-type-system.md,
    docs/technical/ebnf/09-definition-blocks.md,
    docs/technical/ebnf/INDEX.md
  </files>
  <action>
    1. **09-definition-blocks.md**: Remove §9.4 {M} Macro Definitions entirely. In §9.2 {#} Data Definitions, add [#] <param generic input syntax (type_param_input production). Update any cross-references.
    2. **04-type-system.md**: Remove macro_def, macro_body, macro_input, macro_output productions. Replace with generic_param and type_param_input productions for [#] <param syntax. Remove ##Homogeneous, ##Heterogeneous, ##EnumLeafs from schema lists. Add new schemas (##Inf, ##Sorted, ##Fields, ##Nullable, ##Result, ##String, ##Map, ##Array, ##Set, ##Dataframe). Replace %##Children.* with new %## properties (%##Active, %##Flexible, %##Sorted, %##Propagate, %##Level.N, %##Range) and %### properties (%###Type, %###Kind, %###Unique).
    3. **01-file-structure.md**: Remove macro_def from any file-level production references.
    4. **INDEX.md**: Remove {M} from section index; update cross-references.
    
    Refer to memory/pg_design_272_branch_descriptors.md for exact property names and schema list.
  </action>
  <verify>
    Grep for {M}, [M], macro_def, macro_body, macro_input, macro_output, ##Homogeneous, ##Heterogeneous, ##EnumLeafs, %##Children. in all 4 EBNF files → zero matches each
  </verify>
  <done>AC-1, AC-2, AC-3, AC-4, AC-6 satisfied for EBNF files</done>
</task>

<task type="auto">
  <name>Task 2: Metadata tree + compile rules — purge {M} and retired schemas</name>
  <files>
    docs/technical/spec/metadata-tree/FULL-TREE.md,
    docs/technical/spec/metadata-tree/object-types.md,
    docs/technical/spec/metadata-tree/definition-templates.md,
    docs/technical/COMPILE-RULES.md,
    docs/technical/compile-rules/PGE/PGE01002-io-before-trigger.md,
    docs/technical/compile-rules/PGE/PGE01008-wrapper-must-reference-macro.md,
    docs/technical/compile-rules/PGE/PGE01010-pipeline-io-name-mismatch.md,
    docs/technical/compile-rules/PGE/PGE01021-empty-data-definition.md,
    docs/technical/compile-rules/PGE/PGE01023-parameterless-macro.md,
    docs/technical/compile-rules/PGE/PGE01029-invalid-marker-for-definition-type.md,
    docs/technical/compile-rules/PGE/PGE01031-forbidden-element-in-definition.md,
    docs/technical/compile-rules/PGE/PGE02006-metadata-pull-only.md,
    docs/technical/compile-rules/PGE/PGE04017-array-dimension-access-mismatch.md,
    docs/technical/compile-rules/PGE/PGE09007-duplicate-pipeline-definition.md,
    docs/technical/compile-rules/PGE/PGE10006-duplicate-permission.md,
    docs/technical/compile-rules/PGW/PGW01003-no-definitions-in-file.md,
    docs/technical/compile-rules/PGW/PGW10001-unused-permission.md,
    docs/technical/plan/decisions/metadata-data-tree.md
  </files>
  <action>
    **Metadata tree:**
    1. **FULL-TREE.md**: Remove %M branch entirely. Remove ##Homogeneous, ##Heterogeneous, ##EnumLeafs from schema list. Add new schemas. Replace %##Children.* with new %## property names. Add %### section.
    2. **object-types.md**: Remove {M} Macro from object type list. Remove %M references.
    3. **definition-templates.md**: Remove {M} template. Update {#} template to include [#] <param. Replace %##Children.* with new property names.
    4. **metadata-data-tree.md**: Remove %M references.

    **Compile rules:**
    5. **COMPILE-RULES.md**: Remove {M} from definition type lists. Replace %##Children.* with new property names.
    6. **PGE01008**: This rule references {M} macros — retire as redirect stub (macros no longer exist; wrapper [W] now references {#} definitions directly via generic types).
    7. **PGE01023**: This rule is "parameterless macro" — retire as redirect stub (macros no longer exist; {#} definitions with [#] params validated by different mechanism).
    8. **PGE01029**: Remove {M} from valid definition type list.
    9. **PGE01031**: Remove {M} from forbidden element matrix.
    10. For all other compile rule files: Replace {M} mentions with {#} equivalent where applicable. Do NOT remove {M} from historical context — only from active rule definitions and examples.
  </action>
  <verify>
    Grep for {M}, %M, %##Children., ##Homogeneous, ##Heterogeneous, ##EnumLeafs across all modified files → zero matches each
  </verify>
  <done>AC-1, AC-3, AC-4, AC-5 satisfied for metadata tree and compile rule files</done>
</task>

<task type="auto">
  <name>Task 3: Edge cases, decisions, and brainstorming — purge {M} and retired schemas</name>
  <files>
    docs/technical/edge-cases/18-macro-structure.md,
    docs/technical/edge-cases/24-datatype-defs.md,
    docs/technical/edge-cases/INDEX.md,
    docs/technical/plan/decisions/schema-properties.md,
    docs/technical/brainstorming/marker-declarations.md
  </files>
  <action>
    1. **18-macro-structure.md**: Retire as redirect stub — macros no longer exist. Content replaced by generic # type syntax in {#} definitions. Keep file with redirect notice (same pattern as user docs macros.md).
    2. **24-datatype-defs.md**: Remove {M}, [M], ##Homogeneous, ##Heterogeneous, ##EnumLeafs references. Update examples to use parameterized ## schemas and generic # types with [#] <param.
    3. **INDEX.md**: Update entry for 18-macro-structure.md to note retirement. Remove {M} from any cross-references.
    4. **schema-properties.md** (decisions): Update to reflect new %## / %### property design. Remove %##Children.* references. Replace ##Homogeneous/##Heterogeneous with new schema names.
    5. **marker-declarations.md**: Remove {M} from marker declaration examples.
  </action>
  <verify>
    Grep for {M}, [M], ##Homogeneous, ##Heterogeneous, ##EnumLeafs, %##Children. across all 5 files → zero matches each.
    Final sweep: grep all of docs/ for {M} → only redirect stubs in docs/user/concepts/macros.md and docs/user/syntax/types/macro-types.md
  </verify>
  <done>AC-1, AC-2, AC-3, AC-4 satisfied for edge case and decision files. Full docs/ sweep confirms AC-1 through AC-6.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- docs/user/ files (completed in plans 272-01 through 272-04)
- .paul/ files (managed by PAUL framework)
- docs/audit/ files (writing rules — not affected by {M} removal)
- Archive files (historical record)

## SCOPE LIMITS
- Only remove/replace {M}, [M], %M, macro_def/body/input/output productions, retired schemas, and %##Children.* properties
- Do not restructure technical doc files beyond what's needed for {M} removal
- Do not update compile rule numbering — only content
- Retired compile rules (PGE01008, PGE01023) become redirect stubs, not deleted

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `grep -r '{M}' docs/technical/` → zero matches
- [ ] `grep -r '\[M\]' docs/technical/` → zero matches
- [ ] `grep -r '##Homogeneous\|##Heterogeneous\|##EnumLeafs' docs/` → zero matches (except redirect stubs in docs/user/pglib/types/schemas/)
- [ ] `grep -r '%##Children\.' docs/` → zero matches
- [ ] `grep -r '%M\b' docs/technical/` → zero matches (as metadata branch)
- [ ] `grep -r 'macro_def\|macro_body\|macro_input\|macro_output' docs/technical/` → zero matches
- [ ] `grep -r '{M}' docs/user/` → only 2 redirect stubs (macros.md, macro-types.md)
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All 3 tasks completed
- All 6 acceptance criteria pass
- All verification grep checks return zero matches (or only permitted redirect stubs)
- No errors or broken cross-references introduced
</success_criteria>

<output>
After completion, create `.paul/phases/issue-272-parameterized-schemas/272-05-SUMMARY.md`
</output>
