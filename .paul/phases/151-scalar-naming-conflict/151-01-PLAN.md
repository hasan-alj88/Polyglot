---
phase: 151-scalar-naming-conflict
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/user/aj3lib/types/scalars.md, docs/user/syntax/types/basic-types.md, docs/technical/spec/metadata-tree/string-subtypes.md]
autonomous: true
---

<objective>
## Goal
Add a consistent "Naming Convention" note to all files that reference scalar subtypes, clarifying the three naming levels: `##Int` (schema descriptor), `#Int`/`#int` (user-facing alias), `:int` (metadata tree path).

## Purpose
Issue #151 flags that `##Int`/`##Float` and `int`/`float` appear without context, confusing readers about whether these are the same thing or different. The relationship is correct but undocumented in the files where users encounter these names.

## Output
Updated docs with naming convention notes in the 3 key files where scalar subtypes are defined or explained.
</objective>

<context>
@.paul/PROJECT.md
@.paul/STATE.md

## Source Files
@docs/user/aj3lib/types/scalars.md — scalar subtype definitions
@docs/user/syntax/types/basic-types.md — type system overview with scalar layer
@docs/technical/spec/metadata-tree/string-subtypes.md — already has good explanation at line 39
</context>

<acceptance_criteria>

## AC-1: Naming convention note in scalars.md
```gherkin
Given scalars.md defines ##Int, ##Float etc. with alias column
When a reader encounters these names
Then a naming convention note explains: ## = schema descriptor (metadata), # = user alias, : = tree path
```

## AC-2: Naming convention note in basic-types.md
```gherkin
Given basic-types.md Layer 2 section shows ##Int/##Float with aliases
When a reader sees both ## and lowercase forms
Then a note clarifies the three naming levels and their purpose
```

## AC-3: string-subtypes.md cross-referenced as canonical
```gherkin
Given string-subtypes.md already has the best explanation (line 39)
When other files add naming notes
Then they cross-reference string-subtypes.md as the authoritative source
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add naming convention note to scalars.md</name>
  <files>docs/user/aj3lib/types/scalars.md</files>
  <action>
    After the opening paragraph (line 13), add a naming convention note:
    - `##Int` = schema descriptor — metadata that constrains `#String` (lives at `%##`)
    - `#Int` / `#int` = user-facing alias — what you write in type annotations
    - `:int` = metadata tree path — flexible-level key under `%#:String`
    - `##` describes `#` like `###` describes leafs — they set compiler-enforced metadata
    - A `#` struct can compose multiple `##` schemas as long as they don't contradict
    - Cross-reference string-subtypes.md
  </action>
  <verify>Read scalars.md and confirm naming note present after opening paragraph</verify>
  <done>AC-1 satisfied: naming convention note with three levels explained</done>
</task>

<task type="auto">
  <name>Task 2: Add naming convention note to basic-types.md Layer 2</name>
  <files>docs/user/syntax/types/basic-types.md</files>
  <action>
    In the Layer 2 section (after line 51), add a concise note explaining:
    - `##` names are schema descriptors (metadata the compiler enforces on `#` structs)
    - `#int`/`#float` are user aliases that resolve to `#String` with the `##` schema applied
    - Cross-reference string-subtypes.md for the full resolution table
  </action>
  <verify>Read basic-types.md Layer 2 section and confirm note present</verify>
  <done>AC-2 satisfied: naming convention note in basic-types.md</done>
</task>

<task type="auto">
  <name>Task 3: Mark string-subtypes.md as canonical and tighten wording</name>
  <files>docs/technical/spec/metadata-tree/string-subtypes.md</files>
  <action>
    The existing explanation at line 39 is already good. Add:
    - A brief note that `##` schemas are metadata descriptors — a `#` struct can compose multiple `##` schemas
    - Ensure the key distinction paragraph is clearly marked as the canonical reference
  </action>
  <verify>Read string-subtypes.md and confirm canonical marker present</verify>
  <done>AC-3 satisfied: string-subtypes.md marked as canonical source</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- docs/archive/ (historical content)
- Scalar type definitions themselves (##Int regex, alias values, etc.)
- EBNF grammar files (no grammar changes needed)

## SCOPE LIMITS
- Only add naming convention notes — do not restructure existing content
- Do not rename any types or change any code examples
- Do not touch technical/brainstorming/ or edge-cases/ files

</boundaries>

<verification>
- [ ] scalars.md has naming convention note after opening paragraph
- [ ] basic-types.md has naming convention note in Layer 2 section
- [ ] string-subtypes.md has canonical marker
- [ ] All notes consistently explain the three naming levels
- [ ] No existing content modified beyond adding notes
</verification>

<success_criteria>
- All 3 files have naming convention notes
- Notes consistently use the same terminology
- Cross-references point to string-subtypes.md
- No changes to type definitions or examples
</success_criteria>

<output>
After completion, create `.paul/phases/151-scalar-naming-conflict/151-01-SUMMARY.md`
</output>
