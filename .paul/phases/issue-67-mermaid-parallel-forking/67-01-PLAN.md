---
phase: issue-67-mermaid-parallel-forking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/user/concepts/pipelines.md]
autonomous: true
---

<objective>
## Goal
Add a Mermaid `flowchart TD` diagram to pipelines.md that visualizes the parallel forking pattern in wrapper setup/cleanup.

## Purpose
Issue #67 — A flowchart makes the setup-body-cleanup concurrency flow visually clear: how [p] in [\] runs concurrently with the execution body, and how [*] *All in [/] collects the result.

## Output
Modified `docs/user/concepts/pipelines.md` with a Mermaid diagram inserted after the bullet list (lines 223-225) and before the "Pairing constraint:" paragraph (line 227).
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Source Files
@docs/user/concepts/pipelines.md (lines 219-233 — Parallel Forking in Setup section)
</context>

<acceptance_criteria>

## AC-1: Mermaid diagram present
```gherkin
Given the file docs/user/concepts/pipelines.md
When a reader opens "### Parallel Forking in Setup"
Then a Mermaid `flowchart TD` diagram appears after the bullet list and before the pairing constraint
```

## AC-2: Diagram shows concurrent flow
```gherkin
Given the Mermaid diagram
When rendered
Then it shows:
  - [\] Setup splitting into sequential work and [p] fork
  - Execution Body running concurrently with the [p] task
  - [/] Cleanup collecting via [*] *All, then sequential cleanup
```

## AC-3: No other content changes
```gherkin
Given the file docs/user/concepts/pipelines.md
When the diagram is added
Then no existing prose, code blocks, or headings are modified
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Insert Mermaid flowchart into Parallel Forking section</name>
  <files>docs/user/concepts/pipelines.md</files>
  <action>
    Insert a Mermaid `flowchart TD` diagram after line 225 (the last bullet point about variables) and before line 227 ("**Pairing constraint:**").

    The diagram should show:
    1. Top node: "[\] Setup"
    2. Setup splits into two parallel paths:
       - Left: "Sequential work (blocks before body)"
       - Right: "[p] Fork (parallel task)"
    3. Sequential work leads to "Execution Body"
    4. [p] Fork runs alongside Execution Body (shown as concurrent)
    5. Both Execution Body completion and [p] result flow into "[/] Cleanup"
    6. Cleanup shows "[*] *All collects [p] result"

    Use dashed lines or annotations to indicate concurrency.
    Avoid special characters that break Mermaid (no raw <, >, #, ;).
    Use quoted labels. Use ` --- ` separators instead of `\n` for multi-line labels.
  </action>
  <verify>
    - grep for "```mermaid" in pipelines.md finds diagram in Parallel Forking section
    - grep for "flowchart TD" confirms correct diagram type
    - "**Pairing constraint:**" text still present after diagram
    - No other sections modified
  </verify>
  <done>AC-1, AC-2, AC-3 satisfied: Mermaid flowchart shows parallel forking with concurrent flow, no other changes</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- All content before the bullet list in Parallel Forking section
- The bullet list itself (lines 223-225)
- "**Pairing constraint:**" paragraph and everything after
- Frontmatter
- The execution order diagram added in issue #61

## SCOPE LIMITS
- Only add Mermaid diagram between bullet list and pairing constraint
- Do not modify any other files

</boundaries>

<verification>
Before declaring plan complete:
- [ ] grep for "mermaid" finds diagram in Parallel Forking section
- [ ] grep for "flowchart TD" matches
- [ ] Pairing constraint text unchanged
- [ ] No other files modified
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- Mermaid `flowchart TD` diagram inserted in Parallel Forking section
- Diagram shows setup fork, concurrent body+[p], cleanup collection
- No existing content modified
</success_criteria>

<output>
After completion, create `.paul/phases/issue-67-mermaid-parallel-forking/67-01-SUMMARY.md`
</output>
