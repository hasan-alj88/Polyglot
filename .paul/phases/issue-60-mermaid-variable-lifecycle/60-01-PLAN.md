---
phase: issue-60-mermaid-variable-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/user/concepts/variable-lifecycle.md]
autonomous: true
---

<objective>
## Goal
Add a Mermaid `stateDiagram-v2` diagram to `variable-lifecycle.md` showing the 5 lifecycle stages and all transitions with their operators.

## Purpose
The variable lifecycle is a state machine described only in a table. A visual state diagram makes the transitions and their operators immediately graspable — especially the `<!` fallback bypass from Failed to Final.

## Output
- Modified `docs/user/concepts/variable-lifecycle.md` with a Mermaid diagram inserted after the Stages table
- Fix the intro text that says "four lifecycle stages" (should be "five")
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Source Files
@docs/user/concepts/variable-lifecycle.md
@docs/audit/rules/conventions.md (code blocks must have language tags)

## Issue
GitHub #60: https://github.com/hasan-alj88/Polyglot/issues/60
</context>

<acceptance_criteria>

## AC-1: Mermaid diagram present and correct
```gherkin
Given variable-lifecycle.md is opened in a Mermaid-capable renderer (Obsidian, GitHub)
When the file renders
Then a state diagram is visible showing 5 states (Declared, Default, Final, Failed, Released) with labeled transitions matching the operators in the Stages table
```

## AC-2: Diagram placement correct
```gherkin
Given the file structure
When the diagram is located in the document
Then it appears after the "## Stages" table and before "### Declared"
```

## AC-3: Intro text accuracy
```gherkin
Given the introductory paragraph (line 13)
When it describes the number of lifecycle stages
Then it says "five" not "four"
```

## AC-4: No content removed
```gherkin
Given the existing table, subsections, and code examples
When the diagram is added
Then all existing content is preserved unchanged
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add Mermaid state diagram and fix intro text</name>
  <files>docs/user/concepts/variable-lifecycle.md</files>
  <action>
    1. Fix line 13: change "four lifecycle stages" to "five lifecycle stages"

    2. Insert a Mermaid stateDiagram-v2 block after the Stages table (after line 23, before line 25 "### Declared"). The diagram must show:
       - [*] --> Declared (variable appears in block)
       - Declared --> Default (via <~ or ~>)
       - Declared --> Final (via << or >>)
       - Default --> Final (via << or >>)
       - Final --> Released (scope-end or * collect)
       - Default --> Released (scope-end)
       - Declared --> Failed (source pipeline error)
       - Default --> Failed (source pipeline error)
       - Failed --> Final (via <! fallback — bypasses Failed)
       - Failed is a terminal state WITHOUT <! fallback
       - Released is always terminal

    3. Use ```mermaid language tag (per conventions.md)
    4. Do NOT add new heading levels — the diagram sits between ## Stages and ### Declared
    5. Do NOT modify any other content in the file
  </action>
  <verify>
    - grep -c "stateDiagram-v2" docs/user/concepts/variable-lifecycle.md returns 1
    - grep -c "five lifecycle stages" docs/user/concepts/variable-lifecycle.md returns 1
    - File still contains all 5 subsections (### Declared, ### Default, ### Final, ### Failed, ### Released)
    - File size stays well under 50KB
  </verify>
  <done>AC-1, AC-2, AC-3, AC-4 satisfied</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Any content below "### Declared" (subsection text, code examples, tables)
- Frontmatter
- Smart references (<!-- @ --> lines)

## SCOPE LIMITS
- Only variable-lifecycle.md — no other files
- One diagram only — do not add diagrams to other sections of this file
- Do not refactor existing text beyond the "four"→"five" fix

</boundaries>

<verification>
Before declaring plan complete:
- [ ] Mermaid diagram renders correctly (check syntax is valid stateDiagram-v2)
- [ ] All 5 states present in diagram
- [ ] All transitions labeled with correct operators
- [ ] The `<!` fallback bypass from Failed→Final is shown
- [ ] Intro text says "five" not "four"
- [ ] No existing content removed or modified (beyond the "four"→"five" fix)
- [ ] File under 50KB
</verification>

<success_criteria>
- Mermaid state diagram added to variable-lifecycle.md
- Diagram accurately represents all lifecycle transitions
- Intro text corrected
- All existing content preserved
</success_criteria>

<output>
After completion, create `.paul/phases/issue-60-mermaid-variable-lifecycle/60-01-SUMMARY.md`
</output>
