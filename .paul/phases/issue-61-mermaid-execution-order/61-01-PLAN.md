---
phase: issue-61-mermaid-execution-order
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/user/concepts/pipelines.md]
autonomous: true
---

<objective>
## Goal
Add a Mermaid `flowchart LR` diagram to pipelines.md that visualizes the mandatory pipeline execution order.

## Purpose
Issue #61 — A flowchart makes the execution sequence visually clear: how triggers/IO lead to queue, then wrapper setup, execution body, and cleanup.

## Output
Modified `docs/user/concepts/pipelines.md` with a Mermaid diagram inserted after the "Execution order:" sentence (line 206) and before "### Parallel Forking in Setup" (line 208).
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Source Files
@docs/user/concepts/pipelines.md (line 206 — "Execution order:" sentence in Wrappers section)
</context>

<acceptance_criteria>

## AC-1: Mermaid diagram present
```gherkin
Given the file docs/user/concepts/pipelines.md
When a reader opens the "## Wrappers" section
Then a Mermaid `flowchart LR` diagram appears after the "Execution order:" sentence and before "### Parallel Forking in Setup"
```

## AC-2: Diagram shows full execution sequence
```gherkin
Given the Mermaid diagram
When rendered
Then it shows 5 stages connected left to right:
  [t],[=] Trigger/IO → [Q] Queue → [\] Setup → Execution Body → [/] Cleanup
And arrows connect each stage sequentially
```

## AC-3: No other content changes
```gherkin
Given the file docs/user/concepts/pipelines.md
When the diagram is added
Then no existing prose, code blocks, or headings are modified
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Insert Mermaid flowchart after Execution order sentence</name>
  <files>docs/user/concepts/pipelines.md</files>
  <action>
    Insert a Mermaid `flowchart LR` diagram after line 206 ("Execution order: `[t],[=]` → `[Q]` → `[\]` → Execution Body → `[/]`") and before line 208 ("### Parallel Forking in Setup").

    The diagram should show:
    1. Five nodes connected left to right:
       - TIO: "[t],[=] Trigger/IO"
       - Q: "[Q] Queue"
       - S: "[\] Setup"
       - EB: "Execution Body"
       - C: "[/] Cleanup"
    2. Sequential arrows: TIO → Q → S → EB → C
    3. Keep node labels concise and matching the block element names from the spec
    4. Use the established Mermaid fenced code block style (```mermaid)

    Avoid special characters that break Mermaid parsing (no raw <, >, #, ;).
    Use quotes around node labels containing special characters like brackets.
  </action>
  <verify>
    - grep for "```mermaid" in pipelines.md confirms diagram present in Wrappers section
    - grep for "flowchart LR" confirms correct diagram type
    - "### Parallel Forking in Setup" still present and unchanged after the diagram
    - No other sections modified
  </verify>
  <done>AC-1, AC-2, AC-3 satisfied: Mermaid flowchart shows execution order sequence, no other changes</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- All content before "Execution order:" sentence (line 206)
- "### Parallel Forking in Setup" section and everything after
- Frontmatter
- The "Execution order:" sentence itself

## SCOPE LIMITS
- Only add Mermaid diagram between the sentence and the next heading
- Do not modify any other files

</boundaries>

<verification>
Before declaring plan complete:
- [ ] grep for "mermaid" in pipelines.md finds the diagram in Wrappers section
- [ ] grep for "flowchart LR" matches
- [ ] "### Parallel Forking in Setup" heading unchanged
- [ ] No other files modified
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- Mermaid `flowchart LR` diagram inserted after "Execution order:" sentence
- Diagram shows 5-stage pipeline execution sequence with arrows
- No existing content modified
</success_criteria>

<output>
After completion, create `.paul/phases/issue-61-mermaid-execution-order/61-01-SUMMARY.md`
</output>
