---
phase: 06-audit-structure-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/vision.md, docs/audit/README.md]
autonomous: true
---

<objective>
## Goal
Create the docs/audit/ directory tree and its entry-point README.md, and remove the AI meta-instructions ("Who It's For" section) from docs/vision.md.

## Purpose
docs/audit/ is Claude's operating manual for documentation writing — ground truths, rules, and instructions that Claude reads before writing any doc. The "Who It's For" section in vision.md is AI-facing meta-instructions that don't belong in the product vision; this content will be preserved and expanded in Phase 7's audience files.

## Output
- docs/audit/README.md — entry point with file index, authority chain, smart reference guide
- docs/audit/audiences/ — empty directory (populated in Phase 7)
- docs/audit/rules/ — empty directory (populated in Phase 8)
- docs/audit/reference/ — empty directory (populated in Phase 8)
- docs/vision.md — edited, "Who It's For" section removed
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Source Files
@docs/vision.md (lines 54-65 to remove; lines 67-94 must remain untouched)
@.claude/plans/misty-weaving-pancake.md (approved design with smart referencing spec)
</context>

<skills>
No specialized flows configured.
</skills>

<acceptance_criteria>

## AC-1: Directory Structure Exists
```gherkin
Given the docs/ directory exists
When Phase 6 Plan 01 completes
Then docs/audit/ exists with subdirectories: audiences/, rules/, reference/
```

## AC-2: README.md Is Entry Point
```gherkin
Given docs/audit/README.md exists
When Claude reads it before writing documentation
Then it contains: file index table, authority chain (vision.md > audit/ > .paul/), dual smart reference guide (@-imports for Claude + [[wikilinks]] for Obsidian), Obsidian vault compatibility note, and a statement that this folder is AI-facing instructions
```

## AC-3: vision.md Cleaned
```gherkin
Given docs/vision.md currently has "Who It's For" section at lines 55-63 with blockquote at line 63
When the section is removed
Then vision.md flows directly from "---" (after Project Values) to "## The Polyglot Ecosystem" with no AI meta-instructions remaining
And the closing italic line ("This document is the authoritative source...") is preserved
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/audit/ directory tree</name>
  <files>docs/audit/, docs/audit/audiences/, docs/audit/rules/, docs/audit/reference/</files>
  <action>
    Create the nested directory structure:
    - docs/audit/
    - docs/audit/audiences/
    - docs/audit/rules/
    - docs/audit/reference/
    Use mkdir -p for each path.
  </action>
  <verify>ls -R docs/audit/ shows audiences/, rules/, reference/ subdirectories</verify>
  <done>AC-1 satisfied: directory tree exists</done>
</task>

<task type="auto">
  <name>Task 2: Write docs/audit/README.md</name>
  <files>docs/audit/README.md</files>
  <action>
    Create README.md as Claude's entry point to the audit system. Must include:

    1. Header declaring this folder is AI-facing instructions (not user docs)
    2. File index table mapping each file to what it tells Claude:
       - audiences/user.md → Writing for users
       - audiences/developer.md → Writing for developers
       - audiences/ai.md → Writing for AI agents
       - rules/conventions.md → How to write
       - rules/checklist.md → What to check before finalizing
       - reference/glossary.md → What terms mean
    3. Authority chain:
       - docs/vision.md — north star (product what/why)
       - docs/audit/ — writing how (this folder)
       - .paul/ — project management process
       - Rule: If a spec contradicts vision.md, vision.md wins
    4. Dual Smart Reference syntax guide:
       - @vision:&lt;section&gt; + [[vision#&lt;section&gt;]] — Claude import + Obsidian link
       - @glossary:&lt;term&gt; + [[glossary#&lt;term&gt;]] — Claude import + Obsidian link
       - @rule:&lt;id&gt; + [[conventions#&lt;id&gt;]] — Claude import + Obsidian link
       - @checklist:&lt;id&gt; + [[checklist#&lt;id&gt;]] — Claude import + Obsidian link
       - @-refs are mandatory imports (Claude MUST read before proceeding)
       - [[wikilinks]] are Obsidian navigation (clickable, graph view, backlinks)
    5. Obsidian compatibility note: docs/ is an Obsidian vault, all files use YAML frontmatter
    6. Scope note: audit rules apply to ALL documentation Claude writes, not just docs/
    7. Usage instruction: "Read this folder BEFORE writing or editing any documentation"

    Keep under 3KB. Use tables over prose. Token-optimized for AI consumption.
  </action>
  <verify>Read docs/audit/README.md and confirm it contains: file index table, authority chain, smart reference guide, AI-facing declaration</verify>
  <done>AC-2 satisfied: README.md is a complete entry point</done>
</task>

<task type="auto">
  <name>Task 3: Remove "Who It's For" section from vision.md</name>
  <files>docs/vision.md</files>
  <action>
    Remove lines 54-65 from docs/vision.md. Specifically:
    - Line 54: blank line before "## Who It's For"
    - Line 55: "## Who It's For"
    - Lines 56-61: audience descriptions (Users, Developers, AI)
    - Line 62: blank line
    - Line 63: blockquote about Audit document
    - Line 64: blank line
    - Line 65: "---" separator

    After removal, line 53 ("---" ending Project Values) should be followed by a blank line then "## The Polyglot Ecosystem" (currently line 67).

    DO NOT modify any other content. The closing italic line must remain.
  </action>
  <verify>
    Read docs/vision.md and confirm:
    1. No "Who It's For" heading exists
    2. No "audience" or "Audit document" text exists
    3. "## The Polyglot Ecosystem" follows the "---" after Project Values
    4. Final line is still: "*This document is the authoritative source..."
  </verify>
  <done>AC-3 satisfied: vision.md is pure product vision with no AI meta-instructions</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- docs/vision.md content outside lines 54-65 (all other sections must remain identical)
- .paul/ directory (state updates happen separately)
- Any existing docs/ files other than vision.md

## SCOPE LIMITS
- Do NOT create audience content files (Phase 7)
- Do NOT create rules/conventions.md, rules/checklist.md, or reference/glossary.md (Phase 8)
- Do NOT create CLAUDE.md at project root (Phase 8)
- Do NOT update STATE.md (done after plan completion)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `ls -R docs/audit/` shows: README.md, audiences/, rules/, reference/
- [ ] `docs/audit/README.md` is under 3KB and contains all required sections
- [ ] `docs/vision.md` has no "Who It's For" section
- [ ] `docs/vision.md` still ends with authoritative source italic line
- [ ] No other files in docs/ were modified
- All acceptance criteria met
</verification>

<success_criteria>
- All 3 tasks completed
- All verification checks pass
- docs/audit/ tree is ready for Phase 7 and Phase 8 content
- vision.md is clean product vision
</success_criteria>

<output>
After completion, create `.paul/phases/06-audit-structure-core/06-01-SUMMARY.md`
</output>
