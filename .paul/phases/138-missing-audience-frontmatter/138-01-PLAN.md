---
phase: 138-missing-audience-frontmatter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/audit/README.md
  - docs/audit/reference/glossary.md
  - docs/audit/rules/checklist.md
  - docs/audit/rules/conventions.md
  - docs/audit/rules/workflows.md
  - docs/audit/tracking/audience-migration.md
  - docs/audit/tracking/coverage-gaps.md
  - docs/audit/tracking/decisions.md
  - docs/audit/tracking/inconsistencies.md
  - docs/audit/tracking/progress.md
  - docs/technical/compile-rules/PGE/**/*.md
  - docs/technical/compile-rules/PGW/**/*.md
  - docs/technical/compile-rules/algorithms/*.md
  - docs/technical/compile-rules/TYPE-IDENTITY.md
  - docs/technical/spec/type-identity.md
  - docs/technical/brainstorming/*.md
  - docs/technical/plan/decisions/*.md
autonomous: true
---

<objective>
## Goal
Add the missing `audience` frontmatter field to all ~229 docs files that lack it, and add frontmatter to 12 files that have none.

## Purpose
Enforces the audit convention in docs/audit/rules/conventions.md (line 17): "frontmatter | YAML frontmatter required | audience, type, updated (minimum)". Resolves GitHub issue #138.

## Output
Modified markdown files with correct `audience` field in frontmatter.
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Source Files
@docs/audit/rules/conventions.md (the rule being enforced)
</context>

<acceptance_criteria>

## AC-1: All docs/audit/ files have audience: ai
```gherkin
Given all markdown files under docs/audit/
When checking YAML frontmatter
Then every file contains "audience: ai"
```

## AC-2: All docs/technical/ files have audience: developer
```gherkin
Given all markdown files under docs/technical/ that have frontmatter
When checking YAML frontmatter
Then every file contains "audience: developer"
```

## AC-3: Files without frontmatter get minimal frontmatter added
```gherkin
Given the 12 files that currently have no frontmatter
When frontmatter is added
Then each has at minimum audience, type, and updated fields
```

## AC-4: No docs/archive/ changes
```gherkin
Given docs/archive/ contains deprecated files
When the fix is applied
Then archive files are NOT modified (deprecated content frozen)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add audience field to docs/audit/ files</name>
  <files>9 files under docs/audit/</files>
  <action>
    For each of the 9 docs/audit/ files missing `audience`:
    1. Read the file's existing frontmatter
    2. Add `audience: ai` as the first field after the opening ---
    3. Do not modify any other frontmatter fields or file content

    Files: README.md, reference/glossary.md, rules/checklist.md, rules/conventions.md,
    rules/workflows.md, tracking/audience-migration.md, tracking/coverage-gaps.md,
    tracking/decisions.md, tracking/inconsistencies.md, tracking/progress.md
  </action>
  <verify>grep -rn "^audience:" docs/audit/ | wc -l should match total .md file count in docs/audit/</verify>
  <done>AC-1 satisfied: all audit files have audience: ai</done>
</task>

<task type="auto">
  <name>Task 2: Add audience field to docs/technical/ compile-rules files</name>
  <files>~202 files under docs/technical/compile-rules/</files>
  <action>
    For all PGE and PGW rule files under docs/technical/compile-rules/:
    1. Read the file's existing frontmatter
    2. Add `audience: developer` as the first field after the opening ---
    3. Also handle the 2 algorithm files and TYPE-IDENTITY.md

    These files have consistent frontmatter structure (rule, code, name, severity),
    so this can be done systematically.

    IMPORTANT: Do not modify any other frontmatter fields or file content.
  </action>
  <verify>grep -rn "^audience:" docs/technical/compile-rules/ | wc -l should match total .md file count</verify>
  <done>AC-2 satisfied for compile-rules directory</done>
</task>

<task type="auto">
  <name>Task 3: Add audience field to remaining docs/technical/ files + add missing frontmatter</name>
  <files>
    docs/technical/spec/type-identity.md,
    docs/technical/brainstorming/*.md (5 files needing frontmatter),
    docs/technical/compile-rules/TYPE-IDENTITY.md (needs frontmatter),
    docs/technical/plan/decisions/*.md (3 files needing frontmatter)
  </files>
  <action>
    Two sub-tasks:

    A) Files with frontmatter but missing audience (1 file):
       - docs/technical/spec/type-identity.md → add `audience: developer`

    B) Files completely missing frontmatter (12 files):
       Add minimal frontmatter block with audience, type, updated:
       ```yaml
       ---
       audience: developer
       type: [appropriate type — brainstorming, decision, reference, etc.]
       updated: [use git log date or 2026-04-05]
       ---
       ```
       Files:
       - docs/draft.md
       - docs/archive/wip/draft.md
       - docs/technical/brainstorming/marker-declarations.md
       - docs/technical/brainstorming/string-re-subfields.md
       - docs/technical/brainstorming/typed-flex-field-inference.md
       - docs/technical/brainstorming/serial-to-struct-matching.md
       - docs/technical/brainstorming/datetime-tree.md
       - docs/technical/compile-rules/TYPE-IDENTITY.md
       - docs/technical/plan/decisions/metadata-data-tree.md
       - docs/technical/plan/decisions/numeric-re-patterns.md
       - docs/technical/plan/decisions/schema-properties.md
       - docs/technical/plan/decisions/string-re-subfields.md

    IMPORTANT: Do not modify file content beyond adding/updating frontmatter.
    IMPORTANT: docs/draft.md → audience: developer; docs/archive/wip/draft.md → skip (archive).
  </action>
  <verify>Check all 12 files have frontmatter with audience field</verify>
  <done>AC-2 and AC-3 satisfied: all technical files have audience, missing frontmatter added</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- docs/user/ (already has audience fields — confirmed 0 missing)
- docs/archive/deprecated/ (frozen deprecated content — AC-4)
- File content beyond frontmatter
- Existing frontmatter field values (only ADD audience field)

## SCOPE LIMITS
- Only add `audience` field — do not fix other missing frontmatter fields (type, updated)
- Do not retag files that already have correct audience values
- Exception: the 12 files with no frontmatter get full minimal frontmatter (audience + type + updated)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] All docs/audit/ files have `audience: ai`
- [ ] All docs/technical/ files have `audience: developer`
- [ ] 12 previously frontmatter-less files now have frontmatter
- [ ] docs/archive/deprecated/ files NOT modified
- [ ] docs/user/ files NOT modified
- [ ] git diff shows only frontmatter changes (no content modifications)
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- Zero docs/audit/ or docs/technical/ files missing audience field
- All 12 no-frontmatter files now have frontmatter with audience
- No content regressions
- Archive files untouched
</success_criteria>

<output>
After completion, create `.paul/phases/138-missing-audience-frontmatter/138-01-SUMMARY.md`
</output>
