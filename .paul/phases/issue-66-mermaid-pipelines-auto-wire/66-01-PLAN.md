---
phase: issue-66-mermaid-pipelines-auto-wire
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/user/concepts/pipelines.md]
autonomous: true
---

<objective>
## Goal
Add a Mermaid `flowchart LR` diagram to pipelines.md that visualizes how chain auto-wire connects steps implicitly, showing type-matched ports between steps.

## Purpose
Issue #66 — A flowchart makes the auto-wire mechanism visually clear: how outputs connect to inputs when types match, and where explicit [=] wiring is still required (entry/exit IO).

## Output
Modified `docs/user/concepts/pipelines.md` with a Mermaid diagram inserted after the "### Auto-Wire" heading and before the existing prose.
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Source Files
@docs/user/concepts/pipelines.md (lines 307-325 — Auto-Wire section)
</context>

<acceptance_criteria>

## AC-1: Mermaid diagram present
```gherkin
Given the file docs/user/concepts/pipelines.md
When a reader opens the "### Auto-Wire" section
Then a Mermaid `flowchart LR` diagram appears after the heading and before the explanatory prose
```

## AC-2: Diagram shows auto-wire chain
```gherkin
Given the Mermaid diagram
When rendered
Then it shows three steps connected left to right
And Step 0 shows 1 output;string
And Step 1 shows 1 input;string and 1 output;string
And Step 2 shows 1 input;string
And arrows between steps are labeled "auto-wire"
```

## AC-3: Entry/exit IO distinction visible
```gherkin
Given the Mermaid diagram
When rendered
Then entry IO (Step 0 inputs) and exit IO (Step 2 outputs) are visually distinct
And it is clear these require explicit [=] wiring
```

## AC-4: No other content changes
```gherkin
Given the file docs/user/concepts/pipelines.md
When the diagram is added
Then no existing prose, code blocks, or headings are modified
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Insert Mermaid flowchart into Auto-Wire section</name>
  <files>docs/user/concepts/pipelines.md</files>
  <action>
    Insert a Mermaid `flowchart LR` diagram after line 307 ("### Auto-Wire") and before line 309 (the explanatory prose paragraph).

    The diagram should show:
    1. Three step nodes connected left to right:
       - Step 0: shows "1 output ;string"
       - Step 1: shows "1 input ;string" and "1 output ;string"
       - Step 2: shows "1 input ;string"
    2. Arrows between Step 0→Step 1 and Step 1→Step 2 labeled "auto-wire"
    3. Entry IO indicator on Step 0's input side showing "[=] explicit"
    4. Exit IO indicator on Step 2's output side showing "[=] explicit"
    5. Use node shapes or styling to distinguish auto-wired connections from explicit wiring

    Follow the Mermaid style established in other docs (fenced code block, clean labels).
    Keep node labels concise. Use step names that reference the code example below (File.Text.Read, Text.Transform, Text.Format).
  </action>
  <verify>
    - grep for "```mermaid" in pipelines.md confirms diagram present in Auto-Wire section
    - grep for "flowchart LR" confirms correct diagram type
    - "### Error Handling in Chains" still present and unchanged after the section
    - No other sections modified
  </verify>
  <done>AC-1, AC-2, AC-3, AC-4 satisfied: Mermaid flowchart shows auto-wire chain with entry/exit IO distinction, no other changes</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- All content before "### Auto-Wire" heading
- Existing prose in Auto-Wire section (lines 309-325)
- "### Error Handling in Chains" section and everything after
- Frontmatter

## SCOPE LIMITS
- Only add Mermaid diagram between heading and prose
- Do not modify any other files

</boundaries>

<verification>
Before declaring plan complete:
- [ ] grep for "mermaid" in pipelines.md finds the diagram in Auto-Wire section
- [ ] grep for "flowchart LR" matches
- [ ] Existing Auto-Wire prose unchanged
- [ ] No other files modified
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- Mermaid `flowchart LR` diagram inserted after "### Auto-Wire" heading
- Diagram shows 3-step chain with auto-wire arrows and type annotations
- Entry/exit IO marked as requiring explicit [=] wiring
- No existing content modified
</success_criteria>

<output>
After completion, create `.paul/phases/issue-66-mermaid-pipelines-auto-wire/66-01-SUMMARY.md`
</output>
