---
phase: 136-h4-heading-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/technical/compile-rules/PGE/PGE06002-enum-exhaustiveness.md
  - docs/technical/compile-rules/PGE/PGE04015-conditional-type-operator-mismatch.md
  - docs/technical/compile-rules/PGE/PGE12001-undefined-metadata-field-access.md
  - docs/technical/compile-rules/PGE/PGE02005-failed-is-terminal.md
  - docs/technical/compile-rules/PGE/PGE07002-chain-error-scoping.md
  - docs/technical/compile-rules/PGE/PGE06009-conditional-missing-comparison-operator.md
  - docs/technical/compile-rules/PGE/PGE06001-conditional-must-be-exhaustive.md
  - docs/technical/compile-rules/PGE/PGE12003-invalid-inline-pipeline-argument.md
autonomous: true
---

<objective>
## Goal
Eliminate all H4+ (`####`, `#####`) headings from 8 PGE compile-rule files by promoting heading levels, bringing them into compliance with the audit convention "No H4+".

## Purpose
Resolves GitHub issue #136. The `docs/audit/rules/conventions.md` heading-hierarchy rule bans H4+ headings. Eight files in `docs/technical/compile-rules/PGE/` violate this rule with H4 and H5 headings.

## Output
8 modified PGE files with heading hierarchy: H1 (rule title), H2 (sections), H3 (subsections). No H4 or H5 headings remain.
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Source Files
@docs/audit/rules/conventions.md (the rule being enforced)
@docs/technical/compile-rules/PGE/PGE06002-enum-exhaustiveness.md
@docs/technical/compile-rules/PGE/PGE04015-conditional-type-operator-mismatch.md
@docs/technical/compile-rules/PGE/PGE12001-undefined-metadata-field-access.md
@docs/technical/compile-rules/PGE/PGE02005-failed-is-terminal.md
@docs/technical/compile-rules/PGE/PGE07002-chain-error-scoping.md
@docs/technical/compile-rules/PGE/PGE06009-conditional-missing-comparison-operator.md
@docs/technical/compile-rules/PGE/PGE06001-conditional-must-be-exhaustive.md
@docs/technical/compile-rules/PGE/PGE12003-invalid-inline-pipeline-argument.md
</context>

<skills>
No specialized flows configured.
</skills>

<acceptance_criteria>

## AC-1: No H4+ headings in target files
```gherkin
Given the 8 PGE files listed in files_modified
When searching for lines matching ^####
Then zero matches are found across all 8 files
```

## AC-2: Heading hierarchy follows audit convention
```gherkin
Given each of the 8 modified PGE files
When inspecting heading levels
Then each file has exactly one H1 (rule title), H2 for sections, H3 for subsections, and no H4+
```

## AC-3: Content preserved
```gherkin
Given the 8 PGE files before and after modification
When comparing non-heading content (code blocks, tables, prose, frontmatter)
Then all non-heading content is identical
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Promote headings in 6 H4-only files</name>
  <files>
    docs/technical/compile-rules/PGE/PGE06002-enum-exhaustiveness.md,
    docs/technical/compile-rules/PGE/PGE02005-failed-is-terminal.md,
    docs/technical/compile-rules/PGE/PGE07002-chain-error-scoping.md,
    docs/technical/compile-rules/PGE/PGE06009-conditional-missing-comparison-operator.md,
    docs/technical/compile-rules/PGE/PGE06001-conditional-must-be-exhaustive.md,
    docs/technical/compile-rules/PGE/PGE12003-invalid-inline-pipeline-argument.md
  </files>
  <action>
    In each file, promote all headings up by 2 levels:
    - `### Rule X.Y — Name` → `# Rule X.Y — Name` (H3 → H1)
    - `#### Section` → `## Section` (H4 → H2)
    - `### See Also` → `## See Also` (H3 → H2)

    Do NOT change frontmatter, code blocks, tables, or prose.
    Do NOT change any heading text — only the number of `#` characters.
  </action>
  <verify>grep -c '####' on each file returns 0; grep -c '^# ' returns exactly 1 per file</verify>
  <done>AC-1 and AC-2 satisfied for these 6 files</done>
</task>

<task type="auto">
  <name>Task 2: Promote headings in 2 H4+H5 files</name>
  <files>
    docs/technical/compile-rules/PGE/PGE04015-conditional-type-operator-mismatch.md,
    docs/technical/compile-rules/PGE/PGE12001-undefined-metadata-field-access.md
  </files>
  <action>
    In each file, promote all headings up by 2 levels:
    - `### Rule X.Y — Name` → `# Rule X.Y — Name` (H3 → H1)
    - `#### Section` → `## Section` (H4 → H2)
    - `##### Subsection` → `### Subsection` (H5 → H3)
    - `### See Also` → `## See Also` (H3 → H2)

    Do NOT change frontmatter, code blocks, tables, or prose.
    Do NOT change any heading text — only the number of `#` characters.
  </action>
  <verify>grep -c '####' on each file returns 0; grep -c '#####' returns 0; grep -c '^# ' returns exactly 1 per file</verify>
  <done>AC-1 and AC-2 satisfied for these 2 files; AC-3 verified by diff showing only heading-level changes</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- docs/audit/rules/conventions.md (the rule stays as-is)
- docs/archive/ (deprecated files are out of scope)
- All other PGE files not listed in files_modified (H3-as-top-level is a separate issue)
- Frontmatter in any file
- Code blocks, tables, or prose content

## SCOPE LIMITS
- Only fix H4+ violations — do not restructure or rewrite content
- Do not split files into multiple files
- Do not change heading text, only heading levels
- Do not fix H3-as-top-level in non-violating PGE files (out of scope for #136)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `grep -r '####' docs/technical/compile-rules/PGE/PGE06002-enum-exhaustiveness.md docs/technical/compile-rules/PGE/PGE04015-conditional-type-operator-mismatch.md docs/technical/compile-rules/PGE/PGE12001-undefined-metadata-field-access.md docs/technical/compile-rules/PGE/PGE02005-failed-is-terminal.md docs/technical/compile-rules/PGE/PGE07002-chain-error-scoping.md docs/technical/compile-rules/PGE/PGE06009-conditional-missing-comparison-operator.md docs/technical/compile-rules/PGE/PGE06001-conditional-must-be-exhaustive.md docs/technical/compile-rules/PGE/PGE12003-invalid-inline-pipeline-argument.md` returns no matches
- [ ] Each file has exactly one H1 heading (the rule title)
- [ ] No content other than heading markers was changed
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All 8 files have heading hierarchy H1/H2/H3 only
- Zero H4+ headings remain in any target file
- All non-heading content preserved exactly
</success_criteria>

<output>
After completion, create `.paul/phases/136-h4-heading-restructure/136-01-SUMMARY.md`
</output>
