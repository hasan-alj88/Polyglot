---
phase: 132-error-branch-separator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/technical/spec/metadata-tree/path-grammar.md
autonomous: true
---

<objective>
## Goal
Disambiguate the general element rules table in path-grammar.md so that `ref` and `instance` are explicitly scoped to `instance_path` prefixes only, eliminating the contradiction with `%!` error branch documentation.

## Purpose
Issue #132 — the general element rules table implies `ref` (flexible `:`) applies to all branches, but `%!` uses fixed `.` for namespaces. The exception branch rules section (lines 34-37) and object-types.md are already correct. Only the table needs scoping.

## Output
Updated `path-grammar.md` with scoped element rules table.
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Source Files
@docs/technical/spec/metadata-tree/path-grammar.md
@docs/technical/spec/metadata-tree/object-types.md
</context>

<acceptance_criteria>

## AC-1: ref and instance scoped to instance_path
```gherkin
Given the general element rules table in path-grammar.md
When a reader looks at the `ref` and `instance` rows
Then both rows explicitly state they apply to `instance_path` prefixes only
```

## AC-2: No contradiction with exception branches
```gherkin
Given the updated element rules table
When compared with the exception branch rules section (lines 34-37)
Then there is no contradiction — the table no longer implies `ref` applies to %!, %_, or %@
```

## AC-3: object-types.md unchanged
```gherkin
Given object-types.md already correctly documents %! as Fixed (`.namespace`)
When this plan completes
Then object-types.md has zero modifications
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Scope ref and instance rows in element rules table</name>
  <files>docs/technical/spec/metadata-tree/path-grammar.md</files>
  <action>
    In the general element rules table (lines 26-33):
    
    1. Change the `ref` row (line 30) from:
       `| ref | Object name — flexible field (:). |`
       to:
       `| ref | Object name — flexible field (:). Applies to instance_path prefixes only. |`
    
    2. Change the `instance` row (line 31) from:
       `| instance | Instance number — flexible field (:) |`
       to:
       `| instance | Instance number — flexible field (:). Applies to instance_path prefixes only. |`
    
    Do NOT change: the `type_prefix` rows (28-29), the `field` row (32), the exception branch rules section (34-37), or any other file.
  </action>
  <verify>
    Read path-grammar.md and confirm:
    - `ref` row contains "Applies to `instance_path` prefixes only"
    - `instance` row contains "Applies to `instance_path` prefixes only"
    - Exception branch rules section (lines 34-37) unchanged
    - No other files modified
  </verify>
  <done>AC-1 and AC-2 satisfied: ref/instance scoped, no contradiction with exception branches</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- docs/technical/spec/metadata-tree/object-types.md (already correct)
- docs/user/concepts/data-is-trees.md (already correct)
- Exception branch rules section in path-grammar.md (lines 34-37)
- EBNF grammar block in path-grammar.md (lines 13-24)

## SCOPE LIMITS
- Table wording fix only — no restructuring of the table
- No new sections, no new files
- Do not touch any other metadata-tree files

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `ref` row says "Applies to `instance_path` prefixes only"
- [ ] `instance` row says "Applies to `instance_path` prefixes only"
- [ ] Exception branch rules unchanged
- [ ] object-types.md unchanged
- [ ] Only path-grammar.md modified
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- Single file modified (path-grammar.md)
- Two table cells updated with scoping clarification
- Zero contradictions between general table and exception rules
</success_criteria>

<output>
After completion, create `.paul/phases/132-error-branch-separator/132-01-SUMMARY.md`
</output>
