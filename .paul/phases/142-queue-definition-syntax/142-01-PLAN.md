---
phase: 142-queue-definition-syntax
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/INDEX.md
autonomous: true
---

<objective>
## Goal
Add Queue as a first-class object in the Master Index "By Polyglot Object" table, resolving the discoverability gap flagged by issue #142.

## Purpose
The `{Q}` block is a dual-purpose definition (queue instance via `#Queue:Name`, queue pipeline via `=Q.*`) documented in blocks.md and queue.md, but invisible from the Master Index. Users navigating from INDEX.md cannot discover queue definitions exist.

## Output
- Updated `docs/INDEX.md` with Queue row in "By Polyglot Object" table
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Source Files
@docs/INDEX.md — Master Index, missing Queue row (line 30-41)
@docs/user/syntax/blocks.md — {Q} dual-purpose documented (line 26)
@docs/user/concepts/pipelines/queue.md — full queue documentation
@docs/user/pglib/pipelines/Q.md — pglib =Q.* pipeline catalog
@docs/technical/ebnf/09-definition-blocks.md — EBNF §9.5 queue_def
@docs/technical/compile-rules/PGE/PGE01012-queue-definition-prefix.md — #Queue: prefix rule
</context>

<acceptance_criteria>

## AC-1: Queue row in Master Index
```gherkin
Given the "By Polyglot Object" table in docs/INDEX.md
When a user looks for Queue definitions
Then a Queue row exists with prefix `{Q}`, linking to user concept, pglib, EBNF, and edge cases
```

## AC-2: Links are valid
```gherkin
Given the new Queue row in INDEX.md
When each wikilink in the row is followed
Then each target file exists and contains queue-related content
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add Queue row to INDEX.md object table</name>
  <files>docs/INDEX.md</files>
  <action>
    Add a Queue row to the "By Polyglot Object" table (between Data/Type and Metadata rows) with:
    - Object: `Queue`
    - Prefix: `{Q}`
    - User Concept: `[[user/concepts/pipelines/queue]]`
    - pglib Reference: `[[user/pglib/pipelines/Q]]`
    - EBNF Grammar: `[[technical/ebnf/09-definition-blocks]]`
    - Edge Cases: `[[technical/edge-cases/09-definition-blocks]]`

    Also add rows for Wrapper, Trigger, and Permission if missing — these are also first-class definition blocks (`{W}`, `{T}`, `{_}`) absent from the table. Check before adding.
  </action>
  <verify>
    Read docs/INDEX.md and confirm:
    1. Queue row present with all 6 columns populated
    2. All wikilink targets exist as files
    3. Table formatting is valid markdown
  </verify>
  <done>AC-1 and AC-2 satisfied: Queue row exists with valid cross-references</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- docs/user/syntax/blocks.md (already correct)
- docs/user/concepts/pipelines/queue.md (already correct)
- docs/technical/compile-rules/PGE/PGE01012-queue-definition-prefix.md (already correct)
- docs/technical/ebnf/09-definition-blocks.md (already correct)

## SCOPE LIMITS
- Do NOT redesign queue definition syntax — it is already well-documented
- Do NOT add code examples — the issue's recommendation #4 is already fulfilled by queue.md
- Only touch INDEX.md

</boundaries>

<verification>
Before declaring plan complete:
- [ ] Queue row present in INDEX.md "By Polyglot Object" table
- [ ] All wikilinks in new row resolve to existing files
- [ ] Table renders correctly in markdown
- [ ] No other files modified
</verification>

<success_criteria>
- Queue is discoverable from the Master Index
- All cross-reference links are valid
- Issue #142 acceptance criteria fully addressed
</success_criteria>

<output>
After completion, create `.paul/phases/142-queue-definition-syntax/142-01-SUMMARY.md`
</output>
