---
phase: issue-84-add-permission-error-tree
plan: 01
subsystem: jm3lib
tags: [permissions, errors, error-tree, runtime]

requires:
  - phase: issue-80
    provides: _ permission prefix and [_] block marker
  - phase: issue-82
    provides: %_ metadata tree branch for permissions
provides:
  - "!Permission error namespace with 8 .Denied leaves"
  - "Pipeline error associations for File IO pipelines"
affects: []

tech-stack:
  added: []
  patterns: []

key-files:
  modified:
    - docs/user/jm3lib/errors/errors.md
    - docs/user/concepts/errors.md

key-decisions:
  - "Trimmed tree: only .Denied leaves per category; overlapping .NotFound/.Timeout stay in existing namespaces"
  - "Explicit pipeline associations: File IO pipelines declare [=] !Permission.File.Denied"
  - "Alias deferred to issue #87 (multi-alias support needed)"
---

# Issue #84 Plan 01: Add !Permission.* Error Tree

## Objective

Add `!Permission` error namespace for runtime system denials — when Polyglot grants permission via `[_]` but the OS/system denies access.

## Acceptance Criteria

| ID | Criterion | Done When |
|----|-----------|-----------|
| AC-1 | `{!} !Permission` block in jm3lib errors | 8 `.Denied` leaves matching 8 permission categories |
| AC-2 | Pipeline error associations updated | File IO pipelines declare `[=] !Permission.File.Denied` |
| AC-3 | Concepts errors updated | Namespace count, inline list, and table all include `!Permission` |
| AC-4 | Pre-existing bugs fixed | Missing `!Math` in table and `!Validation` in inline list |

## Tasks

### Task 1: Update jm3lib errors (`docs/user/jm3lib/errors/errors.md`)
- Add `{!} !Permission` block with 8 `.Denied` leaves after `{!} !Validation`
- Add `[=] !Permission.File.Denied` to `=File.Text.Read`, `=File.Text.Write`, `=File.Text.Append`
- Update frontmatter date

### Task 2: Update concepts errors (`docs/user/concepts/errors.md`)
- Fix inline list (add `!Validation` + `!Permission`)
- Fix count (five → six)
- Fix table (add `!Math` + `!Permission`)
- Update frontmatter date

### Task 3: Verify
- Grep for `!Permission` references
- Confirm 8 leaves match 8 permission categories
- Confirm pipeline associations consistent
