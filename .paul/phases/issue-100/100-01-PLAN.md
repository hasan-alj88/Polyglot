---
phase: issue-100
plan: 01
type: execute
autonomous: true
---

<objective>
Implement EBNF edge cases X.12--X.14 from issue #100: trigger and queue anomalies.

Decisions:
- X.12: Non-trigger pipeline as trigger → PGE — operations declare allowed markers; compiler validates [t] only accepts trigger-compatible ops. New PGE01024.
- X.13: Multiple triggers → Accept with AND semantics (all must fire; [|] for OR). Add edge case documenting this.
- X.14: Duplicate [Q]/[W] → Accept — grammar already enforces single occurrence. Add edge case confirming.
</objective>

<context>
@docs/technical/ebnf/09-definition-blocks.md — pipeline_body, trigger_io_section
@docs/technical/COMPILE-RULES.md — master index
@docs/technical/edge-cases/09-definition-blocks.md — S9 edge cases
@docs/technical/edge-cases/INDEX.md — edge case index
</context>

<tasks>
<task type="auto">T1: Create PGE01024 — Incompatible Operation Marker
- Operation used with wrong block marker (e.g., non-=T.* as [t])
- Each operation type declares compatible markers
</task>

<task type="auto">T2: Add edge cases EC-9.16 (non-trigger as trigger), EC-9.17 (multi-trigger AND), EC-9.18 (duplicate Q/W)
</task>

<task type="auto">T3: Update COMPILE-RULES.md and edge-cases INDEX.md
</task>

<task type="auto">T4: Update trigger_io_section docs to note AND semantics and [|] for OR
</task>
</tasks>
