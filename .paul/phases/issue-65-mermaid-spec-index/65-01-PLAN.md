---
phase: issue-65-mermaid-spec-index
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/user/SPEC-INDEX.md]
autonomous: true
---

<objective>
## Goal
Add a Mermaid `flowchart LR` diagram to SPEC-INDEX.md that visualizes the 5-phase learning progression from syntax foundations through practice.

## Purpose
Issue #65 — A left-to-right flowchart makes the learning path immediately scannable, showing how the 5 phases build on each other with file counts per phase.

## Output
Modified `docs/user/SPEC-INDEX.md` with a Mermaid diagram inserted after the introductory paragraph and before "## Phase 1: Syntax Foundations".
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Source Files
@docs/user/SPEC-INDEX.md
</context>

<acceptance_criteria>

## AC-1: Mermaid diagram present
```gherkin
Given the file docs/user/SPEC-INDEX.md
When a reader opens the file
Then a Mermaid `flowchart LR` diagram appears after the introductory paragraph and before "## Phase 1: Syntax Foundations"
```

## AC-2: Diagram shows 5-phase progression
```gherkin
Given the Mermaid diagram
When rendered
Then it shows 5 nodes: Syntax Foundations (7 files), Core Concepts (5 files), The Big Picture (2 files), Packages & jm3lib (2 files), Practice
And arrows connect them left to right in reading order
```

## AC-3: No other content changes
```gherkin
Given the file docs/user/SPEC-INDEX.md
When the diagram is added
Then no existing prose, tables, headings, or frontmatter are modified
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Insert Mermaid flowchart into SPEC-INDEX.md</name>
  <files>docs/user/SPEC-INDEX.md</files>
  <action>
    Insert a Mermaid `flowchart LR` diagram after line 9 (the introductory paragraph) and before line 11 ("## Phase 1: Syntax Foundations").

    The diagram should show:
    1. Five nodes, one per phase, with phase name and file count
    2. Left-to-right arrows connecting them in order
    3. Node labels matching the exact phase headings from the file:
       - "Syntax Foundations" (7 files)
       - "Core Concepts" (5 files)
       - "The Big Picture" (2 files)
       - "Packages & jm3lib" (2 files)
       - "Practice"
    4. Use clean, concise labels with file counts in parentheses

    Follow the Mermaid style established in other docs (fenced code block).
    Do not add a heading or introductory sentence — the diagram speaks for itself between the intro and Phase 1.
  </action>
  <verify>
    - grep for "```mermaid" in SPEC-INDEX.md confirms diagram present
    - grep for "flowchart LR" confirms correct diagram type
    - "## Phase 1: Syntax Foundations" still present after the diagram
    - Frontmatter and all phase tables unchanged
  </verify>
  <done>AC-1, AC-2, AC-3 satisfied: Mermaid flowchart present in correct location, shows 5-phase progression, no other changes</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Frontmatter (lines 1-5)
- All phase tables (## Phase 1 through ## Phase 5)
- "## For Contributors" section
- "## Adding New Spec Files" section

## SCOPE LIMITS
- Only add Mermaid diagram between intro paragraph and Phase 1
- Do not add diagrams to other sections
- Do not modify any other files

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `grep -c "mermaid" docs/user/SPEC-INDEX.md` returns 1
- [ ] `grep "flowchart LR" docs/user/SPEC-INDEX.md` matches
- [ ] "## Phase 1: Syntax Foundations" still present and unchanged
- [ ] No other files modified
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- Mermaid `flowchart LR` diagram inserted in correct location
- Diagram shows 5-phase learning path with file counts
- Left-to-right arrows show reading order
- No existing content modified
</success_criteria>

<output>
After completion, create `.paul/phases/issue-65-mermaid-spec-index/65-01-SUMMARY.md`
</output>
