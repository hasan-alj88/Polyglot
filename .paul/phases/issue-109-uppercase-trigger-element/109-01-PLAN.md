---
phase: issue-109-uppercase-trigger-element
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "93 files across docs/ (excluding docs/archive/)"
autonomous: true
---

<objective>
## Goal
Rename `[t]` → `[T]` across all active documentation files for consistency with `[W]` and `[Q]` uppercase block element convention.

## Purpose
Issue #109 — the trigger block element should be uppercase `[T]`, matching `[W]`, `[Q]`, and `[M]`. Currently lowercase throughout all docs.

## Output
93 files updated with `[t]` → `[T]` replacement. Archive files untouched.
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Source Files
All 93 active files containing `[t]` (see research from work-issue phase).
Brainstorming file already uses `[T]` — confirms target convention.
</context>

<acceptance_criteria>

## AC-1: All active docs use [T] not [t]
```gherkin
Given the docs/ directory (excluding docs/archive/)
When grepping for `\[t\]` in trigger contexts
Then zero occurrences remain
```

## AC-2: [T] occurrences are correct
```gherkin
Given the docs/ directory (excluding docs/archive/)
When grepping for `\[T\]`
Then all occurrences are valid trigger element references
```

## AC-3: Archive files untouched
```gherkin
Given the docs/archive/ directory
When checking git diff
Then no archive files are modified
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Bulk replace [t] → [T] in all active docs via sed</name>
  <files>93 files across docs/ (excluding docs/archive/)</files>
  <action>
    Use `find` + `sed` to replace `[t]` with `[T]` in all files under docs/ excluding docs/archive/:

    ```bash
    find docs/ -path docs/archive -prune -o -name '*.md' -print | xargs sed -i 's/\[t\]/[T]/g'
    ```

    Safety: In Polyglot docs, `[t]` is ALWAYS the trigger block element — there are no unrelated uses. The brainstorming file (marker-declarations.md) already uses `[T]` and has 1 remaining `[t]` reference that should also change.

    Do NOT touch docs/archive/ files (migrate-on-touch policy).
  </action>
  <verify>
    1. `grep -r '\[t\]' docs/ --include='*.md' --exclude-dir=archive | wc -l` = 0
    2. `grep -r '\[T\]' docs/ --include='*.md' --exclude-dir=archive | wc -l` > 300 (confirms replacements landed)
    3. `git diff --name-only docs/archive/` shows no archive files changed
  </verify>
  <done>AC-1, AC-2, AC-3 satisfied: all active docs use [T], archive untouched</done>
</task>

<task type="auto">
  <name>Task 2: Verify EBNF grammar productions updated correctly</name>
  <files>docs/technical/ebnf/09-definition-blocks.md, docs/technical/ebnf/05-block-elements.md</files>
  <action>
    Read the two EBNF files and confirm:
    1. `trigger_section` production uses `"[T]"` not `"[t]"`
    2. `05-block-elements.md` element list uses `[T]`
    3. All rule text references updated

    These are critical files — a wrong replacement would break the formal grammar.
  </action>
  <verify>Read both files and confirm [T] in grammar productions and rule text</verify>
  <done>AC-1 and AC-2 satisfied for EBNF files specifically</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- docs/archive/ (deprecated files, migrate-on-touch only)
- Any non-docs files
- .paul/ directory content (except STATE.md updates)

## SCOPE LIMITS
- Only `[t]` → `[T]` replacement — no other renames
- No compiler rule number changes
- No content rewrites — mechanical replacement only

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `grep -r '\[t\]' docs/ --include='*.md' --exclude-dir=archive` returns zero results
- [ ] `grep -r '\[T\]' docs/ --include='*.md' --exclude-dir=archive` returns 300+ results
- [ ] `git diff --name-only docs/archive/` shows nothing
- [ ] EBNF grammar productions use `[T]`
- [ ] No files outside docs/ modified
</verification>

<success_criteria>
- All active docs use [T] for trigger element
- Archive untouched
- EBNF grammar correct
- Zero `[t]` trigger references remain in active docs
</success_criteria>

<output>
After completion, create `.paul/phases/issue-109-uppercase-trigger-element/109-01-SUMMARY.md`
</output>
