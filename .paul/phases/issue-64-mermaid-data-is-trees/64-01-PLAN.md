---
phase: issue-64-mermaid-data-is-trees
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/user/concepts/data-is-trees.md]
autonomous: true
---

<objective>
## Goal
Add a Mermaid `graph TD` tree diagram to data-is-trees.md that visualizes the schema vs instance relationship — showing how `%definition` schema nodes connect to runtime instances across key tree branches.

## Purpose
Issue #64 — Mermaid diagrams make the tree structure visually navigable. The "Schema vs Instance" section currently uses only code blocks; a diagram makes the two-layer architecture immediately clear.

## Output
Modified `docs/user/concepts/data-is-trees.md` with a Mermaid diagram inserted after "## Schema vs Instance" prose and before "### Worked Examples".
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Source Files
@docs/user/concepts/data-is-trees.md
@docs/user/concepts/variable-lifecycle.md (Mermaid pattern reference — issue #60)
</context>

<acceptance_criteria>

## AC-1: Mermaid diagram present
```gherkin
Given the file docs/user/concepts/data-is-trees.md
When a reader opens the "## Schema vs Instance" section
Then a Mermaid `graph TD` diagram appears between the section prose and "### Worked Examples"
```

## AC-2: Diagram shows schema-to-instance relationship
```gherkin
Given the Mermaid diagram
When rendered
Then it shows %definition as the schema layer with branches for key types (#, =, $)
And shows instance nodes (:0, :1) descending from each type
And shows the one-to-many relationship (one definition → many instances)
```

## AC-3: Diagram uses key branches from the document
```gherkin
Given the Mermaid diagram
When rendered
Then it includes #Boolean (enum example), =ProcessData (pipeline example), and $myVar (variable example)
And these match the worked examples already in the document
```

## AC-4: No other content changes
```gherkin
Given the file docs/user/concepts/data-is-trees.md
When the diagram is added
Then no existing prose, code blocks, tables, or frontmatter are modified
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Insert Mermaid tree diagram into Schema vs Instance section</name>
  <files>docs/user/concepts/data-is-trees.md</files>
  <action>
    Insert a Mermaid `graph TD` diagram after the existing code block in "## Schema vs Instance" (after line 69, before "### Worked Examples" at line 71).

    The diagram should show:
    1. Root node: `%` (tree root)
    2. Schema layer: `%definition` with branches `.#`, `.=`, `.$` for key types
    3. Instance layer: instances descending from each type branch
       - `%#:Boolean:0` and `%#:Boolean:1` from `%definition.#:Boolean`
       - `%=:ProcessData:0` from `%definition.=:ProcessData`
       - `%$:myVar:0` from `%definition.$:myVar`
    4. Visual distinction between schema and instance layers (use subgraphs or styling)
    5. Arrows showing "one definition → many instances" relationship

    Use `graph TD` (top-down) as specified in the issue.
    Follow the Mermaid style established in variable-lifecycle.md (fenced code block, clean labels).
    Keep node labels concise — use the path notation from the document.
    Add a brief introductory sentence before the diagram explaining what it shows.
  </action>
  <verify>
    - grep for "```mermaid" in data-is-trees.md confirms diagram present
    - grep for "graph TD" confirms correct diagram type
    - "### Worked Examples" still present and unchanged after the diagram
    - Frontmatter and all other sections unchanged
  </verify>
  <done>AC-1, AC-2, AC-3, AC-4 satisfied: Mermaid diagram present between prose and worked examples, shows schema→instance for #Boolean, =ProcessData, $myVar, no other changes</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Frontmatter (lines 1-5)
- "## All Data is Serialized Strings" section
- "## The Structured Tree" section
- "## How Concepts Connect" table
- "### Worked Examples" content (table)
- "## Key Tree Rules" section and all subsections
- "## Reading the Tree" section

## SCOPE LIMITS
- Only add Mermaid diagram and brief intro sentence
- Do not restructure existing prose in "## Schema vs Instance"
- Do not add diagrams to other sections
- Do not modify any other files

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `grep -c "mermaid" docs/user/concepts/data-is-trees.md` returns 1
- [ ] `grep "graph TD" docs/user/concepts/data-is-trees.md` matches
- [ ] "### Worked Examples" section unchanged
- [ ] No other files modified
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- Mermaid `graph TD` diagram inserted in correct location
- Diagram visualizes schema vs instance with #Boolean, =ProcessData, $myVar
- One-to-many relationship (definition → instances) is clear
- No existing content modified
</success_criteria>

<output>
After completion, create `.paul/phases/issue-64-mermaid-data-is-trees/64-01-SUMMARY.md`
</output>
