---
phase: issue-118-string-depth-max
plan: 02
type: execute
wave: 1
depends_on: ["118-01"]
files_modified:
  # aj3lib type files — ###Enum → ###ScalarEnum for scalar types
  - docs/user/aj3lib/types/string.md
  - docs/user/aj3lib/types/boolean.md
  - docs/user/aj3lib/types/scalars.md
  - docs/user/aj3lib/types/enums.md
  - docs/user/aj3lib/types/BaseCode.md
  - docs/user/aj3lib/types/structs.md
  - docs/user/aj3lib/types/collections.md
  - docs/user/aj3lib/types/types.md
  # DateTime types
  - docs/user/aj3lib/types/datetime/main-type.md
  - docs/user/aj3lib/types/datetime/core-components.md
  - docs/user/aj3lib/types/datetime/calendar-date-types.md
  - docs/user/aj3lib/types/datetime/calendar-infrastructure.md
  - docs/user/aj3lib/types/datetime/cultural-types.md
  - docs/user/aj3lib/types/datetime/non-standard-time.md
  - docs/user/aj3lib/types/datetime/supporting-enums.md
  # Syntax reference files
  - docs/user/syntax/types/hierarchy.md
  - docs/user/syntax/types/prefix-system.md
  - docs/user/syntax/types/macro-types.md
  - docs/user/syntax/types/INDEX.md
  # Technical files
  - docs/technical/spec/metadata-tree/definition-templates.md
  - docs/technical/COMPILE-RULES.md
  - docs/technical/ebnf/04-type-system.md
  - docs/technical/ebnf/03-identifiers.md
  - docs/technical/edge-cases/24-datatype-defs.md
  # Concept files
  - docs/user/concepts/data-is-trees.md
  - docs/user/concepts/collections/dataframe.md
  - docs/user/aj3lib/pipelines/#.md
autonomous: true
---

<objective>
## Goal
Propagate the ##Scalar/##Leaf/###ScalarValue/###ScalarEnum changes from plan 118-01 across all remaining documentation files.

## Purpose
Plan 118-01 defined the new schemas and leaf types in 4 core reference files. This plan updates every other file that references the old model (Depth.Max=0 for scalars, ###Enum/###Value for scalar types).

## Output
All documentation consistent with the new depth model. No stale references to Depth.Max=0 for scalars or ###Enum/###Value in scalar type contexts.
</objective>

<context>
## Prior Work
@.paul/phases/issue-118-string-depth-max/118-01-SUMMARY.md

## Source Files
All files listed in files_modified above.

## Key Changes to Propagate
1. `###Enum` → `###ScalarEnum` in types that compose `##Scalar` (scalar enums: #Boolean, #BaseCode, all datetime enums, all aj3lib enums)
2. `###Value` → `###ScalarValue` in types that compose `##Scalar` (scalar values: #String family, datetime scalar values)
3. `Depth.Max = 0` → `Depth.Max = 1` in explanatory text about scalars (NOT in ##Leaf references)
4. "no flexible children" → remove or reword for scalar context
5. Add `##Leaf` to any type hierarchy listings
6. Keep `###Value`/`###Enum` unchanged for NON-scalar types (#DateTime main type, #Array, #Map, etc.)
</context>

<skills>
No specialized flows configured.
</skills>

<acceptance_criteria>

## AC-1: All scalar enum types use ###ScalarEnum
```gherkin
Given all {#} definitions that compose ##Scalar and have untyped enum fields
When grep finds [#] << ###Enum in those files
Then zero matches — all replaced with [#] << ###ScalarEnum
```

## AC-2: All scalar value types use ###ScalarValue
```gherkin
Given all {#} definitions that compose ##Scalar and have typed value fields
When grep finds [#] << ###Value in those files
Then zero matches — all replaced with [#] << ###ScalarValue
```

## AC-3: Non-scalar types retain ###Value/###Enum
```gherkin
Given #DateTime (main-type.md) and collection types
When they use ###Value or ###Enum
Then those references are UNCHANGED (they are not ##Scalar types)
```

## AC-4: Type hierarchy and prefix docs updated
```gherkin
Given hierarchy.md, types.md, prefix-system.md
When they list ## schemas or ### field types
Then ##Leaf appears, and ###ScalarValue/###ScalarEnum are listed
```

## AC-5: Technical docs consistent
```gherkin
Given COMPILE-RULES.md, EBNF, and edge-cases
When they reference Depth.Max for scalars
Then they show 1 (not 0) unless specifically discussing ##Leaf
```

## AC-6: No stale "Depth.Max=0" for scalars in any modified file
```gherkin
Given all modified files
When grep finds "Depth.Max.*0" 
Then it only appears in ##Leaf context, never describing ##Scalar
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Update aj3lib scalar enum types — ###Enum → ###ScalarEnum</name>
  <files>
    docs/user/aj3lib/types/boolean.md,
    docs/user/aj3lib/types/enums.md,
    docs/user/aj3lib/types/BaseCode.md,
    docs/user/aj3lib/types/datetime/supporting-enums.md,
    docs/user/aj3lib/types/datetime/calendar-infrastructure.md,
    docs/user/aj3lib/types/datetime/cultural-types.md,
    docs/user/aj3lib/types/datetime/non-standard-time.md
  </files>
  <action>
    In every file listed:
    1. Find all `[#] << ###Enum` lines inside {#} definitions that also have `[#] << ##Scalar`
    2. Replace with `[#] << ###ScalarEnum`
    3. Update any explanatory comments that say "###Enum" for these scalar types to "###ScalarEnum"
    4. In boolean.md: update the comment "sets %##Depth.Max << 0" to "sets %##Depth.Max << 1"
    5. Update frontmatter `updated:` date to 2026-04-04
    
    DO NOT change ###Enum in non-scalar types.
  </action>
  <verify>Grep all modified files for "[#] << ###Enum" — should only remain in non-scalar contexts. Grep for "###ScalarEnum" — should appear in all listed files.</verify>
  <done>AC-1 satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Update aj3lib scalar value types — ###Value → ###ScalarValue</name>
  <files>
    docs/user/aj3lib/types/string.md,
    docs/user/aj3lib/types/scalars.md,
    docs/user/aj3lib/types/structs.md,
    docs/user/aj3lib/types/datetime/core-components.md,
    docs/user/aj3lib/types/datetime/calendar-date-types.md
  </files>
  <action>
    In every file listed:
    1. Find `[#] << ###Value` lines inside {#} definitions that also have `[#] << ##Scalar`
    2. Replace with `[#] << ###ScalarValue`
    3. In string.md: update "sets %##Depth.Max << 0" to "sets %##Depth.Max << 1"
    4. Update frontmatter `updated:` date to 2026-04-04
    
    DO NOT change ###Value in non-scalar types (#DateTime main, #Array, #Map, collections).
    Note: structs.md may have scalar types (#path, #Queue) — check each definition.
    Note: core-components.md and calendar-date-types.md have scalar types with ##Scalar — update those.
  </action>
  <verify>Grep all modified files for "[#] << ###Value" — should only remain in non-scalar contexts. Grep for "###ScalarValue" — should appear where appropriate.</verify>
  <done>AC-2 satisfied</done>
</task>

<task type="auto">
  <name>Task 3: Update type hierarchy, prefix, and reference files</name>
  <files>
    docs/user/syntax/types/hierarchy.md,
    docs/user/syntax/types/prefix-system.md,
    docs/user/syntax/types/macro-types.md,
    docs/user/syntax/types/INDEX.md,
    docs/user/aj3lib/types/types.md,
    docs/user/aj3lib/types/collections.md,
    docs/user/concepts/data-is-trees.md,
    docs/user/concepts/collections/dataframe.md,
    docs/user/aj3lib/pipelines/#.md
  </files>
  <action>
    1. hierarchy.md: Add ##Leaf to schema listings. Update any "Depth.Max=0" scalar references to 1.
    2. prefix-system.md: Add ##Leaf example. Update ### examples if they show ###Enum/###Value for scalars.
    3. macro-types.md: Update ##Scalar constraint references — Depth.Max=1 not 0. Add ###ScalarValue where macro generates scalar types.
    4. INDEX.md: Update GT-5 if it mentions Depth.Max=0 for scalars.
    5. types.md: Update type tree display to show ###ScalarValue/###ScalarEnum.
    6. collections.md: Ensure ##Flat distinction from ##Scalar is clear. Both Depth.Max=1 but different constraints.
    7. data-is-trees.md: Update ## schema examples if they mention Depth.Max=0 for scalars.
    8. dataframe.md: Update Depth.Max references if they describe scalars as 0.
    9. #.md (pipelines): Update ##Scalar reference if present.
    10. Update frontmatter dates to 2026-04-04.
  </action>
  <verify>Grep all modified files for "Depth.Max.*0" — only ##Leaf context. Grep for "##Leaf" — should appear in hierarchy and prefix docs.</verify>
  <done>AC-3, AC-4 satisfied</done>
</task>

<task type="auto">
  <name>Task 4: Update technical files — COMPILE-RULES, EBNF, edge-cases</name>
  <files>
    docs/technical/COMPILE-RULES.md,
    docs/technical/ebnf/04-type-system.md,
    docs/technical/ebnf/03-identifiers.md,
    docs/technical/edge-cases/24-datatype-defs.md
  </files>
  <action>
    1. COMPILE-RULES.md: Update any rules that describe ##Scalar as Depth.Max=0 to Depth.Max=1. Add ###ScalarValue/###ScalarEnum to any field type listings.
    2. EBNF 04-type-system.md: Update schema_compose examples if they show ##Scalar with 0. Add ##Leaf to schema_id examples. Add ###ScalarValue/###ScalarEnum to field_type grammar.
    3. EBNF 03-identifiers.md: Add ##Leaf to schema_id example comments. Add ###ScalarValue/###ScalarEnum to field_type_id.
    4. edge-cases/24-datatype-defs.md: Update EC-24.1 and EC-24.8 depth references. Update any "Depth.Max = 0" to 1 for scalar types. Update ###Enum → ###ScalarEnum in scalar contexts.
    5. Update frontmatter dates to 2026-04-04.
  </action>
  <verify>Grep all modified files for "Depth.Max.*= 0" or "Depth.Max.*<< 0" — only ##Leaf. Grep for "###ScalarEnum" in edge-cases — should appear in EC-24.8.</verify>
  <done>AC-5, AC-6 satisfied</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- docs/user/syntax/types/schema-properties.md (already updated in 118-01)
- docs/user/syntax/types/basic-types.md (already updated in 118-01)
- docs/technical/spec/metadata-tree/string-subtypes.md (already updated in 118-01)
- .paul/ directory (except STATE.md updates)
- Any file not listed in files_modified

## SCOPE LIMITS
- Only update references to match new model — no new features or compile rules
- ###Value/###Enum UNCHANGED in non-scalar types (#DateTime main, collections as containers)
- No changes to archive/ content
- No changes to decision docs in docs/technical/plan/decisions/

</boundaries>

<verification>
Before declaring plan complete:
- [ ] Grep all docs/ for `[#] << ###Enum` — only in non-##Scalar types
- [ ] Grep all docs/ for `[#] << ###Value` — only in non-##Scalar types
- [ ] Grep modified files for "Depth.Max.*0" — only ##Leaf context
- [ ] Grep for "###ScalarValue" — present in scalar value type files
- [ ] Grep for "###ScalarEnum" — present in scalar enum type files
- [ ] Grep for "##Leaf" — present in hierarchy/prefix/EBNF docs
- [ ] Non-scalar types (#DateTime, #Array, #Map) unchanged
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All 4 tasks completed
- All 6 acceptance criteria met
- Zero stale Depth.Max=0 references for scalars across all docs
- Complete consistency with 118-01 core definitions
</success_criteria>

<output>
After completion, create `.paul/phases/issue-118-string-depth-max/118-02-SUMMARY.md`
</output>
