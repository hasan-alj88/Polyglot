---
phase: issue-85-document-pglib-pipeline-permissions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/user/pglib/pipelines/File.md
  - docs/user/pglib/pipelines/T.md
  - docs/user/pglib/pipelines/W.md
  - docs/user/pglib/pipelines/Math.md
  - docs/user/pglib/pipelines/Path.md
  - docs/user/pglib/pipelines/Sys.md
  - docs/user/pglib/pipelines/Q.md
  - docs/user/pglib/INDEX.md
autonomous: true
---

<objective>
## Goal
Add `[_]` permission declarations to all pglib pipeline reference files and add a Permission column to INDEX.md.

## Purpose
Complete the permission documentation chain: issues #80-#84 defined the permission system, compile rules, and error tree. Issue #85 closes the loop by documenting which pglib pipelines require which permissions.

## Output
- 7 pglib pipeline files updated with permission sections
- INDEX.md updated with Permission column
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Source Files
@docs/user/pglib/INDEX.md
@docs/user/pglib/pipelines/File.md
@docs/user/pglib/pipelines/T.md
@docs/user/pglib/pipelines/W.md
@docs/user/pglib/pipelines/Q.md
@docs/user/pglib/pipelines/Math.md
@docs/user/pglib/pipelines/Path.md
@docs/user/pglib/pipelines/Sys.md
@docs/user/concepts/permissions.md
</context>

<acceptance_criteria>

## AC-1: Permission Declarations in Pipeline Files
```gherkin
Given each pglib pipeline file in docs/user/pglib/pipelines/
When the file is read
Then IO-touching pipelines have a "## Permissions" section listing required [_] declarations
And pure pipelines state "No permissions required"
```

## AC-2: INDEX.md Permission Column
```gherkin
Given docs/user/pglib/INDEX.md Pipeline Namespaces table
When the table is read
Then a "Permission" column exists showing permission categories or "None" for each namespace
```

## AC-3: Permission Map Matches Issue Specification
```gherkin
Given the permission map from issue #85
When compared to the documented permissions
Then all pipelines from the issue map are covered with correct _Category.subfield mappings
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add permission sections to all 7 pglib pipeline files</name>
  <files>
    docs/user/pglib/pipelines/File.md,
    docs/user/pglib/pipelines/T.md,
    docs/user/pglib/pipelines/W.md,
    docs/user/pglib/pipelines/Math.md,
    docs/user/pglib/pipelines/Path.md,
    docs/user/pglib/pipelines/Sys.md,
    docs/user/pglib/pipelines/Q.md
  </files>
  <action>
    Add a "## Permissions" section to each file. Place it after the signature block and before any existing error/status sections.

    **File.md** — IO pipelines, add per-operation permissions:
    - =File.Text.Read → _File.read (inline)
    - =File.Text.Write → _File.write (inline)
    - =File.Text.Append → _File.write (inline)
    - =File.Binary.Read → _File.read (inline)
    - =File.Binary.Write → _File.write (inline)
    - =File.Copy → _File.read + _File.write (inline)
    - =File.Move → _File.read + _File.write (inline)
    - =File.Delete → _File.delete (inline)
    - =File.Access → _File.read (inline)
    - =File.List → _File.read (inline)

    **T.md** — Mixed:
    - =T.Call → None
    - =T.Manual → None
    - =T.Daily → None
    - =T.Folder.NewFiles → _File.read (inline, filesystem monitoring)
    - =T.Webhook → _Web.socket (IO, incoming HTTP)

    **W.md** — Per-wrapper permissions from issue map:
    - =W.Polyglot → None
    - =W.DB.Session → _Database.connect (IO)
    - =W.File.Lock → _File.read (inline)
    - =W.HTTP.Session → _Web.request (IO)
    - =W.SSH.Session → _System.process (IO)
    - =W.Auth.Token → _Crypto.key (IO)
    - =W.Log.File → _File.write (inline)
    - =W.Queue.Consumer → _IPC.receive (IO)
    - =W.Cache.Scope → _Database.read (IO)
    - =W.Python → _System.process (IO)

    **Math.md, Path.md, Q.md** — Pure computation, add:
    "No permissions required. All operations are pure computation."

    **Sys.md** — System info:
    - =Sys.OS → _System.env (inline)

    Format as a table:
    | Pipeline | Permission | Type |
    |----------|-----------|------|
    | `=File.Text.Read` | `_File.read` | Inline |

    Add a cross-reference: See [[permissions]] for permission categories and [[errors#Built-in Error Namespaces]] for `!Permission.*` errors.

    Update frontmatter `updated` to `2026-03-25` on all modified files.
  </action>
  <verify>Grep for "## Permissions" in all 7 files — all must have the section</verify>
  <done>AC-1 and AC-3 satisfied: All pipeline files have permission sections matching the issue map</done>
</task>

<task type="auto">
  <name>Task 2: Add Permission column to INDEX.md pipeline table</name>
  <files>docs/user/pglib/INDEX.md</files>
  <action>
    Add a "Permission" column to the Pipeline Namespaces table (lines 27-35).

    Values:
    | Prefix | File | Description | Status | Permission |
    | `=File` | ... | ... | Stable | `_File.*` |
    | `=Path` | ... | ... | Stable | None |
    | `=Math` | ... | ... | Stable | None |
    | `=Sys` | ... | ... | Stable | `_System.env` |
    | `=T` | ... | ... | Stable | Mixed |
    | `=Q` | ... | ... | Stable | None |
    | `=W` | ... | ... | Stable | Mixed |

    "Mixed" means some pipelines in the namespace need permissions and some don't — see the individual file for details.

    Also update the Error Namespaces table to include `!Permission`:
    | `!File`, `!No`, `!Timeout`, `!Math`, `!Validation`, `!Permission` | ... | Standard error trees | Stable |

    Update frontmatter `updated` to `2026-03-25`.
  </action>
  <verify>Read INDEX.md and confirm Permission column exists with correct values</verify>
  <done>AC-2 satisfied: INDEX.md has Permission column</done>
</task>

<task type="auto">
  <name>Task 3: Verify completeness</name>
  <files>None (read-only verification)</files>
  <action>
    - Grep for "## Permissions" across all 7 pipeline files
    - Grep for "_File\|_Web\|_Database\|_System\|_Crypto\|_IPC" to confirm permission categories used
    - Verify INDEX.md table has 5 columns
    - Confirm no pipelines from the issue #85 map are missing
  </action>
  <verify>All 7 files have permissions section; INDEX.md has Permission column; all issue map entries covered</verify>
  <done>All AC satisfied</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- docs/user/concepts/permissions.md (permission system definition — reference only)
- docs/user/concepts/errors.md (error system — reference only)
- docs/user/pglib/errors/errors.md (error tree — reference only)
- .paul/ directory (except STATE.md updates)

## SCOPE LIMITS
- Only add permission sections — do not restructure existing content
- Do not add new pipelines or modify signatures
- Do not create compile rules (already done in #83)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] All 7 pipeline files have "## Permissions" section
- [ ] INDEX.md has "Permission" column in pipeline table
- [ ] All pipelines from issue #85 map are documented
- [ ] Cross-references to [[permissions]] present
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Permission documentation chain complete (#80 → #81 → #82 → #83 → #84 → #85)
</success_criteria>

<output>
After completion, create `.paul/phases/issue-85-document-pglib-pipeline-permissions/85-01-SUMMARY.md`
</output>
