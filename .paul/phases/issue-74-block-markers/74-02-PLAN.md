---
phase: issue-74-block-markers
plan: 02
type: execute
wave: 1
depends_on: ["74-01"]
files_modified:
  - docs/technical/EDGE-CASES.md
  - docs/technical/compile-rules/algorithms/compound-exhaustiveness.md
  - docs/technical/compile-rules/algorithms/overlap-detection.md
  - docs/technical/compile-rules/PGE/PGE06001-conditional-must-be-exhaustive.md
  - docs/technical/compile-rules/PGE/PGE06005-compound-condition-overlap.md
  - docs/technical/compile-rules/PGE/PGE06008-compound-condition-exhaustiveness.md
  - docs/technical/compile-rules/PGE/PGE06013-tautological-branch-condition.md
  - docs/technical/compile-rules/PGE/PGE01018-tautological-trigger-condition.md
autonomous: true
---

<objective>
## Goal
Migrate all remaining `[+]` OR references to `[|]` in compile rules, algorithm docs, and EDGE-CASES. After this plan, zero non-archive files use `[+]` as OR.

## Purpose
Completes issue #74 — Plan 01 updated the core spec files, this plan updates all downstream references for full consistency.

## Output
8 files updated with `[+]` → `[|]` for OR references.
</objective>

<context>
## Project Context
@.paul/STATE.md

## Prior Work
@.paul/phases/issue-74-block-markers/74-01-SUMMARY.md

## Source Files
@docs/technical/EDGE-CASES.md
@docs/technical/compile-rules/algorithms/compound-exhaustiveness.md
@docs/technical/compile-rules/algorithms/overlap-detection.md
@docs/technical/compile-rules/PGE/PGE06001-conditional-must-be-exhaustive.md
@docs/technical/compile-rules/PGE/PGE06005-compound-condition-overlap.md
@docs/technical/compile-rules/PGE/PGE06008-compound-condition-exhaustiveness.md
@docs/technical/compile-rules/PGE/PGE06013-tautological-branch-condition.md
@docs/technical/compile-rules/PGE/PGE01018-tautological-trigger-condition.md
</context>

<skills>
No specialized flows configured.
</skills>

<acceptance_criteria>

## AC-1: EDGE-CASES uses [|] for OR
```gherkin
Given docs/technical/EDGE-CASES.md references logical operators
When I grep for [+] in logical/OR contexts
Then all OR references use [|] instead of [+]
And code examples use [|] for OR conditions
```

## AC-2: Compile rules use [|] for OR
```gherkin
Given 5 PGE compile rule files and 2 algorithm files reference [+] as OR
When I grep all compile-rules files for [+] in OR contexts
Then all references use [|] instead of [+]
```

## AC-3: Zero non-archive [+]-as-OR remains
```gherkin
Given all Plan 01 and Plan 02 files are updated
When I grep the entire non-archive docs/ for [+] used as OR logical operator
Then zero matches found (only [+] as line continuation in blocks.md/EBNF.md)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Update EDGE-CASES.md</name>
  <files>docs/technical/EDGE-CASES.md</files>
  <action>
    Replace all `[+]` references that mean OR with `[|]`:
    1. Line ~537: Logical marker table — change `[+]` to `[|]`
    2. Line ~1065: Compound conditions test description — change `[+]` to `[|]`
    3. Line ~1075: Code example `[+] $role =? #Role.Superuser` → `[|] $role =? #Role.Superuser`
    4. Line ~1672: Negation + logical test description — change `[+]` to `[|]`
    Do NOT change any `[+]` that might refer to line continuation (unlikely in this file).
  </action>
  <verify>Grep EDGE-CASES.md for `[+]` — should return zero matches in non-archive context</verify>
  <done>AC-1 satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Update compile rule and algorithm files</name>
  <files>
    docs/technical/compile-rules/algorithms/compound-exhaustiveness.md,
    docs/technical/compile-rules/algorithms/overlap-detection.md,
    docs/technical/compile-rules/PGE/PGE06001-conditional-must-be-exhaustive.md,
    docs/technical/compile-rules/PGE/PGE06005-compound-condition-overlap.md,
    docs/technical/compile-rules/PGE/PGE06008-compound-condition-exhaustiveness.md,
    docs/technical/compile-rules/PGE/PGE06013-tautological-branch-condition.md,
    docs/technical/compile-rules/PGE/PGE01018-tautological-trigger-condition.md
  </files>
  <action>
    In each file, replace `[+]` with `[|]` where it means OR:
    - Text references like "`[+]` OR" → "`[|]` OR"
    - Code examples with `[+]` as OR → `[|]`
    - Table cells with `[+]` as OR → `[|]`
    Careful: escape pipes in markdown tables with `\|` inside backticks.
  </action>
  <verify>Grep all compile-rules/ for `[+]` — should return zero matches</verify>
  <done>AC-2 satisfied</done>
</task>

<task type="auto">
  <name>Task 3: Final sweep verification</name>
  <files>docs/ (non-archive)</files>
  <action>
    Grep entire docs/ (excluding docs/archive/) for `[+]` to confirm only line-continuation references remain (blocks.md and EBNF.md).
  </action>
  <verify>Grep docs/ excluding archive for `[+]` — only blocks.md and EBNF.md continuation references</verify>
  <done>AC-3 satisfied</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- docs/archive/* (deprecated files — historical record)
- docs/user/syntax/blocks.md (already updated in Plan 01)
- docs/user/concepts/conditionals.md (already updated in Plan 01)
- docs/technical/EBNF.md (already updated in Plan 01)
- .paul/* (except STATE.md)

## SCOPE LIMITS
- Only `[+]` → `[|]` migration — no new content or restructuring
- Do not modify any file not listed in files_modified

</boundaries>

<verification>
Before declaring plan complete:
- [ ] Zero `[+]`-as-OR in any non-archive docs/ file
- [ ] All 8 files updated
- [ ] No broken references or formatting introduced
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Complete consistency: `[|]` is OR everywhere in non-archive docs
</success_criteria>

<output>
After completion, create `.paul/phases/issue-74-block-markers/74-02-SUMMARY.md`
</output>
