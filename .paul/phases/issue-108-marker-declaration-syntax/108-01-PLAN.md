---
phase: issue-108-marker-declaration-syntax
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/technical/ebnf/09-definition-blocks.md
  - docs/user/syntax/blocks.md
  - docs/user/concepts/pipelines/INDEX.md
autonomous: true
---

<objective>
## Goal
Add `[exe]` marker declaration syntax to the Polyglot specification: update EBNF grammar for `{=}` pipeline definitions, update blocks.md `{=}` row and add explanatory note, and add "Marker Declarations" section to pipelines INDEX.md.

## Purpose
Issue #108 documents how `{=}` pipelines declare their invocation context via markers. This is foundational for compiler rules (#111) and enables per-marker validation.

## Output
Three spec files updated with marker declaration grammar, user-facing explanations, and examples.
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Source Files
@docs/technical/ebnf/09-definition-blocks.md
@docs/user/syntax/blocks.md
@docs/user/concepts/pipelines/INDEX.md
@docs/technical/brainstorming/marker-declarations.md (§0 Marker Groups — reference)
</context>

<acceptance_criteria>

## AC-1: EBNF §9.3 grammar includes marker_decl
```gherkin
Given the EBNF §9.3 Pipeline Definition grammar
When a developer reads the pipeline_def production
Then it shows `marker_decl?` as an optional element after `"{=}"`
And marker_decl is defined as `"[exe]" | "[b]" | "[r]" | "[p]" | "[rp]" | "[rb]" | "[pb]"`
And a note states `{=}` without marker defaults to `{=}[exe]` with no warning
```

## AC-2: blocks.md {=} row mentions marker syntax
```gherkin
Given the blocks.md Definition Elements table
When a user reads the `{=}` row
Then it mentions `{=}[exe]` marker declaration syntax
And a note below the table explains `[exe]` covers `[r]`, `[p]`, `[b]` with subset restrictions
```

## AC-3: Pipelines INDEX.md has Marker Declarations section
```gherkin
Given the pipelines INDEX.md page
When a user reads the Marker Declarations section
Then they understand what markers mean, how subsets restrict invocation, and the default behavior
And examples show `{=}[exe]`, `{=}[b]`, and subset syntax
```

## AC-4: Consistency across all three files
```gherkin
Given all three modified files
When compared for consistency
Then the marker list is identical (exe, b, r, p, rp, rb, pb)
And the default behavior description is consistent (no warning)
And cross-references link correctly between files
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Update EBNF §9.3 pipeline_def grammar with marker_decl</name>
  <files>docs/technical/ebnf/09-definition-blocks.md</files>
  <action>
    1. In §9.3 Pipeline Definition, update the `pipeline_def` production to include `marker_decl?`:
       ```
       pipeline_def ::= "{=}" marker_decl? pipeline_id NEWLINE pipeline_body ;
       marker_decl  ::= "[exe]" | "[b]" | "[r]" | "[p]" | "[rp]" | "[rb]" | "[pb]" ;
       ```
    2. Add rules below the grammar:
       - `{=}` without `marker_decl` defaults to `{=}[exe]` — no warning, no error.
       - `[exe]` = execution pipeline, invocable via `[r]`, `[p]`, or `[b]`.
       - Subsets restrict invocation: `{=}[b]` means background-only (no outputs allowed), `{=}[rp]` means sequential or parallel only.
       - `{T}`, `{W}`, `{Q}` already have implicit markers (`[T]`, `[W]`, `[Q]`) — they cannot take `marker_decl`.
    3. Add one example showing `{=}[b]` (background-only pipeline).
    4. Do NOT modify any other §9.x sections.
  </action>
  <verify>Read §9.3 and confirm: pipeline_def includes marker_decl?, marker_decl production exists, rules explain default and subsets</verify>
  <done>AC-1 satisfied: EBNF grammar has marker_decl with all 7 options and default rule</done>
</task>

<task type="auto">
  <name>Task 2: Update blocks.md {=} row and add marker note</name>
  <files>docs/user/syntax/blocks.md</files>
  <action>
    1. Update the `{=}` row in the Definition Elements table to mention marker syntax:
       `| {=} | Pipeline definition. Supports marker declarations: {=}[exe] (default), subsets. See [[concepts/pipelines/INDEX|pipelines]] |`
    2. Add a brief note after the Definition Elements table (before the `[X]` section) explaining:
       - `[exe]` is the execution marker covering `[r]`, `[p]`, `[b]`
       - `{=}` without a marker defaults to `{=}[exe]`
       - Subsets like `{=}[b]` or `{=}[rp]` restrict how the pipeline can be invoked
       - Link to [[concepts/pipelines/INDEX#Marker Declarations|Marker Declarations]] for full details
    3. Do NOT modify any `[X]` block element tables — markers are on definition lines, not block elements.
  </action>
  <verify>Read blocks.md and confirm: {=} row mentions markers, note explains [exe] and subsets with cross-reference</verify>
  <done>AC-2 satisfied: blocks.md documents marker syntax on {=} definitions</done>
</task>

<task type="auto">
  <name>Task 3: Add Marker Declarations section to pipelines INDEX.md</name>
  <files>docs/user/concepts/pipelines/INDEX.md</files>
  <action>
    1. Add a "## Marker Declarations" section after the Pipeline Structure table and notes but before "## Sub-Pages".
    2. Content should cover:
       - What markers are: declarations on `{=}` that specify invocation context
       - The `[exe]` marker: covers `[r]` (sequential), `[p]` (parallel), `[b]` (background)
       - Default: `{=}` without marker = `{=}[exe]`, no warning
       - Subsets restrict invocation:
         - `{=}[b]` — background-only, no outputs allowed (fire-and-forget)
         - `{=}[rp]` — sequential or parallel only (no background)
         - `{=}[rb]` — sequential or background only (no parallel)
         - `{=}[pb]` — parallel or background only (no sequential)
         - `{=}[r]`, `{=}[p]`, `{=}[b]` — single-mode only
       - Subtypes already have fixed markers: `{T}` = `{=}[T]`, `{W}` = `{=}[W]`, `{Q}` = `{=}[Q]` — they cannot take additional markers
    3. Include a table summarizing all marker options.
    4. Include 2-3 examples: default `{=}`, explicit `{=}[exe]`, restricted `{=}[b]`.
    5. Link to [[technical/ebnf/09-definition-blocks#9.3|EBNF §9.3]] for formal grammar.
  </action>
  <verify>Read pipelines INDEX.md and confirm: Marker Declarations section exists with table, examples, and cross-references</verify>
  <done>AC-3 and AC-4 satisfied: Marker Declarations section complete with consistent terminology</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- docs/technical/ebnf/ sections other than §9.3
- docs/user/syntax/blocks.md `[X]` block element tables
- docs/user/concepts/pipelines/ sub-pages (io-triggers.md, queue.md, etc.)
- docs/technical/brainstorming/ (reference only, do not modify)

## SCOPE LIMITS
- No compiler rules (PGE) — that's issue #111
- No `[t]` → `[T]` rename — that's issue #109
- No base pipeline / #BaseCode documentation — that's issue #110
- No new files — only edit existing files

</boundaries>

<verification>
Before declaring plan complete:
- [ ] EBNF §9.3 pipeline_def includes marker_decl? production
- [ ] blocks.md {=} row references marker syntax
- [ ] pipelines INDEX.md has Marker Declarations section
- [ ] Marker list is consistent across all 3 files (exe, b, r, p, rp, rb, pb)
- [ ] Default behavior stated consistently (no warning)
- [ ] Cross-references link correctly between files
- [ ] No changes outside the 3 target files
</verification>

<success_criteria>
- All 3 tasks completed
- All 4 acceptance criteria met
- All verification checks pass
- No changes to out-of-scope files
</success_criteria>

<output>
After completion, create `.paul/phases/issue-108-marker-declaration-syntax/108-01-SUMMARY.md`
</output>
