---
phase: issue-95-96-doc-audit-inconsistencies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/user/syntax/types/structs.md
  - docs/technical/spec/type-identity.md
  - docs/audit/tracking/inconsistencies.md
autonomous: true
---

<objective>
## Goal
Fix IC-005 (enums mislabeled as structs) and correct IC-005 tracking entry. Verify all other IC items and #96 metadata split are genuinely resolved.

## Purpose
Issue #95 is the umbrella tracker for 8 documentation inconsistencies. A prior commit (516139a) resolved most, but IC-005 was marked resolved incorrectly — `structs.md:17` still lists enum types as structs, and `type-identity.md:14` has the same error. `#DateTime` references are kept because plan 95-02 will formalize it as a stdlib type. Issue #96 (metadata split, IC-002/AD-005) is already implemented.

## Output
- Fixed `structs.md` and `type-identity.md` — correct struct/enum classification, `#DateTime` kept as valid upcoming stdlib type
- Corrected `inconsistencies.md` — accurate IC-005 resolution entry
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Source Files
@docs/user/syntax/types/structs.md
@docs/technical/spec/type-identity.md
@docs/audit/tracking/inconsistencies.md
@docs/user/stdlib/types/enums.md
@docs/user/stdlib/types/boolean.md
@docs/user/stdlib/types/structs.md

## Key Facts
- Actual stdlib structs: `#path`, `#Queue` (defined in `docs/user/stdlib/types/structs.md`)
- Actual stdlib enums: `#Boolean`, `#OS`, `#PipelineStatus`, `#VarState`, `#QueueStrategy`, `#RetriggerStrategy`, `#QueueState`, `#FileAccess`, `#FieldKind` (in `enums.md`, `boolean.md`)
- `#DateTime` exists in `docs/technical/brainstorming/datetime-tree.md` — will be formalized in plan 95-02
- All `{#}` types are technically structs, but enums (`###Enum` leaf type) are documented separately
</context>

<acceptance_criteria>

## AC-1: structs.md lists only non-enum structs
```gherkin
Given docs/user/syntax/types/structs.md line 17
When the struct examples are listed
Then only non-enum stdlib structs (#path, #Queue, #DateTime) appear
And enum types (#Boolean, #OS, #PipelineStatus, #VarState) are cross-referenced to enums.md/boolean.md
```

## AC-2: type-identity.md uses accurate examples
```gherkin
Given docs/technical/spec/type-identity.md line 14
When {#} struct examples are given
Then only existing or in-progress stdlib types are listed (replacing #DateTime is acceptable since 95-02 formalizes it)
And the broader claim that all {#} types are structs remains intact
```

## AC-3: IC-005 tracking is accurate
```gherkin
Given docs/audit/tracking/inconsistencies.md IC-005 entry
When the Resolved table is checked
Then the resolution accurately describes the fix applied
And the resolution date reflects this fix, not the prior incorrect entry
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Fix struct/enum classification in spec files</name>
  <files>docs/user/syntax/types/structs.md, docs/technical/spec/type-identity.md</files>
  <action>
    1. In `structs.md:17`, replace the current line:
       "Stdlib structs include `path`, `#Boolean`, `#OS`, `#PipelineStatus`, `#VarState`, and `#DateTime`."
       With a line listing only non-enum stdlib structs (`#path`, `#Queue`, `#DateTime`) and cross-referencing enums.md/boolean.md for enum types. `#DateTime` is kept because plan 95-02 formalizes it.

    2. In `type-identity.md:14`, fix the example list:
       Keep `#Boolean`, `#OS`, `#DateTime` as valid {#} examples since this file discusses the broader category (all {#} types are structs).
       Add `#path` or `#Queue` to the examples for balance between enum and non-enum struct examples.
  </action>
  <verify>
    - Read structs.md:17 — should mention #path, #Queue, #DateTime (non-enum structs) and cross-ref enums
    - Read type-identity.md:14 — should list only existing/in-progress stdlib types
  </verify>
  <done>AC-1 and AC-2 satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Correct IC-005 tracking entry</name>
  <files>docs/audit/tracking/inconsistencies.md</files>
  <action>
    Update the IC-005 Resolved table entry (around line 52):
    - Change the resolution text from "Already fixed — structs.md no longer lists enum types; confirmed via grep" to an accurate description of the actual fix
    - Update date to current date (2026-04-01)
  </action>
  <verify>Read inconsistencies.md Resolved table — IC-005 entry should describe the actual fix accurately</verify>
  <done>AC-3 satisfied</done>
</task>

<task type="auto">
  <name>Task 3: Final verification sweep</name>
  <files>(read-only verification)</files>
  <action>
    Verify all 8 IC items are genuinely resolved:
    - IC-001: grep for #VarState.Ready in docs/ (should be 0 outside tracking)
    - IC-002: check metadata.md frontmatter says audience: user
    - IC-003: check scalars.md regex is ^[0-9]+D$
    - IC-004: grep for [[STDLIB]] in docs/ (should be 0 outside tracking)
    - IC-005: confirm fix from Task 1
    - IC-006: check @-imports in SPEC-INDEX.md and technical/INDEX.md
    - IC-007: check no TBD stubs in metadata.md
    - IC-008: confirmed Won't Fix (archived)

    For #96: spot-check 3-4 metadata wikilinks resolve to correct targets.
  </action>
  <verify>All grep/read checks pass — no remaining inconsistencies</verify>
  <done>All IC items verified resolved; issues ready to close on GitHub</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- docs/audit/tracking/progress.md (audit progress — not part of this fix)
- docs/audit/tracking/coverage-gaps.md (coverage gaps — separate scope)
- docs/audit/tracking/decisions.md (AD decisions — already correct)
- docs/user/concepts/metadata.md (already fixed by AD-005)
- docs/technical/spec/metadata-tree.md (already created by AD-005)
- Any file in docs/archive/ or docs/technical/brainstorming/

## SCOPE LIMITS
- Only fix IC-005 and verify others — do not reopen resolved items
- Do not create new IC items (defer to future audit)
- Do not modify wikilinks unless broken (verification only)
- Do not close GitHub issues — deferred to plan 95-03 verification

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `grep -r "VarState.Ready" docs/` returns only tracking file matches
- [ ] `grep -r "\[\[STDLIB\]\]" docs/` returns only tracking file matches
- [ ] structs.md:17 lists #path, #Queue, #DateTime — not enums
- [ ] type-identity.md:14 lists only existing/in-progress stdlib types
- [ ] inconsistencies.md IC-005 Resolved entry is accurate
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- IC-005 genuinely fixed in both affected files
- IC-005 tracking entry corrected
- All 8 IC items verified resolved (or Won't Fix)
- No new inconsistencies introduced
</success_criteria>

<output>
After completion, create `.paul/phases/issue-95-96-doc-audit-inconsistencies/95-01-SUMMARY.md`
</output>
