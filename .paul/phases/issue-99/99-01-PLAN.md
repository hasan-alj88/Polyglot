---
phase: issue-99
plan: 01
type: execute
autonomous: true
---

<objective>
Implement EBNF edge cases X.7--X.11 from issue #99: empty definitions and zero-param macros.

Five edge cases, each with checked decisions from the issue:
- X.7: Empty pipeline body → PGE + tighten EBNF (PGE01005/01006 already catch; add edge case)
- X.8: Empty {#} data definition → PGE + tighten EBNF (PGW01002 exists; upgrade to error)
- X.9: Empty {!} error namespace → PGE + tighten EBNF (new rule)
- X.10: File with only comments after {@} → PGW + semantic only (new warning)
- X.11: Zero-parameter macro → PGE + tighten EBNF (new rule)
</objective>

<context>
@docs/technical/ebnf/09-definition-blocks.md — EBNF grammar for all definition blocks
@docs/technical/ebnf/04-type-system.md — EBNF for {M} macro_def
@docs/technical/COMPILE-RULES.md — master index of PGE/PGW codes
@docs/technical/compile-rules/PGW/PGW01001-empty-execution-body.md — pattern for empty-body warnings
@docs/technical/compile-rules/PGW/PGW01002-empty-data-definition.md — empty data def (to upgrade)
@docs/technical/compile-rules/PGE/PGE01005-missing-trigger.md — covers empty pipeline partially
@docs/technical/compile-rules/PGE/PGE01004-macro-structural-constraints.md — macro constraints
@docs/technical/edge-cases/INDEX.md — edge case index
@docs/technical/edge-cases/09-definition-blocks.md — S9 definition block edge cases
@docs/technical/edge-cases/18-macro-structure.md — S18 macro edge cases
</context>

<acceptance_criteria>
Given the five edge cases from issue #99
When all changes are applied
Then:
1. EBNF in 09-definition-blocks.md tightened: pipeline_body requires trigger+wrapper, data_def requires 1+ body line, error_def requires 1+ leaf
2. EBNF in 04-type-system.md tightened: macro_def requires 1+ parameter
3. PGW01002 upgraded from warning to PGE01021 (empty data definition is now an error)
4. New PGE01022 created for empty error namespace
5. New PGE01023 created for zero-parameter macro
6. New PGW01003 created for file with no definitions
7. Edge cases added: EC-9.12 (empty pipeline), EC-9.13 (empty data), EC-9.14 (empty error namespace), EC-9.15 (comment-only file), EC-18.4 (zero-param macro)
8. COMPILE-RULES.md index updated with new codes
9. Edge case INDEX.md updated with new entries and count
</acceptance_criteria>

<tasks>
<task type="auto">T1: Tighten EBNF grammar in 09-definition-blocks.md
- pipeline_body: already has trigger_io_section, queue_section, wrapper_section, execution_section as required — verify X.7 is caught by PGE01005+PGE01006
- data_def: change `{ indent data_body_line NEWLINE }` to `indent data_body_line NEWLINE { indent data_body_line NEWLINE }` (1+ body lines)
- error_def: change `{ indent error_leaf_line NEWLINE }` to `indent error_leaf_line NEWLINE { indent error_leaf_line NEWLINE }` (1+ leaves)
</task>

<task type="auto">T2: Tighten EBNF grammar in 04-type-system.md
- macro_def: change `{ indent macro_type_body_line NEWLINE }` to require at least one macro_param or macro_type_param
</task>

<task type="auto">T3: Create PGE01021 — Empty Data Definition (upgrade from PGW01002)
- New file: PGE01021-empty-data-definition.md
- Statement: {#} with no fields is a compile error (not warning)
- Update PGW01002 to reference PGE01021 as superseding it, or remove PGW01002
</task>

<task type="auto">T4: Create PGE01022 — Empty Error Namespace
- New file: PGE01022-empty-error-namespace.md
- Statement: {!} with no error leaves is a compile error
</task>

<task type="auto">T5: Create PGE01023 — Parameterless Macro
- New file: PGE01023-parameterless-macro.md
- Statement: {M} with no [#] parameters is a compile error
</task>

<task type="auto">T6: Create PGW01003 — No Definitions in File
- New file: PGW01003-no-definitions-in-file.md
- Statement: File with {@} but no {#}/{=}/{M}/{W}/{Q}/{!} definitions emits warning
</task>

<task type="auto">T7: Add edge cases to 09-definition-blocks.md
- EC-9.12: Empty pipeline body (shows PGE01005 catching it)
- EC-9.13: Empty {#} data definition (shows PGE01021)
- EC-9.14: Empty {!} error namespace (shows PGE01022)
- EC-9.15: Comment-only file after {@} (shows PGW01003)
</task>

<task type="auto">T8: Add edge case to 18-macro-structure.md
- EC-18.4: Zero-parameter macro (shows PGE01023)
</task>

<task type="auto">T9: Update COMPILE-RULES.md index
- Add PGE01021, PGE01022, PGE01023 to 01-Pipeline Structure table
- Add PGW01003 to warnings table
- Mark PGW01002 as superseded by PGE01021
</task>

<task type="auto">T10: Update edge-cases/INDEX.md
- Update S9 row: EC-9.1--9.15
- Update S18 row: EC-18.1--18.4
- Update total count
</task>
</tasks>

<boundaries>
- DO NOT modify any files outside docs/technical/
- DO NOT change existing PGE/PGW rule numbering (only add new codes)
- DO NOT add edge cases to sections other than S9 and S18
- Pipeline EBNF already requires trigger — verify, don't re-add
</boundaries>

<verification>
- All 5 edge cases from issue #99 have corresponding compile rules
- EBNF grammar tightened where checked in issue
- No orphan references (every PGE/PGW in edge cases exists as a file)
- INDEX.md counts match actual edge case files
</verification>
