---
phase: 273-three-bracket-symbol-redesign
plan: 04
type: execute
wave: 4
depends_on: ["273-01"]
files_modified:
  - docs/user/jm3lib/**/*.md
autonomous: true
---

<objective>
## Goal
Apply three-bracket syntax changes to all jm3lib documentation (47 files).

## Purpose
jm3lib docs define standard library pipelines, expanders, collectors, and types. Must match EBNF grammar.

## Output
47 markdown files updated with consistent three-bracket syntax.
</objective>

<context>
## Prior Work
@.paul/phases/273-three-bracket-symbol-redesign/273-01-SUMMARY.md

## Reference
@docs/technical/ebnf/05-block-elements.md
@docs/technical/ebnf/09-definition-blocks.md
@docs/technical/ebnf/10-execution.md
@docs/technical/ebnf/12-collections.md
</context>

<skills>
No specialized flows configured.
</skills>

<acceptance_criteria>

## AC-1: No stale markers in jm3lib docs
```gherkin
Given all files in docs/user/jm3lib/
When I grep for old markers [r], [p], {=}, ~ForEach, ~Into
Then zero matches are found
```

## AC-2: New syntax present
```gherkin
Given the updated files
When I grep for new markers {-}, (-), [-], [=], =ForEach
Then they appear in appropriate contexts
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Update jm3lib pipeline + type docs (~25 files)</name>
  <files>docs/user/jm3lib/INDEX.md, docs/user/jm3lib/pipelines/**/*.md, docs/user/jm3lib/types/*.md</files>
  <action>Same 16-step replacement order as 273-02/273-03. Read each file, apply, write back.</action>
  <verify>Grep for stale markers — zero matches.</verify>
  <done>AC-1 partially satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Update jm3lib expander + collector docs (~22 files)</name>
  <files>docs/user/jm3lib/expanders/**/*.md, docs/user/jm3lib/collectors/**/*.md</files>
  <action>Same 16-step replacement order. Heavy ~ForEach → =ForEach and ~Into → =Into here.</action>
  <verify>Grep for stale markers — zero matches.</verify>
  <done>AC-1 and AC-2 satisfied</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- docs/technical/ebnf/ (273-01)
- docs/user/syntax/, docs/user/concepts/ (273-02)
- docs/technical/compile-rules/ (273-03)
- Comparison/assignment operators, definition headers, unchanged markers
</boundaries>

<verification>
- [ ] Grep docs/user/jm3lib/ for `[r]`, `[p]`, `{=}`, `~ForEach`, `~Into` — zero matches
- [ ] New markers present
</verification>

<success_criteria>
- All 47 jm3lib files updated
- No stale markers remain
</success_criteria>

<output>
After completion, create `.paul/phases/273-three-bracket-symbol-redesign/273-04-SUMMARY.md`
</output>
