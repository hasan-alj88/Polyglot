---
phase: 273-three-bracket-symbol-redesign
plan: 03
type: execute
wave: 3
depends_on: ["273-01"]
files_modified:
  - docs/technical/COMPILE-RULES.md
  - docs/technical/compile-rules/PGE/*.md
  - docs/technical/compile-rules/PGW/*.md
  - docs/technical/compile-rules/algorithms/*.md
autonomous: true
---

<objective>
## Goal
Apply three-bracket syntax changes to all compile rule documentation (138 files).

## Purpose
Compile rules define the compiler's validation behavior. They must use the same syntax as the EBNF grammar (273-01) and user docs (273-02).

## Output
138 markdown files updated with consistent three-bracket syntax.
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work
@.paul/phases/273-three-bracket-symbol-redesign/273-01-SUMMARY.md
@.paul/phases/273-three-bracket-symbol-redesign/273-02-SUMMARY.md

## Reference (new authoritative grammar)
@docs/technical/ebnf/05-block-elements.md
@docs/technical/ebnf/09-definition-blocks.md
@docs/technical/ebnf/10-execution.md
@docs/technical/ebnf/12-collections.md
</context>

<skills>
No specialized flows configured.
</skills>

<acceptance_criteria>

## AC-1: No stale markers in compile-rules
```gherkin
Given all files in docs/technical/compile-rules/
When I grep for old markers [r], [p], {=}, ~ForEach, ~Into
Then zero matches are found
```

## AC-2: No stale markers in COMPILE-RULES.md
```gherkin
Given docs/technical/COMPILE-RULES.md
When I grep for old markers
Then zero matches are found
```

## AC-3: New syntax present and consistent
```gherkin
Given the updated files
When I grep for new markers {-}, (-), [-], [=], =ForEach
Then they appear in appropriate contexts matching EBNF grammar
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Update PGE 01-04 compile rules (~40 files)</name>
  <files>docs/technical/compile-rules/PGE/PGE01*.md, docs/technical/compile-rules/PGE/PGE02*.md, docs/technical/compile-rules/PGE/PGE03*.md, docs/technical/compile-rules/PGE/PGE04*.md</files>
  <action>
    Apply all replacements following strict ordering to avoid collisions:

    **Replacement order (critical — symbol reuse requires this sequence):**
    1. `{=}` → `{-}` (pipeline definition blocks)
    2. `[=]` → `(-)` (pipeline IO — BEFORE [p] → [=])
    3. `[~]` → `(=)` (expander IO — BEFORE [+] → [~])
    4. `[*]` → `(*)` (collector IO)
    5. `[>]` → `(>)` (output param handling)
    6. `[<]` → `(<)` (input param handling)
    7. `[p]` → `[=]` (parallel execution — AFTER old [=] gone)
    8. `[r]` → `[-]` (sequential execution)
    9. `[⏐]` or `[|]` (trigger OR) → `[+]`
    10. `[+]` (line continuation) → `[~]` (AFTER old [⏐] → [+])
    11. `=>` → `->` (chain operator — ONLY in chain context, NOT comparison)
    12. `~ForEach` → `=ForEach`, `~Into` → `=Into`
    13. Pipeline prefix `=` → `-` in pipeline refs (=T.*, =Q.*, =W.*, =File.*, =Math.*, etc.)
    14. `.~` → `.=` (level iteration suffix)
    15. `%=` → `%-` (metadata pipeline branch)

    **Context-sensitive:** Do NOT change `=?`, `=!?`, `<=?`, `>=?`, `<<`, `>>`, `<~`, `~>`, `{#}`, `{T}`, etc.
    **Import context:** `@alias=Pipeline` → `@alias-Pipeline`

    Read each file, apply replacements, write back.
  </action>
  <verify>Grep docs/technical/compile-rules/PGE/PGE0[1-4]* for `[r]`, `[p]`, `{=}`, `~ForEach` — zero matches.</verify>
  <done>AC-1 partially satisfied (PGE 01-04)</done>
</task>

<task type="auto">
  <name>Task 2: Update PGE 05-12 compile rules (~55 files)</name>
  <files>docs/technical/compile-rules/PGE/PGE05*.md, docs/technical/compile-rules/PGE/PGE06*.md, docs/technical/compile-rules/PGE/PGE07*.md, docs/technical/compile-rules/PGE/PGE08*.md, docs/technical/compile-rules/PGE/PGE09*.md, docs/technical/compile-rules/PGE/PGE10*.md, docs/technical/compile-rules/PGE/PGE12*.md</files>
  <action>
    Same replacement order as Task 1.

    Special attention:
    - PGE06*: conditional rules with `[r]` match → `[-]`, many code examples
    - PGE07*: error handling rules with `[r]` → `[-]`, `[=]` IO → `(-)`
    - PGE08*: chain/auto-wire rules with `=>` → `->`, pipeline refs
    - PGE09*: import rules with `@alias=Pipeline` → `@alias-Pipeline`
    - PGE10*: permission rules with `{=}` → `{-}`
    - PGE12*: inline pipeline rules with pipeline prefix changes
  </action>
  <verify>Grep docs/technical/compile-rules/PGE/PGE0[5-9]*, PGE1*, PGE12* for stale markers — zero matches.</verify>
  <done>AC-1 partially satisfied (PGE 05-12)</done>
</task>

<task type="auto">
  <name>Task 3: Update PGW rules + algorithms + COMPILE-RULES.md (~25 files)</name>
  <files>docs/technical/compile-rules/PGW/*.md, docs/technical/compile-rules/algorithms/*.md, docs/technical/COMPILE-RULES.md</files>
  <action>
    Same replacement order as Task 1.

    Special attention:
    - PGW02*: variable/lifecycle warnings with `[r]`/`[p]` → `[-]`/`[=]`
    - PGW07*: error handler warnings with pipeline refs
    - PGW08*: auto-wire warnings with chain operator
    - PGW09*: import warnings with `@alias=Pipeline` → `@alias-Pipeline`
    - algorithms/cycle-detection.md: pipeline refs in algorithm examples
    - COMPILE-RULES.md: index file with minimal marker refs
  </action>
  <verify>Grep docs/technical/compile-rules/PGW/, algorithms/, COMPILE-RULES.md for stale markers — zero matches.</verify>
  <done>AC-1 and AC-2 fully satisfied</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- docs/technical/ebnf/ (already updated in 273-01)
- docs/user/ (already updated in 273-02)
- docs/user/aj3lib/ (plan 273-04)
- docs/technical/edge-cases/ (plan 273-05)
- docs/technical/brainstorming/ (plan 273-05)
- docs/technical/spec/ (plan 273-05)
- docs/technical/plan/ (plan 273-05)
- docs/audit/ (writing rules, not syntax examples)
- lib/ source files (plan 273-05)

## SCOPE LIMITS
- Only update docs/technical/compile-rules/ and docs/technical/COMPILE-RULES.md
- Do NOT change comparison operators (=?, <?, etc.)
- Do NOT change assignment operators (<<, >>, <~, ~>)
- Do NOT change definition headers ({#}, {T}, {W}, {Q}, {N}, {!}, {_}, {@})
- Do NOT change unchanged markers: [T], [Q], [W], [?], [!], [#], [b], [\], [/], [{], [}], [.], [:], [C], [%], [ ], [@], [_], [&], [^]

</boundaries>

<verification>
Before declaring plan complete:
- [ ] Grep docs/technical/compile-rules/ for `[r]`, `[p]`, `{=}`, `~ForEach`, `~Into` — zero matches
- [ ] Grep docs/technical/COMPILE-RULES.md for same — zero matches
- [ ] Grep both for `{-}`, `(-)`, `[-]`, `[=]` — present
- [ ] No comparison operators damaged (=?, <?, >=?)
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All 138 compile rule files updated with three-bracket syntax
- No stale markers remain
- New syntax consistent with EBNF grammar from 273-01
</success_criteria>

<output>
After completion, create `.paul/phases/273-three-bracket-symbol-redesign/273-03-SUMMARY.md`
</output>
