---
phase: 162-native-dispatch-config-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/user/jm3lib/types/NativeKind.md
  - docs/user/concepts/pipelines/INDEX.md
  - docs/technical/compile-rules/PGE/PGE01028-base-derived-mutual-exclusion.md
autonomous: true
---

<objective>
## Goal
Update three docs files that still reference the old `base: Rust` config format to use the new per-operation native dispatch YAML config introduced in `native-dispatch.md`.

## Purpose
Consistency across documentation — the authoritative spec (`native-dispatch.md`) defines subsystem defaults with per-operation overrides, but three files still show the flat `base: Rust` pattern.

## Output
Three updated documentation files with config examples matching `native-dispatch.md`.
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Source Files
@docs/technical/spec/native-dispatch.md — authoritative new config format
@docs/user/jm3lib/types/NativeKind.md — target: lines 64-73
@docs/user/concepts/pipelines/INDEX.md — target: lines 130-138
@docs/technical/compile-rules/PGE/PGE01028-base-derived-mutual-exclusion.md — target: line 24 (sub-condition e)
</context>

<acceptance_criteria>

## AC-1: NativeKind.md config section updated
```gherkin
Given NativeKind.md has a "Configuration" section showing `base: Rust`
When the section is updated
Then it shows the new YAML schema (native.defaults + native.overrides)
And the prose explains subsystem defaults with per-operation overrides
And the PGE01028e reference is updated to match the new validation logic
```

## AC-2: pipelines/INDEX.md config subsection updated
```gherkin
Given pipelines/INDEX.md has a "Configuration" subsection under "Native vs Derived" showing `base: Rust`
When the subsection is updated
Then it shows the new YAML schema (native.defaults + native.overrides)
And the prose explains that all {N} definitions must have a binding for the configured language per subsystem
```

## AC-3: PGE01028 sub-condition (e) updated
```gherkin
Given PGE01028 sub-condition (e) references `base: Rust` config format
When the text and example are updated
Then sub-condition (e) describes the new config validation (subsystem defaults + overrides)
And the error message reflects the new resolution logic
And the INVALID example for PGE01028e matches the new config format
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Update NativeKind.md Configuration section</name>
  <files>docs/user/jm3lib/types/NativeKind.md</files>
  <action>
    Replace the "Configuration" section (lines 63-74) with:
    - New YAML example showing `native.defaults` (tm/qh/runner/pgcompiler) and `native.overrides`
    - Prose explaining subsystem defaults with per-operation overrides
    - Updated PGE01028e reference: validation checks that each {N} has a binding for the resolved language (default or override)
    - Keep the "future languages" note about extending .<Language> fields
    - Reference native-dispatch.md as the authoritative source
  </action>
  <verify>Read updated file; confirm no `base: Rust` flat config remains; YAML matches native-dispatch.md schema</verify>
  <done>AC-1 satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Update pipelines/INDEX.md Configuration subsection</name>
  <files>docs/user/concepts/pipelines/INDEX.md</files>
  <action>
    Replace the "Configuration" subsection under "Native vs Derived" (lines 130-138) with:
    - New YAML example showing `native.defaults` and `native.overrides`
    - Updated prose: "all {N} definitions must include a binding for the language resolved for their subsystem"
    - Keep brief — this is the user-facing overview, not the full spec
    - Reference [[technical/spec/native-dispatch|native-dispatch]] for full details
  </action>
  <verify>Read updated file; confirm no `base: Rust` flat config remains; YAML matches native-dispatch.md schema</verify>
  <done>AC-2 satisfied</done>
</task>

<task type="auto">
  <name>Task 3: Update PGE01028 sub-condition (e)</name>
  <files>docs/technical/compile-rules/PGE/PGE01028-base-derived-mutual-exclusion.md</files>
  <action>
    Update sub-condition (e) in the table and the INVALID example:
    - Table row: change trigger text from "Config says `base: Rust` but {N} has no `.Rust` field" to describe the new resolution (subsystem default or per-operation override)
    - Detection paragraph: update "configured base language" to "resolved language for the operation's subsystem"
    - INVALID example for PGE01028e: update comment to reference new config format
    - Keep all other sub-conditions (a-d) unchanged
  </action>
  <verify>Read updated file; confirm sub-condition (e) references subsystem defaults/overrides, not flat `base: Rust`</verify>
  <done>AC-3 satisfied</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- docs/technical/spec/native-dispatch.md (authoritative source — read only)
- All other docs files not listed in files_modified
- Sub-conditions a-d in PGE01028 (only e changes)

## SCOPE LIMITS
- Config format alignment only — no new content, no restructuring
- Do not add new compile rules or error codes
- Do not change {N} definition examples (those already show correct .<Language> fields)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `grep -r "base: Rust" docs/` returns no matches in the 3 target files
- [ ] All 3 files show `native.defaults` / `native.overrides` YAML schema
- [ ] PGE01028 sub-condition (e) references subsystem resolution, not flat base
- [ ] No other files were modified
</verification>

<success_criteria>
- All three files updated to reference new native dispatch config format
- YAML examples consistent with native-dispatch.md schema
- No stale `base: Rust` references remain in target files
</success_criteria>

<output>
After completion, create `.paul/phases/162-native-dispatch-config-update/162-01-SUMMARY.md`
</output>
