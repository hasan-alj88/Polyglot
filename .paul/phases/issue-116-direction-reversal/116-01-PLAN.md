---
phase: issue-116-direction-reversal
plan: 01
subsystem: docs
tags: [operators, EBNF, naming, push-pull]
---

# Issue #116 Plan 01: Rename Assignment Operator Names (PushLeft/PushRight)

**Objective:** Resolve Push/Pull direction reversal between io.md and other docs by renaming all operator names to Push + Direction, eliminating the Push/Pull ambiguity.

## Tasks

1. **EBNF definitions** (5 files) — Rename `final_push`/`final_pull` to `push_left`/`push_right`, same for default/fallback
2. **User-facing operator tables** (3 files) — Update display names in operators.md, variable-lifecycle.md, io.md
3. **Compile rules** (1 file) — Update operator-name usage in PGE02010
4. **Edge cases** (1 file) — Update operator-name usage in 08-expressions.md
5. **Verification** — Grep for old names, confirm 0 matches

## Acceptance Criteria

- AC-1: No instances of `final_push`/`final_pull` remain in docs/
- AC-2: No instances of `Push (Final)`/`Pull (Final)` remain in docs/
- AC-3: New EBNF names (`push_left`/`push_right`) present in all 5 EBNF files
- AC-4: New display names (`PushLeft`/`PushRight`) present in user docs
- AC-5: io.md summary table (lines 150-154) uses consistent push-left/push-right labels
