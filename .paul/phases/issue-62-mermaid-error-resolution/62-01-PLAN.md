---
phase: issue-62-mermaid-error-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/user/concepts/errors.md]
autonomous: true
---

<objective>
## Goal
Add a Mermaid `flowchart TD` diagram to errors.md that visualizes the error resolution precedence decision flow.

## Purpose
Issue #62 — A flowchart makes the error resolution hierarchy visually clear: how [!] blocks, <!Error.Name fallbacks, generic <! fallbacks, and no-fallback termination relate to each other.

## Output
Modified `docs/user/concepts/errors.md` with a Mermaid diagram inserted after the "### Precedence: `[!]` Before `<!`" heading (line 253) and the prose sentence (line 255), before the numbered list (line 257).
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Source Files
@docs/user/concepts/errors.md (lines 253-282 — Precedence section)
</context>

<acceptance_criteria>

## AC-1: Mermaid diagram present
```gherkin
Given the file docs/user/concepts/errors.md
When a reader opens the "### Precedence" section
Then a Mermaid `flowchart TD` diagram appears after the prose sentence and before the numbered list
```

## AC-2: Diagram shows decision flow
```gherkin
Given the Mermaid diagram
When rendered
Then it shows the error resolution decision tree:
  - Pipeline call errors (start)
  - "[!] block matches?" decision
  - "Pushes replacement?" decision
  - Variable Final outcomes for each path
  - "<!Error.Name fallback?" decision
  - "Generic <! fallback?" decision
  - No fallback → Failed/terminate outcome
```

## AC-3: No other content changes
```gherkin
Given the file docs/user/concepts/errors.md
When the diagram is added
Then no existing prose, code blocks, or headings are modified
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Insert Mermaid flowchart into Precedence section</name>
  <files>docs/user/concepts/errors.md</files>
  <action>
    Insert a Mermaid `flowchart TD` diagram after line 255 ("When both `[!]` blocks and `<!` fallback exist on the same pipeline call:") and before line 257 ("1. Pipeline call errors").

    The diagram should show:
    1. Start node: "Pipeline call errors"
    2. Decision: "[!] block matches?"
       - Yes → Decision: "Pushes replacement?"
         - Yes → "Variable Final (done)"
         - No → falls through to next decision
       - No → falls through to next decision
    3. Decision: "Error-specific fallback on output line?"
       - Yes → "Variable Final (fallback value)"
    4. Decision: "Generic fallback on output line?"
       - Yes → "Variable Final (fallback value)"
       - No → "Pipeline terminates / variable Failed"

    Use diamond shapes for decisions, rounded boxes for outcomes.
    Avoid special characters that break Mermaid parsing (no raw <, >, #, ;).
    Use quoted labels for nodes containing special characters.
    Keep labels concise.
  </action>
  <verify>
    - grep for "```mermaid" in errors.md confirms diagram present in Precedence section
    - grep for "flowchart TD" confirms correct diagram type
    - Numbered list "1. Pipeline call errors" still present after diagram
    - No other sections modified
  </verify>
  <done>AC-1, AC-2, AC-3 satisfied: Mermaid flowchart shows error resolution decision tree, no other changes</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- All content before "### Precedence: `[!]` Before `<!`" heading
- The heading itself and its prose sentence
- The numbered list (lines 257-264)
- Code block and explanation after the list
- Frontmatter

## SCOPE LIMITS
- Only add Mermaid diagram between the prose sentence and the numbered list
- Do not modify any other files

</boundaries>

<verification>
Before declaring plan complete:
- [ ] grep for "mermaid" in errors.md finds the diagram in Precedence section
- [ ] grep for "flowchart TD" matches
- [ ] Numbered list unchanged after diagram
- [ ] No other files modified
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- Mermaid `flowchart TD` diagram inserted in Precedence section
- Diagram shows error resolution decision tree with 4 paths
- No existing content modified
</success_criteria>

<output>
After completion, create `.paul/phases/issue-62-mermaid-error-resolution/62-01-SUMMARY.md`
</output>
