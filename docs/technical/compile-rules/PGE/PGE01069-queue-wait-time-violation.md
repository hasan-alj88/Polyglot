# PGE01069: Queue Wait Time Violation

code: PGE01069
description: A pipeline cannot define a local `<maxWaitTime` that exceeds the queue's `.maxWaitTime`.
category: Syntax and Structure

## Description

**Statement:** A pipeline cannot define a local `<maxWaitTime` that exceeds the queue's `.maxWaitTime`.
