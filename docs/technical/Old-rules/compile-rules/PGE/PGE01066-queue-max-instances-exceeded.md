# PGE01066: Queue Max Instances Exceeded

code: PGE01066
description: A pipeline's local `<maxInstances` cannot exceed the queue's `.maxInstances`.
category: Syntax and Structure

## Description

**Statement:** A pipeline's local `<maxInstances` cannot exceed the hard limit defined by the queue's `.maxInstances`.
