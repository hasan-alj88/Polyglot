# Go — Native Implementations (Placeholder)

Go support is planned but not yet implemented. When ready, this folder will contain the same structure as `rust/`:

- `integrator/` — Go SDK for bidirectional Polyglot integration
- `tm/` — Trigger Monitor native operations
- `qh/` — Queue Handler native operations
- `runner/` — Runner native operations
- `pgcompiler/` — Compiler implementation in Go

See `lib/README.md` for the overall architecture and `docs/technical/spec/native-dispatch.md#Adding a New Language` for the implementation guide.
